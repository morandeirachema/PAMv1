# pamv1 — Low-Level Architecture

> **Living document.** Update this whenever code structure, packages, schemas,
> wire formats, env vars or algorithms change. This is the engineer's map of
> the codebase; the conceptual view lives in
> [ARCHITECTURE-HIGH-LEVEL.md](ARCHITECTURE-HIGH-LEVEL.md).
>
> Last updated: 2026-08-09 · Reflects: Phases 0–87 + the 2026-07 hardening pass — through the 5250 management console (11), the configuration subsystem + custom-profile RBAC + hot-swap (12), the AI-agent access broker (13), SOPS-encrypted secrets (14), the PostgreSQL database session proxy (15), live session monitoring + command control (16), safes + dependent-account propagation (17), optional CyberArk Conjur secret sourcing (18), access certification campaigns (19), an ITSM/ticketing gate (20), richer approval workflows (21), Zero Standing Privilege via ephemeral SSH certificates (22), privileged threat analytics (23), a Conjur-style application-secrets API for non-agent apps (24), console parity (25 — 5250 screens for safes, campaigns, risk analytics, a live session viewer, and the request-workflow fields), session-recording playback + one-time access (26 — audited, hash-verified replay of stored recordings and consume-on-connect single-use approvals), broker completion (27 — approver-group separation of duties, periodic ed25519 in-chain audit checkpoints + signing-key rotation with JWKS publication + a min-entries truncation floor, OCSF SIEM export, and the MCP SSE transport with elicitation), operator SSH certificates (28 — proof-of-possession issuance of a short-lived cert for an operator's own key, scoped by principal + source-address, with KRL revocation), the third-party vendor access gate (29 — time-boxed, customer-approved contract grants with live employment attestation, an instant-offboard cascade, a window-close session sweeper, and per-vendor SOC 2/DORA evidence export), in-session policy + step-up (30 — numeric policy comparators for the agent broker, and a pause-for-supervisor step-up on the PostgreSQL session proxy that keeps the session open), and the identity blast-radius / CIEM engine (31 — a real AWS IAM effective-permission evaluator, pivot-vs-containment reachability, toxic-combination findings, and remediation-as-code over a normalized identity graph), SFTP file-transfer control (32 — the SSH proxy parses the SFTP subsystem stream to audit every file operation and, in read-only mode, refuse writes/deletes; `deny` disables it), RDP clipboard control (33 — `PAM_RDP_CLIPBOARD` gates the Guacamole clipboard bridge and always disables drive redirection), an HA cross-replica session kill-switch (34 — a kill published on any replica terminates the session wherever it is hosted, over Postgres LISTEN/NOTIFY), continuous audit→SIEM forwarding (35 — every audit event streamed as RFC 5424 syslog or CEF from a durable cursor), retention/pruning (36 — aged recordings and, when the chain is off, audit rows are swept on a schedule), and cross-replica live monitoring (55 — cluster-wide session listing over a shared heartbeat-refreshed inventory, and cluster-wide SSE watching over an interest-gated store relay), and cross-replica step-up decisions (56 — the pending list is cluster-wide over a shared TTL-bounded inventory whose statements rest sealed under the bus key, and a supervisor's decision on any replica is dispatched, sealed and freshness-bound, to the replica whose memory holds the pause), and RFC 8693 token-exchange minting + Terraform remediation (57 — the broker issues the delegated SVIDs it had only ever verified, and the blast-radius engine renders its cut as reviewable HCL), and safe-scoped policy (58 — a safe carries its own approval requirement and dual-control floor binding every target in it, strictest-wins, folded once and enforced at all five gates), and SFTP per-file content recording (59 — every file moved over SFTP leaves a sealed, hash-chained chunk-log artifact beside the session recordings, attributed in the audit trail and replayable from the console; the per-file cap refuses data past it, and an unparsable stream fails closed while capture is on) — plus a keyboard-first console and the security/correctness hardening passes and code-generated diagrams. Items that need external infrastructure or a paid account to build/verify honestly are catalogued in [EXTERNAL-INFRA-GAPS.md](EXTERNAL-INFRA-GAPS.md). See the [ROADMAP](../ROADMAP.md) for authoritative per-phase status. Commit the doc update with the code change.

## 1. Language, layout, dependencies

- **Go** (module `github.com/morandeirachema/pamv1`, `go 1.26`).
- Standard layout: `cmd/` entrypoints, `internal/` non-exported packages.
- Direct deps: [`jackc/pgx/v5`](https://github.com/jackc/pgx) (Postgres), [`golang.org/x/crypto/ssh`](https://pkg.go.dev/golang.org/x/crypto/ssh) (proxy). Standard library otherwise.

```
cmd/pam-server/main.go        # wiring, flags (-genkey, -hashkey), lifecycle
internal/
  config/      # env (PAM_*) -> Config
  logging/     # slog setup (json/text, level) + per-service loggers
  vault/       # AES-256-GCM encrypt/decrypt, key gen
  sshca/       # Zero Standing Privilege SSH certificate authority (mints short-lived user certs)
  analytics/   # privileged threat analytics — behavioral risk scoring over the audit trail
  mfa/         # TOTP (RFC 6238) generate/validate + otpauth URI
  winrm/       # WinRM command Runner (Windows targets) + real client (basic/NTLM)
  guacd/       # Apache Guacamole protocol client (RDP handshake + JIT injection)
  oidc/        # OIDC Authorization Code + PKCE, RS256 JWKS validation
  maint/       # offline maintenance (vault KEK rotation)
  rotate/      # credential rotation/verify connectors (SSH chpasswd, ssh_key, WinRM)
  discovery/   # TCP port scan → onboard targets (ssh/winrm/rdp)
  metrics/     # dependency-free Prometheus /metrics
  session/     # live-session registry + hub + cross-replica kill/live/step-up buses, shared proxy↔api
  shamir/      # Shamir secret sharing (GF(2^8)) for break-glass quorum
  alert/       # real-time alert delivery (webhook, syslog, email), break-glass events
  auditfwd/    # continuous audit→SIEM forwarder (RFC 5424 syslog / CEF, durable cursor)
  ratelimit/   # shared per-key fixed-window limiter (API auth, proxies, broker)
  conjur/      # CyberArk Conjur bootstrap-secret sourcing at startup (Phase 18)
  ticket/      # ITSM/ticketing gate — regex + webhook ticket validation (Phase 20)
  auth/        # roles, capabilities, Principal, Resolver (RBAC), LDAP/Entra/OIDC
  auditchain/  # keyed-HMAC tamper-evident chain + ed25519 signed checkpoints (broker + primary audit)
  # AI-agent access broker (Phase 13):
  agentid/     # agent identity + SPIFFE JWT-SVID verification
  policy/      # broker policy engine (allow/deny over tool + args)
  broker/      # JIT server-side tool execution, verifiable audit, approval/resume
  mcp/         # Model Context Protocol transport for the broker
  store/       # Store interface + domain types + CredentialAAD
    memstore/  # in-memory impl (tests, demo)
    pgstore/   # PostgreSQL impl + embedded versioned migrations
    storetest/ # shared store-conformance suite (memstore + live pgstore)
  api/         # REST handlers, authz middleware, user administration
  cmdguard/    # command denylist, shared by the proxies AND the API/broker paths
  recording/   # at-rest encryption for session recordings (chunked AEAD under the KEK)
  keycustody/  # shared custody of the SSH host + ZSP CA keys and the broker audit keys across replicas
  tds/         # Microsoft SQL Server wire-protocol codec (framing, LOGIN7, refusal tokens)
  proxy/       # SSH + PostgreSQL + SQL Server gateways, JIT injection, recording, sftpguard
  web/         # embedded AS/400 portal (static/index.html) + vendored RDP viewer (guacamole-common.min.js)
```

## 2. Package contracts

### 2.1 `vault`

**Envelope encryption** (the KMS/PAM-vendor pattern). Each secret is sealed with
a fresh random **data key** (AES-256-GCM, 12-byte nonce, `aad` authenticated),
and that data key is **wrapped by a KEK** (Key Encryption Key). Token format is
versioned: `"v2:" + base64url( uint16(len(wrappedDEK)) || wrappedDEK || nonce ||
ciphertext )`. `Encrypt(ctx, plaintext, aad)` / `Decrypt(ctx, token, aad)` are
context-aware because a KMS KEK makes network calls; data keys are zeroed after use.

The **KEK is pluggable** (`KEK` interface, `NewKEK(KEKOptions)`):

- `LocalKEK` — AES-256-GCM key from `PAM_MASTER_KEY` (base64). **Dev/test only** —
  the key sits in an env var.
- `TransitKEK` — HashiCorp [Vault Transit](https://developer.hashicorp.com/vault/docs/secrets/transit)
  over HTTPS (`transit.go`); the KEK never leaves Vault, wrap/unwrap round-trip
  the data key. **Production / vendor-aligned.**
- `AWSKMSKEK` — AWS KMS (`awskms.go`): the data key is Encrypt/Decrypt'd by KMS,
  so the CMK never leaves KMS. **Production.** The `kmsAPI` client is an interface
  (tests inject a fake).
- `PKCS11KEK` — on-prem **HSM** via a PKCS#11 module (`pkcs11.go`, build tag
  `pkcs11`): an AES key inside the token wraps/unwraps the data key
  (`CKM_AES_CBC_PAD`); the KEK never leaves the HSM. Needs cgo + a dynamic loader,
  so it is **excluded from the default static/distroless build** — a `pkcs11_stub.go`
  (`!pkcs11`) returns a clear "not built in" error. Verified against SoftHSM2 in CI.

`GenerateMasterKey()` → 32 random bytes, urlsafe-base64 (seeds the local KEK).
Selected by `PAM_KEK_PROVIDER` (`local` | `vault-transit` | `aws-kms` | `pkcs11`).

### 2.2 `store`

Interface `Store` with `memstore` and `pgstore` implementations. Domain types:
`Target`, `Credential` (field `SecretEnc` is `json:"-"` — **never serialized**),
`AuditEvent`. Sentinel errors `ErrNotFound`, `ErrConflict`.
`CredentialAAD(targetID, credID) = "target:<id>/cred:<id>"` — shared by `api` and `proxy` so vault
AAD matches on both encrypt and decrypt paths.

Objects are **editable in place** (Phase 44): `UpdateTarget` (name/host/port/
os/protocol/approval — never the safe assignment), `UpdateSafe` (name/
description), `UpdateUserRole` and `UpdateVendorOrg`, each `ErrNotFound` on a
missing row and `ErrConflict` on a name collision — so fixing a port no longer
means delete + recreate, which cascades away credentials, grants, dependencies
and safe assignment. Grants and safe members deliberately stay create + delete:
they have no dependents to lose, and two audited events (deleted, created) are a
clearer trail than one mutated row. The top-level list reads (`ListTargets`,
`ListCredentials`, `ListUsers`, `ListSafes`, `ListCheckouts`,
`ListAccessRequests`, `ListVendors`) take a shared **(limit, afterID) window**
— id-ascending, strictly after the cursor, `limit <= 0` = uncapped for
in-process sweeps only; the HTTP layer always clamps (1..500, default 100).

Schema is applied by an embedded **migration runner** (`pgstore/migrate.go`):
ordered `migrations/*.sql` files run once each in a transaction, tracked in a
`schema_migrations` table. `0001_init.sql` is the idempotent baseline (safe on a
pre-migrations database); later changes are new numbered files (through the latest
numbered migration — `0025_live_sessions.sql` at time of writing), applied under a
`pg_advisory_lock` so concurrent replicas don't race. Tables: `targets`,
`credentials` (FK `ON DELETE CASCADE`), `target_grants`, `audit_events` (with the
optional `prev_hash`/`hmac` chain columns from `0018`), `users`, `sessions`,
`mfa_enrollments`, `mfa_recovery_codes`, `access_requests`, `checkouts` (partial
UNIQUE index — one active lease per credential), `oidc_states`, `settings` (config
overrides), `profiles` (custom capability sets), `safes` + `safe_members`,
`credential_dependencies`, `campaigns` + `campaign_items`, `agent_keys`,
`broker_audit_events` (the hash-chained agent-broker audit log), `broker_tokens`,
`app_keys` + `app_secret_grants`, `ssh_certificates` (operator-cert revocation
handles, `0020`), `vendors` + `vendor_grants` (`0021`), `key_material`
(shared custody of the host/CA keys and, when not env-set, the broker
audit-chain keys, `0022`), and `live_sessions` (`0025`, **UNLOGGED** — the
cross-replica live-session inventory, heartbeat-refreshed and freshness-filtered
at read time, so it is rebuilt in seconds and crash-losing it is correct).
Cross-process serialization uses Postgres
advisory locks keyed `pam_mig` (migrations), `pam_br` (broker chain), `pam_audc`
(primary audit chain), and `pam_lfc`/`pam_ana` (worker leader election).

### 2.3 `auth` *(Phase 3a)*

RBAC. Four `Role`s — `admin`, `user`, `auditor`, `approver` — each granted a set
of `Capability` values by the authoritative `roleCaps` matrix (`Role.Can(cap)`).
`Principal{Name, Role, BreakGlass}` is the authenticated identity.

`Resolver.Resolve(ctx, key)` maps a presented key to a Principal, trying in
order: the bootstrap admin key (`PAM_API_KEY`) → `admin`; the break-glass key →
`admin` with `BreakGlass=true`; a per-user token (`store.GetUserByTokenHash`);
then a **login session token** (`store.GetSessionByTokenHash`, non-expired).
Shared by `api` and `proxy` so both enforce the same identities and roles.
`auth` imports `store` (no cycle).

**Password identity sources** (`Authenticator` interface, Phase 3b): verify
username+password and return a Principal whose role comes from the directory.
Pluggable and composable via `NewChain` (try each, first success wins):

- `LDAPAuthenticator` (`ldap.go`) — on-prem AD: binds a service account, searches
  the user under `BaseDN`, reads `memberOf`, re-binds as the user to verify the
  password, maps groups → roles (`MatchedRoles` — a user in several mapped groups
  keeps **all** of them, carried on `Principal.Roles`, and `Can` evaluates the
  **union** of their capabilities). The LDAP connection is behind an `ldapConn`
  interface so tests inject a fake; real dial uses LDAPS.
- `EntraAuthenticator` (`entra.go`) — Microsoft Entra ID (Azure AD): OAuth2 ROPC
  grant to the tenant token endpoint (over TLS, back-channel), requesting `openid`
  so Entra returns an **id_token**. It **validates the id_token's RS256 signature
  against the tenant JWKS** (`oidc.VerifyRS256`, plus audience + expiry) before
  reading `roles` (app roles) / `groups` claims and mapping to a role.
  `AuthorityHost` supports sovereign clouds; the endpoints are overridable in
  tests. Note: ROPC still skips IdP-side Conditional Access/MFA (use pamv1's TOTP;
  prefer the OIDC auth-code flow for production).

`POST /api/login` runs the configured authenticator, enforces TOTP if enrolled,
and issues a session token (see `api`). `HighestRole` is the shared claim→role
mapper used by LDAP, Entra and OIDC.

**OIDC login** (`internal/oidc`, Phase 3b hardening): the production-grade
browser flow. `GET /api/auth/oidc/start` generates PKCE + state + nonce and
persists them via `store.PutOIDCState` (shared, so any replica can complete the
login — HA), then redirects to the IdP. `GET /api/auth/oidc/callback`
atomically takes the state (`store.TakeOIDCState`, single-use), exchanges the
code, and **verifies the ID token's RS256 signature against the IdP JWKS** plus
issuer/audience/nonce/exp, maps roles/groups → role, issues a session, and
redirects to the portal with the token in the URL fragment. Unlike ROPC, the IdP
performs the login (so its Conditional Access / MFA apply); pamv1 does not layer
its own TOTP on OIDC.

Capability grants: admin = all; user = `CapReadInventory`+`CapConnect`; auditor =
`CapReadInventory`+`CapReadAudit`; approver = auditor + `CapApprove` (approval
endpoints arrive with the OT/approval phase).

### 2.4 `api`

- Router: Go 1.22+ `http.ServeMux` pattern methods (`GET /api/targets/{id}` …).
- `authz(cap, handler)` middleware: resolves the `X-API-Key` into a Principal via
  `auth.Resolver`, emits the loud `breakglass.access` audit if applicable, then
  enforces `Principal.Role.Can(cap)` (403 + `authz.denied` audit otherwise). The
  Principal goes in the request context; `actorFrom` reads its name for audit.
- Each route declares its required capability (e.g. `POST /api/targets` needs
  `CapManageTargets`, `GET /api/audit` needs `CapReadAudit`).
- User administration (`CapManageUsers`): `POST /api/users` mints a token and
  returns it **once**; only the SHA-256 is stored. `GET`/`DELETE /api/users`.
- **Break-glass quorum** (`breakglass_handlers.go`, Phase 6): `POST
  /api/breakglass/unseal` (public, rate-limited) collects Shamir shares in an
  in-memory `unsealState`; when `PAM_BREAK_GLASS_THRESHOLD` shares reconstruct a
  key whose SHA-256 matches `PAM_BREAK_GLASS_KEY_HASH`, it issues a short-lived
  session (`SessionScopeBreakGlass` → admin + `BreakGlass=true`, auto-expiring).
  Every break-glass use fires an `alert.Notifier` (webhook). Shares are produced
  offline by `pam-server -split-key`; the server holds none.
- Login (`authn` optional): `POST /api/login` (public) verifies username+password
  via the configured `Authenticator`, then — if the user has a **confirmed TOTP
  enrollment** — requires a valid `otp` (401 `mfa_required` if missing), and
  issues a session token (12h TTL, stored as SHA-256) with the role from the
  directory; `POST /api/logout` revokes it. Returns 503 when no authenticator is
  configured.
- MFA (`internal/mfa`, self-service, authenticated): `POST /api/mfa/enroll`
  generates a TOTP secret (returned once, plus an `otpauth://` URI), stored
  **vault-encrypted** (AAD `mfa:<user>`), unconfirmed; `POST /api/mfa/verify`
  confirms it with a code; `POST /api/mfa/recovery-codes` issues 10 single-use
  backup codes (hashes stored); `GET /api/mfa` status; `DELETE /api/mfa` disables.
  A login OTP accepts a TOTP code **or** a recovery code (consumed on use).
- **Enforce-MFA policy** (`Options.MFARequired`, `PAM_MFA_REQUIRED`): when a user
  with no confirmed MFA logs in, they get an **enrollment-only session**
  (`store.Session.Scope="enroll"` → `Principal.EnrollOnly`) that only the MFA
  endpoints accept — `authz` and the proxy reject it (403 / `session.denied`)
  until enrollment is confirmed and the user re-logs in with a full session.
- Handlers validate input (os_type ∈ {linux,windows}, protocol ∈
  {ssh,winrm,rdp,vnc,postgres,mssql}), translate store errors to HTTP codes, and append
  audit events.
- **Edit-in-place + bounded lists** (Phase 44): `PUT /api/targets/{id}`,
  `/api/safes/{id}` (both `CapManageTargets`), `/api/users/{id}` and
  `/api/vendors/{id}` (both `CapManageUsers`) update an object with the same
  validation and authorization as create — `user.update` also re-runs the
  privilege-escalation guard (`Covers`), so a delegated user-admin cannot
  promote past their own capabilities, and the user's token survives a role
  change (no re-mint). Every top-level inventory list (`/api/targets`,
  `/api/credentials`, `/api/users`, `/api/safes`, `/api/checkouts`,
  `/api/access-requests`, `/api/vendors`) serves a `?limit=&after=` window
  (default 100, clamped 1..500, id-ascending) — `listWindow` — so no
  authenticated caller can pull an unbounded result set; the console drains the
  cursor page by page (`apiAll`).
- `reveal` (needs `CapRevealSecret`, admin only) decrypts on demand and is audited.
- **WinRM** (`POST /api/targets/{id}/winrm`, needs `CapConnect`): the Windows
  counterpart to the SSH proxy. Resolves the target (must be `protocol=winrm`),
  decrypts its credential **just-in-time**, runs the command via `winrm.Runner`,
  writes a transcript to `RecordingDir` with its SHA-256 in the audit, and
  returns stdout/stderr/exit — the secret is never returned. `winrm.Runner` is an
  interface (tests inject a fake); `winrm.Client` wraps masterzen/winrm over HTTPS
  with basic or NTLMv2 auth (`PAM_WINRM_AUTH`).
- **RDP** (`GET /api/targets/{id}/rdp`, WebSocket, needs `CapConnect`): brokers RDP
  through Apache Guacamole. Authorizes via a `token` query param (browsers can't
  set WS headers), resolves the `protocol=rdp` target, decrypts the credential
  **just-in-time**, and `guacd.Connect` performs the Guacamole handshake injecting
  the credential (`internal/guacd`) — it reaches guacd, never the browser. It
  gates on `session.Registry.AllowNew` (the same `PAM_MAX_SESSIONS_*` cap the SSH
  and PostgreSQL proxies enforce) **before** decrypting the secret, then emits the
  **tunnel prelude** guacamole-common-js requires — an internal tunnel-UUID
  instruction, then a re-emitted `ready` (the handshake consumed guacd's own
  `ready` to learn the connection id) — and bridges the WebSocket ↔ guacd stream.
  The bridge forwards **one whole Guacamole instruction per WebSocket message**
  (`guacd.Conn.NextInstruction`): guacamole-common-js parses each message
  independently and rejects a partial one, so a raw byte-stream copy would split a
  large screen paint and break the viewer. Enabled by `PAM_GUACD_ADDR`; audited
  `rdp.connect`/`rdp.end`.
- **RDP token** (`POST /api/rdp-token`, needs `CapConnect`): mints a short-lived
  (60s) **RDP-tunnel-scoped** session token (`auth.SessionScopeRDP`) so the viewer
  keeps the operator's long-lived key out of the WebSocket URL. The token resolves
  to a `TunnelOnly` principal that the `authz`/`authenticated` middleware refuse
  (403), so a copy leaked from a proxy/access log cannot call any other endpoint
  or re-mint itself; only `rdpTunnel` (which resolves its own principal) accepts
  it. A break-glass caller instead gets a break-glass-scoped token so the tunnel
  still fires the loud `breakglass.access` audit and bypasses the approval gate.
  Audited `rdp.token` (detail `ttl:60s`). The browser viewer is the vendored
  guacamole-common-js served by `web` (`GET /static/guacamole-common.min.js`); the
  portal opens it from *Work with Targets* option 7 and renders to a canvas.
  WebSocket upgrade rides the access-log middleware, so `statusWriter` forwards
  `http.Hijacker` (a missing `Hijack` had broken every upgrade with 501).

- **Credential lifecycle** (`lifecycle_handlers.go` + `internal/rotate`, Phase 7,
  needs `CapManageCredentials`): rotation and reconciliation run over the same
  secure protocols as the proxy.
  - `POST /api/credentials/{id}/rotate`: generates a strong password
    (`rotate.GeneratePassword` — shell-safe alphabet, guaranteed complexity),
    sets it on the target via a protocol `Rotator` (SSH `chpasswd` fed on stdin;
    WinRM `net user`), then re-encrypts it into the vault and stamps `rotated_at`
    (`store.RotateCredentialSecret`). The new secret is **never returned**.
  - `POST /api/credentials/{id}/reconcile[?remediate=true]`: a `Verifier` checks
    the vaulted secret still authenticates (SSH handshake / WinRM probe). Drift is
    reported as `out_of_sync`; with `remediate=true` an out-of-band change is
    healed by rotating to a PAM-managed secret.
  - `GET /api/reconcile`: read-only drift scan over every credential (safe to
    schedule). All three audit `credential.rotate` / `credential.reconcile` /
    `credential.remediate`.
  - **Background worker** (`scheduler.go`, `RunLifecycleWorker`): on
    `PAM_ROTATE_INTERVAL_MIN` it reconciles all credentials and rotates password
    ones older than `PAM_ROTATE_MAX_AGE_HOURS` (actor `system-scheduler`).
  - `Rotator`/`Verifier` are interfaces keyed by protocol in `Options`; tests
    inject fakes, and the SSH connector is proven against an in-process SSH server.
  - **Checkout/check-in** (`store.Checkout`, migration `0003`): `POST
    /api/credentials/{id}/checkout` (needs `CapRevealSecret`) grants an exclusive
    lease (`PAM_CHECKOUT_TTL_MIN`) and returns the secret; a second holder gets
    409. `/checkin` ends the lease and rotates the credential so the seen password
    is dead. `GET /api/checkouts` lists them. Honors `PAM_REVEAL_DISABLED`.
  - **Discovery** (`internal/discovery`, `POST /api/discovery/scan`, needs
    `CapManageTargets`): TCP-probes hosts for known management ports
    (22→ssh, 1433→mssql, 3389→rdp, 5900→vnc, 5985/5986→winrm) and, with `create:true`, onboards new
    targets. Reachability only — no credentials are used.

- **Access-request approval** (`approval_handlers.go`, Phase 8 — OT 4-eyes):
  `POST /api/access-requests` (needs `CapConnect`) files a request; `GET
  /api/access-requests` + `POST /api/access-requests/{id}/{approve,deny}` (needs
  `CapApprove`) let an approver decide. The **four-eyes** rule refuses
  self-approval (approver ≠ requester); an approval is valid for
  `PAM_APPROVAL_WINDOW_MIN`. Enforcement (`enforceApproval`): when a target sets
  `require_approval` or the global `PAM_REQUIRE_APPROVAL` is on, the WinRM/RDP
  handlers and the SSH proxy require `store.HasActiveApproval` before brokering;
  **break-glass bypasses**. Decisions audit + fire the `alert.Notifier`.
- **Air-gap mode** (`Options.AirGap`, `PAM_OT_AIRGAP`): forces the alerter to
  `alert.Noop` so an isolated OT deployment makes no outbound calls.
- **Audit export** (`compliance_handlers.go`, Phase 9 — NIS2, needs
  `CapReadAudit`): `GET /api/audit/export` returns a `store.ExportAudit` slice
  filtered by `since`/`until`/`actor`/`action` as JSON or CSV, with a **SHA-256**
  over the canonical event list in the body and the `X-PAM-Export-SHA256` header
  (tamper evidence for Art. 23 incident reports). The export is audited
  (`audit.export`).
- **Observability** (Phase 10, unauthenticated like `/healthz` — restrict at the
  network): `GET /metrics` renders a dependency-free Prometheus exposition
  (`internal/metrics`: `pam_http_requests_total{status}`, `pam_audit_events_total`,
  `pam_breakglass_access_total`, `pam_auth_failures_total`,
  `pam_credential_rotations_total`, `pam_active_sessions` gauge). `GET /healthz`
  is liveness; `GET /readyz` is readiness (calls `store.Ping`, 503 if the DB is
  unreachable). All three are skipped by the access log + request counter.

### 2.5 `proxy`  *(Phase 2, RBAC in 3a)*

SSH gateway. See §3 for the wire-level flow.

- `New(store, vault, resolver, Config)` → `Proxy`; `ListenAndServe(ctx, addr)` or
  `Serve(ctx, ln)` (tests inject a `127.0.0.1:0` listener).
- **Auth** (`authenticate`): `PasswordCallback` resolves the SSH password (a PAM
  key or per-user token) into a Principal via `auth.Resolver`. The SSH *username*
  selects the target: `splitLogin` parses `creduser@target` (rightmost `@`) or
  bare `target`. Principal name/role + target stashed in `ssh.Permissions.Extensions`.
- **Authz**: after the handshake, a connection whose role lacks `CapConnect`
  (e.g. `auditor`, `approver`) is rejected with a `session.denied` audit before
  any target is resolved.
- **Gates** (in order, each denial audited): protocol allowlist
  (`PAM_ALLOWED_PROTOCOLS`), per-target grants (`CanConnectTarget`), and the
  4-eyes approval gate (`HasActiveApproval`; break-glass bypasses). Non-`ssh`
  targets branch to the WinRM loop (`serveWinRM`) when a runner is configured.
- **Resolve + JIT decrypt** (`resolveTarget` → `decryptSecret`): look up target
  by name and pick the credential (matching `creduser` or first) *without*
  decrypting, then **decrypt the secret only after every gate passes** — plaintext
  never exists for a denied session. The AAD binds the ciphertext to
  `(target, credential)` (`store.CredentialAAD`).
- **Upstream** (`dialUpstream`): `ssh.Dial` to `host:port` as the credential
  user, `ssh.Password` or `ssh.PublicKeys` (for `secret_type=ssh_key`). The
  `HostKeyCallback` verifies the target's host key against `PAM_SSH_KNOWN_HOSTS`
  (`knownhosts`); when unset it falls back to trust-any with a **startup warning**
  (`proxy.Config.UpstreamHostKey`; the rotation connector shares the callback).
- **Session** (`handleSession`): for each `session` channel, open an upstream
  session channel and bidirectionally forward: channel requests via
  `pumpRequests` (pty-req, shell, exec, window-change, exit-status), stdin,
  stdout (tee'd into the recording), stderr.
- **Recording** (`record.go`): asciicast v2 (`{header}` line + `[t,"o",data]`
  events) written to `PAM_RECORDING_DIR`, hashed with SHA-256 as it is written;
  on close the audit stores path, byte count, file hash and a **chain hash**
  (`recordChain`: `SHA-256(prev-chain ‖ file-hash)`, head persisted to `.chain`)
  so recordings are tamper-evident as a sequence, not just individually.
- **Host key** (`hostkey.go`): `PAM_SSH_HOST_KEY` PEM path (persisted, generated
  if missing) or ephemeral ed25519.
- **Also**: a `+observe` login suffix opens a read-only session (operator
  keystrokes dropped, output still streamed/recorded); `Config.Jump` reaches
  targets through an SSH bastion (`direct-tcpip`); `Config.WinRMRunner` brokers an
  interactive WinRM command loop; `Config.OnSessionEnd` fires post-session
  credential rotation; `Config.RequireRecording` refuses a session that can't be
  recorded. Shutdown is a **bounded drain**: `Serve` force-closes active
  connections on `ctx` cancel and waits for handlers, and closing audits are
  written detached from the cancelled context.

### 2.5a `sshca` *(Phase 22 — Zero Standing Privilege)*

An SSH **certificate authority** for the ZSP model: no standing secret is stored
for an account; the proxy mints a short-lived user certificate just-in-time per
session. `LoadOrCreate(PAM_SSH_CA_KEY)` persists an ed25519 CA key (generated on
first use, like the host key). `IssueUser(principal, ttl, keyID)` generates a
**fresh ephemeral keypair** (used for one dial, then discarded), builds an
`ssh.Certificate` (`UserCert`, `ValidPrincipals=[principal]`, a serial for audit,
`ValidBefore=now+ttl`, standard interactive extensions), signs it with the CA, and
returns an `ssh.NewCertSigner`. A credential of `secret_type="ssh_ca"` stores **no
secret** (`SecretEnc` empty); the proxy's `dialUpstream` branches on it to mint a
cert instead of decrypting — a missing CA fails the session closed (`session.error`).
`GET /api/ca/ssh` (`CapReadInventory`) publishes the CA public key + a
`TrustedUserCAKeys` install hint. Audit `session.cert_issued` (serial · principal ·
valid-before · key-id — never the private key). Reconciliation reports `ssh_ca` as
`unsupported`; post-session rotation and the lifecycle worker skip it.

### 2.5b `analytics` *(Phase 23 — privileged threat analytics)*

A deterministic, explainable behavioral **risk scorer** over the audit trail (no
clock, no I/O, no opaque model in `Score`). Each actor's score is the sum of named
**signals** — `break_glass`, `command_blocked`, `auth_failure`, `off_hours`,
`decrypt_failure`, `high_velocity` — each with a configurable weight and a
per-signal cap; thresholds map the total to low/medium/high/critical. `Config`
carries the weights, thresholds, business hours (for off-hours) and the velocity
limit; `New` fills zero fields from `DefaultConfig` (a single break-glass access
alone reaches **high**). The API wires it up: `GET /api/analytics/risk`
(`CapReadAudit`, `?min_level=`/`?window_min=`) scores the recent window over
`store.ExportAudit`; `RunAnalyticsWorker` (`PAM_ANALYTICS_INTERVAL_MIN`) scores each
tick and, for a **newly elevated** high/critical actor, audits `analytics.risk_flagged`
+ alerts, and — with `PAM_ANALYTICS_AUTO_KILL` — terminates a critical actor's live
sessions (`session.Registry.KillByActor`, audit `analytics.auto_response`). A
steady state is not re-alerted; a worsening trend is (a per-actor high-water mark).

### 2.5c `auditfmt` *(Phase 76 — one sanitiser for the whole repo)*

`Field(s string, limit int) string` — quote **and** bound an untrusted value
before it goes into an audit detail. An audit detail is a space-separated
`key:value` string read by humans in the console and parsed by the SIEM
forwarder, so raw input lets whoever controls it invent fields; and a quoted
value is still unbounded, so an audit row whose size an attacker chooses is an
audit trail they can flood.

It is a package rather than a helper because the fix kept being re-typed:
`auditField` existed as two byte-identical copies in `api` and `proxy` and was
**absent from `guacd`**, which also writes details — and that is precisely where
the class was live (finding AZ). `api` and `proxy` keep their package-local
`auditField` name delegating here, since it is used on nearly every handler.

**Use it for any value the caller does not choose**: client-supplied paths and
commands, names entered through the API, anything read off a wire. Values with
legitimate colons (SPIFFE IDs) must be quoted **per field at the source** — never
by quoting the assembled detail as one string, which stops a break-out but not an
inner forged field (finding AY).

### 2.6 `web`

Single embedded `static/index.html` (`//go:embed`). 5250-style terminal UI:
Sign On, menu, "Work with…" screens, F-keys, plain JS calling the REST API with
the key/token entered at Sign On. Panels load independently and tolerate a 403,
so a non-admin role gets a working portal (panels it can't see stay empty).
Served with a strict CSP. Since Phase 25 the console has **full parity** with
the backend: safes (menu 16, members + target assignment), certification
campaigns (menu 17, snapshot/decide/close), risk analytics (menu 18), a live
session watch pane (*Active Sessions* option 5 — the SSE stream read via
`fetch`, since EventSource cannot send the `X-API-Key` header), and the
ticket/N-of-M/window fields on access requests.

## 3. Proxy wire flow (per connection)

```mermaid
sequenceDiagram
    participant C as ssh client
    participant P as proxy (handleConn)
    participant V as vault
    participant S as store
    participant U as upstream sshd
    C->>P: TCP + SSH handshake
    C->>P: password = API key; user = "root@web-01"
    P->>P: authenticate() -> ext{target,cred_user}
    P->>S: resolve target + credential
    P->>V: Decrypt(SecretEnc, target:ID)  %% JIT
    V-->>P: plaintext (memory only)
    P->>U: ssh.Dial(host:port, user=root, pw=plaintext)
    P->>S: audit session.start
    loop each session channel
        C->>P: OpenChannel("session")
        P->>U: OpenChannel("session")
        C-->>U: requests + stdin (pumped)
        U-->>C: stdout/stderr (tee -> recording)
        P->>S: audit session.record (file, bytes, sha256)
    end
    C->>P: disconnect
    P->>S: audit session.end
```

Concurrency notes: one goroutine per connection (`handleConn`), one per session
channel (`handleSession`), request pumps and stdin copy run in their own
goroutines; the stdout copy runs in the foreground of `handleSession`. Teardown
is keyed on the connection lifecycle, not on stdin: the client→upstream stdin
copy only `CloseWrite`s on a stdin half-close (so a batch command's remaining
output and `exit-status` still flow back), and the upstream is fully closed by
`handleConn` when the client's channel loop ends — i.e. when the client is
actually gone — which unblocks any session still copying idle/wedged upstream
output. Before the client channel closes we `Wait` on both the upstream→client
request pump (so `exit-status` is delivered) and the client→upstream request
pump (so the `exec`/`shell` reply the client's `Session.Start` blocks on is
flushed first). `Serve`'s shutdown is bounded: on `ctx` cancel it closes the
listener **and** force-closes every tracked client connection, then waits for
handlers to return, so no handler goroutine outlives `Serve` and the drain does
not block on operators voluntarily disconnecting. `main` waits for this drain on
SIGTERM (before closing the store), and the closing audit events (`session.end`,
`session.record`) are written through `auditClosing`, which detaches from the
cancelled shutdown context so they are not dropped mid-drain.

## 4. Configuration (env `PAM_*`)

| Var | Default | Used by |
|---|---|---|
| `PAM_MASTER_KEY` | — (required for local KEK) | local KEK key (base64, dev/test); see `PAM_KEK_PROVIDER` |
| `PAM_API_KEY` | — (required) | api auth, proxy auth |
| `PAM_DATABASE_URL` | — (required); `memory` for demo | store |
| `PAM_BREAK_GLASS_KEY_HASH` | "" (disabled) | api auth |
| `PAM_CONJUR_URL` | "" (off) | Conjur secret-source appliance URL; presence enables sourcing bootstrap `PAM_*` at startup (Phase 18) |
| `PAM_CONJUR_ACCOUNT` / `_POLICY_PREFIX` | `default` / `pamv1` | Conjur account + variable-name prefix |
| `PAM_CONJUR_AUTHN_LOGIN` / `_API_KEY` | "" | Conjur authn-api-key credentials |
| `PAM_CONJUR_AUTHN_JWT_SERVICE_ID` / `_JWT_FILE` | "" | Conjur authn-jwt service id + JWT file (K8s-native) |
| `PAM_CONJUR_CACERT` | "" | PEM CA bundle for TLS to Conjur |
| `PAM_CONJUR_VARS` | "" | per-variable override of the `<prefix>/<suffix>` convention: a comma-separated list of `PAM_SOMETHING=conjur/variable/id`. An unknown name is fail-loud, so a typo cannot be silently ignored (Phase 78) |
| `PAM_CONJUR_REFRESH_MIN` | `0` (off, max `1440`) | minutes between re-reads of the **refreshable** bootstrap secrets — `PAM_API_KEY` and `PAM_BREAK_GLASS_KEY_HASH` only. Every replica runs its own (no leader lock): each holds its own copy, so a leader-only refresh would leave the rest authenticating against the retired key. Audited `config.secret_refreshed` (Phase 78) |
| `PAM_REQUIRE_TICKET` | `false` | require an ITSM ticket on access requests (Phase 20) |
| `PAM_TICKET_PATTERN` / `PAM_TICKET_VALIDATE_URL` | "" | ticket format regex / generic ITSM validation webhook (`POST {"ticket":…,"actor":…}` → 2xx = valid) |
| `PAM_TICKET_PROVIDER` | "" (webhook when a URL is set) | ITSM connector: `webhook`, `servicenow` or `jira` (Phase 84) |
| `PAM_TICKET_URL` / `_USER` / `_TOKEN` | "" | ITSM instance URL and HTTP-basic credentials for the first-class connectors |
| `PAM_TICKET_STATES` | `implement,scheduled` (SN) / `in progress` (Jira) | states that authorise access |
| `PAM_TICKET_ACTOR_FIELDS` | `assigned_to,requested_by` (SN) / `assignee,reporter` (Jira) | ticket fields that may name the operator |
| `PAM_TICKET_REQUIRE_WINDOW` | `true` | enforce the change's planned start/end (ServiceNow) |
| `PAM_TICKET_BIND_ACTOR` | `true` | require the ticket to name the operator — off, a ticket number is a shared password |
| `PAM_TICKET_REVALIDATE` | `false` | re-check the admitting request's ticket when access is USED, not only when it was requested — at all five gates, refusing (`access.ticket_revoked`) when the ITSM no longer confirms it or cannot be reached (Phase 60) |
| `PAM_APPROVALS_REQUIRED` | `1` | default distinct approvers per access request (Phase 21) |
| `PAM_REQUIRE_REASON` | `false` | reject an access request with no reason |
| `PAM_ACCESS_ONE_TIME` | `false` | make every access request single-use: the first privileged use consumes the approval (Phase 26) |
| `PAM_LISTEN_ADDR` | `:8080` | http server |
| `PAM_SSH_ADDR` | `:2222`; `off` disables | proxy |
| `PAM_DB_ADDR` | `off` | PostgreSQL session-proxy listen address (Phase 15) |
| `PAM_MSSQL_ADDR` | `off` | SQL Server (TDS) session-proxy listen address (Phase 53). Shares every policy knob with the PostgreSQL proxy — TLS, upstream verification, command control, step-up, recording |
| `PAM_COMMAND_DENY_FILE` | "" (off) | regex denylist file for command control (Phase 16) |
| `PAM_DB_STEPUP_FILE` | "" (off) | regex file marking SQL statements that pause for a supervisor's live approval — in-session step-up (Phase 30) |
| `PAM_DB_STEPUP_TTL_SEC` | `120` | how long a paused statement waits for a supervisor decision before it is denied (Phase 30) |
| `PAM_ANALYTICS_INTERVAL_MIN` | `0` (worker off) | privileged-threat-analytics worker interval (Phase 23); the read-only risk endpoint stays available |
| `PAM_ANALYTICS_WINDOW_MIN` | `60` | how far back each risk-scoring pass looks |
| `PAM_ANALYTICS_BASELINE_DAYS` | `30` (`0` = off, max `366`) | history read for the `new_target` novelty signal, from the window *before* the scored one. Silent when an actor has no history, so a new joiner is not an anomaly (Phase 86) |
| `PAM_ANALYTICS_AUTO_STEPUP` | `false` | revoke a **high**-risk actor's portal logins so their next action re-authenticates (a second factor where MFA is enrolled) — the rung below AUTO_KILL (Phase 86) |
| `PAM_ANALYTICS_AUTO_KILL` | `false` | terminate a critical-risk actor's live sessions (automated response) |
| `PAM_ANALYTICS_BUSINESS_START` / `_END` | `7` / `20` | business hours for the off-hours risk signal |
| `PAM_ANALYTICS_TIMEZONE` | "" (UTC) | IANA timezone the business hours are interpreted in (audit timestamps are UTC) |
| `PAM_APP_SECRETS_ENABLED` | `false` | enable the application-secrets API (Phase 24, Tier-4): Conjur-style secret delivery to non-agent apps |
| `PAM_SSH_HOST_KEY` | "" (ephemeral) | proxy host key |
| `PAM_SSH_CA_KEY` | "" (ZSP off) | Zero Standing Privilege SSH CA key path (Phase 22); presence enables `ssh_ca` credentials (mint short-lived certs) |
| `PAM_SSH_CERT_TTL_MIN` | `2` | validity (minutes) of a minted ZSP certificate |
| `PAM_SSH_OPERATOR_CERT_TTL_MIN` | `10` | cap (minutes) on an operator-issued SSH certificate (Phase 28: `POST /api/ca/ssh/sign`) |
| `PAM_VENDOR_ATTEST_URL` | "" (off) | webhook the vendor-management system answers 2xx for a currently-employed vendor; checked on contract-grant approval (Phase 29) |
| `PAM_CERT_REMIND_DAYS` | `7` (`0` = off, max `366`) | days before a certification campaign's due date that its first reminder fires; it then repeats daily while items are pending and stops when the campaign closes or empties (Phase 70). Delivered through `PAM_ALERT_*`, audited `certification.reminder` |
| `PAM_VENDOR_SWEEP_INTERVAL_MIN` | `0` (off) | interval for the background sweep that cuts a vendor's live sessions once their contract window closes (Phase 29) |
| `PAM_SSH_KNOWN_HOSTS` | "" (trust-any + warn) | pin upstream target host keys (OpenSSH known_hosts) |
| `PAM_SSH_SFTP` | `allow` | SFTP file-transfer policy: `allow` (forward + audit each op), `readonly` (refuse writes/deletes/renames), `deny` (refuse the subsystem) (Phase 32) |
| `PAM_SSH_SFTP_CAPTURE` | `off` | record the CONTENT of files moved over SFTP: `uploads`, `downloads`, or `all` — each transferred file becomes a `.sftp` chunk-log artifact in the recording dir, sealed under `PAM_RECORDING_ENCRYPT`, hash-chained, audited `sftp.file_recorded`; while on, an unparsable SFTP stream fails the transfer closed (Phase 59) |
| `PAM_SSH_SFTP_CAPTURE_MAX_MB` | `0` (unlimited) | per-file cap on captured bytes; past it the transfer is REFUSED (permission-denied), not merely unrecorded, so it doubles as a transfer size limit (Phase 59) |
| `PAM_SSH_JUMP_HOST` / `_USER` / `_KEY` | — | reach SSH targets through an SSH bastion (jump host) |
| `PAM_RECORDING_DIR` | `recordings` | proxy recordings |
| `PAM_LOG_LEVEL` | `info` | logging (debug/info/warn/error) |
| `PAM_LOG_FORMAT` | `json` | logging (json/text) |
| `PAM_TLS_CERT` / `PAM_TLS_KEY` | — | native HTTPS (TLS 1.2+) when both set |
| `PAM_AUTH_RATE_LIMIT` | `20` | auth attempts / client IP / minute (0 disables) |
| `PAM_TRUSTED_PROXY_HOPS` | `0` | trusted reverse-proxy hops; picks the client IP from `X-Forwarded-For` for rate limiting (0 = use RemoteAddr) |
| `PAM_PROXY_AUTH_RATE_LIMIT` | `10` | failed-auth throttle / source IP / minute on the SSH (:2222) and DB (:5433) proxies (0 disables) |
| `PAM_REQUIRE_HTTPS` | `false` | refuse to start the API/portal without native TLS (fail-closed transport) |
| `PAM_REQUIRE_DB_CLIENT_TLS` | `false` | refuse to start the DB proxy without operator-leg TLS |
| `PAM_DB_UPSTREAM_CA` | — | PEM CA bundle to VERIFY the upstream PostgreSQL cert (fail-closed upstream TLS) |
| `PAM_DB_UPSTREAM_TLS_VERIFY` | `false` | verify the upstream PostgreSQL cert against the system roots |
| `PAM_ALLOW_WEAK_API_KEY` | `false` | override the 16-char `PAM_API_KEY` floor (demos only; ignored for `memory`) |
| `PAM_AUDIT_HMAC_KEY` | — | base64 32-byte key; enables the tamper-evident HMAC chain over the **primary** audit trail (verify via `GET /api/audit/verify`). Unset = plain audit table |
| `PAM_AUDIT_SIGN_SEED` | — | base64 32-byte ed25519 seed (needs `PAM_AUDIT_HMAC_KEY`); signs audit checkpoints served by `GET /api/audit/head` so an auditor can detect **tail truncation** |
| `PAM_AUDIT_FORWARD_ADDR` | — (off) | host:port of a SIEM collector; streams every audit event to it (Phase 35). `PAM_AUDIT_FORWARD_PROTO` (udp/tcp/tls — tls is RFC 5425, verification always on), `PAM_AUDIT_FORWARD_FORMAT` (rfc5424/cef/leef), `PAM_AUDIT_FORWARD_CA` (PEM bundle pinning the collector's CA, tls only), `PAM_AUDIT_FORWARD_INTERVAL_SEC` (10) tune it |
| `PAM_RECORDING_RETENTION_DAYS` / `PAM_AUDIT_RETENTION_DAYS` | `0` (keep forever) | prune recording files / audit rows older than N days (Phase 36); audit pruning is skipped while the HMAC chain is on. `PAM_RETENTION_INTERVAL_HOURS` (24) is the sweep cadence |
| `PAM_RETENTION_ARCHIVE_DIR` | — (delete on expiry) | archive-then-prune (Phase 49): aged audit rows are exported as digest-stamped JSON Lines and aged recordings are MOVED here (write-once, `0400`) — and the prune runs **only** if that archive succeeded. Point it at WORM storage |
| `PAM_REVEAL_DISABLED` | `false` | make credential reveal break-glass-only |
| `PAM_BREAK_GLASS_THRESHOLD` / `_SHARES` | `0` / `5` | quorum M / N (M≥2 enables unseal) |
| `PAM_BREAK_GLASS_TTL_MIN` | `15` | break-glass session lifetime (minutes) |
| `PAM_ALERT_WEBHOOK` | — | JSON POST target for break-glass alerts |
| `PAM_ALERT_SYSLOG` | — | syslog alert channel, `udp://host:port` or `tcp://host:port` |
| `PAM_ALERT_EMAIL_SMTP` / `_FROM` / `_TO` / `_USER` / `_PASS` | — | SMTP alert channel (comma-separated `_TO`) |
| `PAM_KEK_PROVIDER` | `local` | vault KEK: `local` \| `vault-transit` \| `aws-kms` \| `pkcs11` |
| `PAM_KEK_TRANSIT_ADDR` / `_TOKEN` / `_KEY` | — | HashiCorp Vault Transit KEK (production; `https://` required off-loopback) |
| `PAM_NEW_MASTER_KEY` / `PAM_NEW_KEK_*` | — | target KEK for the `-rotate-kek` run (any provider; `PAM_NEW_KEK_PROVIDER` etc. mirror `PAM_KEK_*`, enabling local→KMS migration) |
| `PAM_KEK_AWS_KEY_ID` / `_AWS_REGION` | — | AWS KMS KEK (`aws-kms` provider) |
| `PAM_KEK_PKCS11_MODULE` / `_PIN` / `_KEY_LABEL` / `_TOKEN_LABEL` | — | on-prem HSM KEK (`pkcs11` provider; **only in the `pkcs11`-tagged build**) |
| `PAM_LDAP_URL` | — (disabled) | AD/LDAP login; `ldaps://…` enables `/api/login` |
| `PAM_LDAP_BIND_DN` / `_BIND_PASSWORD` | — | service account for user search |
| `PAM_LDAP_BASE_DN` / `_USER_FILTER` | — / `(sAMAccountName=%s)` | search base + filter |
| `PAM_LDAP_GROUP_ADMIN` / `_USER` / `_AUDITOR` / `_APPROVER` | — | group DN → role |
| `PAM_LDAP_INSECURE_SKIP_VERIFY` | `false` | disable TLS verify (dev only) |
| `PAM_ENTRA_TENANT_ID` | — (disabled) | Entra ID login; set to enable |
| `PAM_ENTRA_CLIENT_ID` / `_CLIENT_SECRET` / `_SCOPE` / `_AUTHORITY_HOST` | — | Entra app registration |
| `PAM_ENTRA_ROLE_ADMIN` / `_USER` / `_AUDITOR` / `_APPROVER` | — | Entra app-role/group → role |
| `PAM_MFA_REQUIRED` | `false` | require a confirmed TOTP factor for password login |
| `PAM_ROTATE_INTERVAL_MIN` | `0` (off) | credential-lifecycle worker interval (minutes) |
| `PAM_ROTATE_MAX_AGE_HOURS` | `0` (report only) | auto-rotate password credentials older than this |
| `PAM_ROTATE_AFTER_SESSION` | `false` | rotate a credential as soon as a proxied session using it ends |
| `PAM_ALLOWED_PROTOCOLS` | — (all) | OT: comma-separated protocol allowlist (e.g. `ssh,winrm`) enforced at create + connect |
| `PAM_REQUIRE_APPROVAL` | `false` | OT: gate every target behind an approved access request (4-eyes) |
| `PAM_REQUIRE_RECORDING` | `false` | refuse a session if its recording cannot be created (fail-closed). Covers **every** path to a target since Phase 52c — SSH, WinRM and PostgreSQL proxies, the in-portal RDP viewer (needs `PAM_GUACD_RECORDING_PATH`) and the REST WinRM endpoint (needs `PAM_RECORDING_DIR`); checked before anything reaches the target, refusals audited as `rdp.refused`/`winrm.refused` |
| `PAM_MAX_SESSIONS_PER_USER` | `0` (∞) | cap concurrent live proxied sessions per actor (checked before decrypt; per-replica) |
| `PAM_MAX_SESSIONS_TOTAL` | `0` (∞) | cap concurrent live proxied sessions across all actors (per-replica) |
| `PAM_MAX_RECORDING_MB` | `0` (∞) | cap a single session recording's output (MB); a session that exceeds it is terminated (`session.record_limit`) rather than run unrecorded |
| `PAM_RECORDING_ENCRYPT` | `false` | seal session recordings at rest: a per-recording data key wrapped by the KEK (Phase 41). The audited SHA-256 is taken over the bytes on disk, so the tamper-evidence still describes the stored artifact |
| `PAM_SECRETS_PROVIDER` | "" (env/SOPS) | selects the bootstrap-secret source explicitly; `conjur` makes `PAM_CONJUR_URL` required so a misconfigured deploy fails loudly instead of silently falling back to env vars |
| `PAM_RECORDING_OPAQUE_NAMES` | `false` | name recording files `<unixnano>_<random hex>` instead of `<unixnano>_<target>_<actor>` (Phase 48), so the volume/backup leaks no access metadata; the mapping lives in the audited `session.record`/`winrm.run` event and `GET /api/recordings` resolves it back (read_audit) |
| `PAM_APPROVAL_WINDOW_MIN` | `60` | how long an approved access request stays valid |
| `PAM_CHECKOUT_TTL_MIN` | `30` | credential checkout lease lifetime (minutes) |
| `PAM_OT_AIRGAP` | `false` | air-gapped sites: forces the no-op alerter **and refuses to start** alongside any integration that would egress (ITSM webhook, vendor attestation, SIEM forwarder, Conjur, OIDC issuer, alert webhook) unless named in `PAM_OT_AIRGAP_ALLOW`. `PAM_KEK_PROVIDER=aws-kms` and `PAM_ENTRA_TENANT_ID` are refused outright |
| `PAM_OT_AIRGAP_ALLOW` | — | comma-separated variable names the operator certifies resolve **inside** the enclave, re-permitting them under air-gap |
| `PAM_WINRM_HTTPS` | `true` | use HTTPS (5986) for WinRM |
| `PAM_WINRM_INSECURE_SKIP_VERIFY` | `false` | skip WinRM TLS verify (dev only) |
| `PAM_WINRM_AUTH` | `basic` | `basic` or `ntlm` |
| `PAM_GUACD_ADDR` | — (RDP off) | Guacamole guacd address, e.g. `127.0.0.1:4822` |
| `PAM_GUACD_RECORDING_PATH` | — | server-side path for guacd to record RDP sessions |
| `PAM_PROXY_WINRM` | `false` | broker an interactive WinRM command loop through the SSH proxy |
| `PAM_GUACD_RDP_SECURITY` / `PAM_GUACD_IGNORE_CERT` | negotiate / `false` (verify) | RDP security mode; cert verification (opt-out for dev) |
| `PAM_SSH_SFTP_DENY_FILE` | — (no path policy) | regex denylist file (same format as `PAM_COMMAND_DENY_FILE`) matched against every SFTP path; a match is refused in EVERY mode including reads, and on both sides of a rename (Phase 51) |
| `PAM_RDP_CLIPBOARD` | `allow` | Clipboard policy for **both** in-portal viewers, RDP and VNC (the name predates VNC): `allow`, `readonly` (block paste into the target), `deny` (off both ways). RDP drive redirection and VNC's `enable-sftp` are always off (Phases 33, 54). Per-target `Target.RDPClipboard` tightens it — strictest wins. A policy guacd cannot enforce refuses the session |
| `PAM_RDP_CLIPBOARD_AUDIT` | `off` | audit what crosses the clipboard bridge (Phase 50): `meta` (direction, mimetype, bytes, SHA-256) or `full` (also the content, truncated — the clipboard may hold a just-copied password, so this is opt-in); emits `rdp.clipboard`. Per-target `Target.RDPClipboardAudit` raises it — whichever records more wins |
| `PAM_OIDC_ISSUER` | — (disabled) | OIDC login; set to enable the auth-code flow |
| `PAM_OIDC_CLIENT_ID` / `_CLIENT_SECRET` / `_REDIRECT_URL` / `_SCOPES` | — | OIDC client |
| `PAM_OIDC_AUTH_URL` / `_TOKEN_URL` / `_JWKS_URL` | — (discovered) | override endpoints |
| `PAM_OIDC_ROLE_ADMIN` / `_USER` / `_AUDITOR` / `_APPROVER` | — | OIDC app-role/group → role |
| `PAM_PORTAL_URL` | `/` | OIDC callback redirect base |
| `PAM_BROKER_POLICY_FILE` | — (broker off) | AI-agent broker: YAML policy rules; set to enable the broker |
| `PAM_BROKER_AUDIT_KEY` | — (shared custody) | base64 32-byte HMAC key for the broker audit chain; unset = generated once under shared custody (KEK-sealed in `key_material`, converged on by every replica) |
| `PAM_BROKER_AUDIT_SIGN_SEED` | — (shared custody) | base64 32-byte ed25519 seed for signed head checkpoints; unset = shared custody like the HMAC key. Set it explicitly to drive a signing-key rotation |
| `PAM_BROKER_TOKEN_TTL_MIN` | `15` | Lifetime (minutes) of a single-use approval resume token |
| `PAM_BROKER_MAX_ARG_BYTES` | `16384` | Cap on a tool call's serialized arguments; `0` disables |
| `PAM_BROKER_RATE_PER_MIN` | `0` (off) | Per-agent tool-call rate limit (calls per minute) |
| `PAM_BROKER_AUDIT_CHECKPOINT_EVERY` | `0` (off) | Emit a signed in-chain audit checkpoint every N broker events (Phase 27) |
| `PAM_BROKER_AUDIT_SIGN_PREV` | — | Comma-separated base64 ed25519 **public** keys still trusted to verify older checkpoints during a signing-key rotation overlap (Phase 27) |
| `PAM_BROKER_TRUST_DOMAIN_JWKS` | — (SVID off) | File with the SPIFFE trust-domain JWKS; set to accept JWT-SVIDs |
| `PAM_BROKER_TRUST_DOMAIN` | — | SPIFFE trust domain host (e.g. `example.org`); required with the JWKS |
| `PAM_BROKER_AUDIENCE` | — | Required SVID audience; required with the JWKS |
| `PAM_BROKER_MAX_DELEGATION_DEPTH` | `1` | RFC 8693 `act`-chain depth cap (fail-closed beyond it) — applied both when verifying a presented chain and when minting a new link |
| `PAM_BROKER_TOKEN_EXCHANGE` | `false` | enable `POST /v1/token`, the RFC 8693 token-exchange endpoint that MINTS delegated JWT-SVIDs (Phase 57). Needs the broker and a trust-domain JWKS; fail-loud otherwise |
| `PAM_BROKER_EXCHANGE_TTL_MIN` | `5` | lifetime of a minted delegated token, additionally capped by the delegator's own expiry |
| `PAM_BROKER_TOKEN_SIGN_SEED` | *(shared custody)* | base64 32-byte ed25519 seed the broker signs delegated SVIDs with; unset = generated once into KEK-sealed shared custody, so every replica issues and accepts under one key |

## 4a. Logging (operational, to stdout)

`internal/logging` installs the process slog logger (`Setup`) and hands each
component a logger tagged `service=<name>` (`Component`). Distinct from the DB
**audit trail**: logs are for ops/debugging/SIEM, the audit trail is the security
record. Highlights: `api` logs one line per HTTP request (method/path/status/
actor/duration) via an access-log middleware + `statusWriter`; `proxy` logs
connection auth, session start/end, denials, upstream errors; `store` (pgstore)
logs the Postgres connect and traces each query at `debug` (SQL text + duration +
rows, **never argument values**). The vault deliberately logs nothing about
secrets. Format `json` (SIEM) or `text` (humans); collect from stdout.

## 5. Audit action vocabulary

`target.create` · `target.update` · `target.delete` · `safe.update` · `user.update` · `vendor.update` · `credential.create` · `credential.reveal` ·
`credential.delete` · `credential.reveal_denied` · `credential.checkin_denied` · `credential.rotate_orphaned` · `grant.create` · `grant.delete` ·
`winrm.denied` · `session.kill` · `breakglass.unseal` · `user.create` · `user.delete` · `login` · `logout` ·
`credential.rotate` · `credential.rotate_started` · `credential.rotate_failed` · `credential.reconcile` · `credential.reconcile_scan` · `credential.remediate` ·
`credential.checkout` · `credential.checkin` · `credential.checkout_denied` · `credential.checkin_rotate_failed` · `credential.decrypt_failed` · `discovery.scan` ·
`access.request` · `access.approve` · `access.deny` · `access.denied` · `access.decision_denied` · `audit.export` · `identity.reconcile` · `user.revoked` ·
`mfa.enroll` · `mfa.confirm` · `mfa.disable` · `mfa.recovery_generated` ·
`mfa.recovery_used` · `session.kill_failed` · `winrm.run` · `winrm.error` · `winrm.refused` · `ssh.exec` · `rdp.token` · `rdp.connect` · `rdp.end` · `rdp.refused` · `vnc.token` · `vnc.connect` · `vnc.end` · `vnc.refused` · `vnc.error` · `vnc.clipboard` · `session.self_stepup_denied` ·
`rdp.error` · `rdp.clipboard` · `authz.denied` · `login.failed` · `api.auth_failed` · `proxy.auth_failed` · `session.revoked` · `session.killed` · `vault.kek_rotated` · `breakglass.access` · `breakglass.unseal_failed` · `session.relay_start` · `session.start` ·
`session.record` · `session.record_failed` · `session.record_limit` · `session.end` · `session.denied` · `session.error` · `session.cert_issued` · `ssh.cert_issued` · `ssh.cert_denied` · `ssh.cert_revoked` ·
`analytics.risk_flagged` · `analytics.auto_response` ·
`db.session.start` · `db.session.end` · `db.session.denied` · `db.session.error` · `db.query` ·
`command.blocked` · `session.monitor` · `session.playback` · `db.stepup_required` · `db.stepup_approved` · `db.stepup_denied` · `session.stepup_decided` · `blast.analyze` ·
`sftp.session` · `sftp.open` · `sftp.modify` · `sftp.blocked` · `sftp.denied` · `sftp.parse_error` · `sftp.file_recorded` · `sftp.capture_failed` · `sftp.extension` · `session.subsystem` ·
`recording.pruned` · `audit.pruned` · `recording.archived` · `audit.archived` ·
`safe.create` · `safe.delete` · `safe.member.add` · `safe.member.remove` · `target.safe_set` ·
`dependency.create` · `dependency.create_denied` · `dependency.delete` · `credential.dependency_updated` · `credential.dependency_failed` ·
`certification.campaign_created` · `certification.item_assigned` · `certification.reminder` · `certification.item_certified` · `certification.item_revoked` · `certification.decision_denied` · `certification.campaign_closed` ·
`access.ticket_rejected` · `access.ticket_revoked` · `access.approve_partial` · `access.consumed` ·
`vendor.create` · `vendor.offboard` · `vendor.grant_created` · `vendor.grant_approved` · `vendor.grant_revoked` · `vendor.grant_decision_denied` · `vendor.attestation_failed` · `vendor.session_expired` · `vendor.evidence_export` ·
`app.create` · `app.revoke` · `app.grant` · `app.grant_revoked` · `app.secret_retrieved` · `app.secret_denied` ·
`agent.create` · `agent.revoke` · `broker.tool_call.requested` · `broker.tool_call.executed` · `broker.tool_call.denied` · `broker.tool_call.pending_approval` · `broker.tool_call.failed` · `broker.tool_call.resumed` · `broker.tool_call.withdrawn` · `broker.approval.granted` · `broker.approval.denied` · `broker.approval.refused` · `broker.elicit.accepted` · `broker.elicit.declined` · `broker.audit.checkpoint` · `broker.token.exchanged` · `broker.token.refused` · `audit.ocsf_export` ·
`config.update` · `config.revert` · `config.secret_refreshed` (actor `system-conjur`) · `profile.create` · `profile.delete`. The actor is the Principal name
(`bootstrap-admin`, `break-glass`, a username, or an agent name). Agent-broker
events are also written to the separate tamper-evident `broker_audit_events` chain.

## 6. Security-relevant invariants (do not regress)

> The algorithms and per-protocol TLS posture these invariants rest on are
> catalogued in [PROTOCOLS-AND-CRYPTO.md](PROTOCOLS-AND-CRYPTO.md).

1. `Credential.SecretEnc` must never be serialized to any client (`json:"-"`).
2. All key/secret comparisons use `crypto/subtle.ConstantTimeCompare`.
3. Vault AAD on decrypt must equal AAD on encrypt (`store.CredentialAAD`).
2a. Per-target authorization: connect paths (proxy/WinRM/RDP) must pass
   `auth.CanConnectTarget(p, grants, safeScoped)` — a target with grants admits
   only matching users/roles (admins always). An **ungated** target (not in a
   safe) is open to any connect-capable principal; a **safe-scoped** target
   (`Target.SafeID` set) is default-DENY when no grant matches, so safe
   membership actually contains it. Pass `target.SafeID != nil` at every call.
3a. Envelope encryption: per-secret data keys are wrapped by the KEK and zeroed
   after use; the base64 `PAM_MASTER_KEY` (local KEK) is dev/test only —
   production uses a KMS-backed KEK.
4. Every code path that reveals or uses a secret appends an audit event, and the
   secret-delivery paths do so **fail-closed**: reveal / checkout / app-secret use
   `mustAudit`/`mustAuditAs` (HTTP 503 if the durable write fails), and both
   proxies audit `session.start` *before* decryption via `appendAuditErr` (session
   refused if it fails). Only non-secret events remain best-effort.
4a. TOTP secrets are stored vault-encrypted (AAD `mfa:<user>`), returned only
   once at enrollment, and compared in constant time (`crypto/subtle`). Recovery
   codes are stored only as SHA-256 hashes, shown once, and single-use.
5. Break-glass config holds only the SHA-256 hash, never the plaintext key.
6. Proxy plaintext secret is confined to `resolve`→`dialUpstream`; never logged.
7. User access tokens are stored only as `TokenHash` (`json:"-"`); the plaintext
   is returned once at creation and never persisted or re-derivable.
8. Every protected route/connection declares a capability; the role→capability
   matrix in `auth` is the single source of truth (don't inline role checks).

**Known, accepted limitations (documented, not defects):**

- **Session-recording UTF-8 fidelity.** `proxy.Recording.Write` records each read
  as an asciicast v2 `"o"` frame (`string(p)`), so a multi-byte rune split across
  two reads lands on a frame boundary; Go's JSON encoder emits U+FFFD for the
  partial bytes. The recording stays valid JSON and the SHA-256 still covers what
  was written — only the split rune is cosmetically off in playback. Byte-exact
  capture of arbitrary binary is out of scope for the text-oriented asciicast
  format by design.
- **Local-KEK nonce ceiling.** `LocalKEK.Wrap` uses a random 96-bit AES-GCM nonce
  per DEK wrap. [NIST SP 800-38D §8.3](https://csrc.nist.gov/pubs/sp/800/38/d/final)
  bounds a single key to ~2³² random-nonce invocations; one wrap per secret (plus
  rotations) stays far below that here, and production uses an external KEK
  (Vault-Transit / KMS / PKCS#11) as invariant 3a already requires.

## 7. Testing

- `vault`: envelope roundtrip, AAD binding, tamper detection, version rejection, distinct tokens; local KEK wrap/unwrap + tamper; KEK factory; **Transit KMS** wrap/unwrap and full envelope over a mock Vault Transit server; **PKCS#11 HSM** wrap/unwrap + full envelope against SoftHSM2 (tag `pkcs11`, CI).
- `auth`: role→capability matrix, role parsing, resolver (bootstrap/break-glass/token/**session**/unknown); **LDAP** authenticate against a fake `ldapConn`; **Entra** app-role/group login with an RS256-signed id_token (valid maps roles; a forged signature is rejected).
- `store` conformance: `storetest.RunStoreContract` exercises the whole `Store` interface (targets/creds/grants/access-requests/checkouts/audit+export/users/sessions/MFA/recovery/OIDC-state) — run by **memstore** and, in CI, against a **live PostgreSQL** (`PAM_TEST_DATABASE_URL`).
- `mfa`: RFC 6238 test vector, validate roundtrip + skew + wrong/short code, otpauth URI, secret randomness, recovery-code generation.
- `api` (WinRM): JIT injection (fake runner receives the vaulted secret), non-Windows target rejected, `CapConnect` required, transcript recorded + audited.
- `winrm`: basic and NTLM client construction (NTLM must not mutate library defaults).
- `guacd`: instruction encode (byte vs code-point length), and handshake against a mock guacd asserting the **JIT credential injection** into `connect` (+ unknown args empty).
- `api` (RDP): tunnel disabled without guacd (404), no/invalid token 401, non-connect role 403, non-rdp target 422 (pre-WebSocket checks); `guacamolePrelude` wire-format; a full WebSocket round-trip against a fake guacd (`TestRDPTunnelEndToEnd`) asserting the prelude, the injected secret in guacd's `connect`, and both piping directions; `rdp-token` gating (`TestRDPTokenRequiresConnect`).
- `oidc`: Exchange with a real RSA-signed token (valid), and rejects bad signature / wrong issuer / wrong audience / nonce mismatch / expired; PKCE + auth URL.
- `api` (OIDC): full flow (start redirect → callback with a signed token → session → admin role enforced), bad-state error redirect, not-configured 404.
- `auth` (Entra/chain): Entra app-role & group login, multi-group role **union** (`TestMatchedRoles`/`TestMultiGroupUnion`), bad password, no mapped role (mock token endpoint); chain resolves via a later source and rejects when none match.
- `shamir`: split/combine roundtrip (any M reconstruct), below-threshold and corrupted shares don't, validation.
- `alert`: webhook receives the event; no-op is safe.
- `api` (break-glass): quorum unseal issues a break-glass session that reads audit + fires unseal & access alerts; wrong shares 401; not-configured 404.
- `api`: full CRUD flow, auth required, secret-non-leak, cascade delete, break-glass audited, validation/conflict, **RBAC** (user/auditor/approver each allowed only their capabilities), **login session** (issue → use with role enforcement → logout revokes), login-not-configured 503, **MFA** (enroll → confirm → login requires OTP → wrong OTP rejected), **enforce-MFA** (enrollment-only session blocked from other routes → enroll → full session), **recovery codes** (login with a code, single-use).
- `proxy`: **end-to-end JIT injection** against an in-process upstream sshd that
  accepts only the vaulted password (proves the client never had it), plus wrong-key
  rejection, unknown-target denial, credential selector, recording hash match, and
  **auditor-denied connect** (RBAC gate).
- CI runs `gofmt -l`, `go vet`, `staticcheck`, `govulncheck`, `gosec`, `go build`, `go test -race`, and a Docker build.

## 8. Change log

| Date | Change |
| --- | --- |
| 2026-08-09 | **Phase 87 — the review of Phase 86.** One finding, a denial of service delivered through the defence. The threat-analytics risk score counts auth failures, and an auth failure records the *presented* username as the actor (`login.failed` stores `in.Username` raw); the auto-kill (Phase 23) and new auto-step-up (Phase 86) responses fired on the finding's `Level` and acted on its `Actor` with no check that the risk was the actor's own doing. So an unauthenticated attacker who knows a username could fail login under it — **7** times → *high* → revoke the victim's logins, **10** → *critical* → kill their live sessions (confirmed by execution). `analytics.Finding` gains **`ResponseScore`/`ResponseLevel`**, excluding `auth_failure` (the only signal a stranger can attribute to a name they do not control — every other requires the actor to have authenticated and acted); responses gate on `ResponseLevel`, alerts stay on `Level` so a brute-forced account is still surfaced to a human. `analytics.risk_flagged` details carry `response_level:` beside `level:`. Auto-kill has shipped since Phase 23, so this fixes released behaviour. Tests `analytics.TestResponseScoreExcludesAuthFailures` and `api.TestAnalyticsPassIgnoresAuthFailureOnlyActor`, both verified to fail pre-fix. No schema, route or audit-vocabulary change |
| 2026-08-09 | **Phase 86 — analytics that need history.** Closes the Phase 23 deferral. The six original signals all ask "is this event suspicious?", which misses the quiet thing an insider does: ordinary, well-formed access somewhere they have no business being. New `internal/analytics/baseline.go`: `BuildBaseline(events)` summarises the audit window *preceding* the scored one into actor→targets membership (no new storage, only a wider read), and `Engine.ScoreWithBaseline` adds two signals — **`new_target`** (counted per DISTINCT target: ten sessions on one new host is one new thing) and **`peer_outlier`** (activity above `PeerFactor` × the peer **median**, skipped below `PeerMinActors`). Both are **absent, not zero**, when there is nothing to compare against: a nil baseline scores exactly as before, and an actor the baseline does not know scores no novelty — otherwise the first run and every new joiner's first week read as a stream of anomalies, which is how a signal teaches people to ignore it. New response `PAM_ANALYTICS_AUTO_STEPUP`: a **high** (not critical) actor's portal logins are revoked via `DeleteSessionsByUsername`, so the next action re-authenticates with a second factor where MFA is enrolled — the rung below `AUTO_KILL`, because killing a high-risk-but-legitimate operator mid-change is itself an incident. `PAM_ANALYTICS_BASELINE_DAYS` (default 30, range-checked 0–366) bounds the extra read, and a failed baseline read degrades the pass rather than skipping it. **A zero-value trap**: a zero `PeerFactor` makes the threshold `median × 0` = 0 and a zero `PeerMinActors` removes the peer-group guard, so every actor scored as an outlier — caught by an existing timezone test; `New` now defaults both and `peerVolumes` refuses the configuration outright. No schema, route or audit-vocabulary change |
| 2026-08-08 | **Phase 84 — the ticket gate learns who you are.** Closes the Phase 20 deferral. `internal/ticket` gains a `Provider` interface and three implementations: **ServiceNow** (Table API `change_request` by number, `sysparm_display_value=true` so reference fields come back readable rather than as sys_ids), **Jira** (`/rest/api/3/issue/{key}`) and the existing **webhook**, selected by `PAM_TICKET_PROVIDER`. `Validator.Validate` now takes the **actor**, threaded through both call sites (`createAccessRequest` and the connect-time `store.ClaimApproval` fold, where it is the person *connecting*, not the approval's recorded requester). That is the security point: a 2xx webhook can only answer "does this ticket exist", so a valid change number **admitted anyone who knew one** — the gate proved a ticket was valid, never that it was yours. The connectors check three things a webhook cannot express: **state** (a closed/cancelled/draft change is not authorisation), **window** (`start_date`/`end_date`, the classic audit finding; an absent bound means "no bound" so open-ended standard changes still work), and **person** (`assigned_to`/`requested_by`, or Jira's `assignee`/`reporter`), matched case-insensitively and on an email local part because the same human is `alice` here and `alice@acme.com` in Jira — strictness there gets the control switched off, which protects nobody. `PAM_TICKET_BIND_ACTOR` defaults **on**. The webhook payload gains `"actor"`, backward compatible: an endpoint that ignores it behaves exactly as before. Fake-ITSM tests cover state, window, person, unknown ticket and unauthenticated lookup for both connectors, and the person and window checks were each verified against a deliberately broken build. What a live instance actually returns is not asserted — that needs an account, and is catalogued in EXTERNAL-INFRA-GAPS. No schema, route or audit-vocabulary change |
| 2026-08-08 | **Phase 82 — the review of Phases 79–81.** Three findings. The first is Phase 80's fix for finding BM **reintroducing BM inside itself**: ownership moved correctly from "Conjur filled it at boot" to "Conjur manages it", and a warning was added for a secret both pinned in the environment and managed in Conjur ("enabling refresh means Conjur wins for it") — but `NewRefresher` then seeded the change digest from **Conjur's** value, so the opening tick compared Conjur against Conjur, found no change and skipped forever. Since every shipped deployment sets `PAM_API_KEY`, that was the only case, and the log asserted the opposite of the behaviour. Seeded from `os.Getenv` at construction now — one read of what the process booted with, which is not finding BH's mistake of using the environment as the last-applied store across ticks. Also: three explanatory comments had stacked in `config.Load`'s validation with no code between them, so the 1 PiB SFTP reasoning documented the Conjur refresh interval (each insertion landed between a comment and its `if`); and an applier keyed on a non-sourceable name was never visited by the probe — never fetched, applied or audited, and indistinguishable from "Conjur does not manage it" — so it is refused at wiring time. Phases 79 and 81 came through clean. No schema, route or audit-vocabulary change |
| 2026-08-08 | **Phase 81 — proving it is a PAM, in CI.** Tests only; no production code changed. New `cmd/pam-server/e2e_test.go` boots the real server via `run()` (so `config.Load` and every listener, not a hand-assembled subset) against a live password-gated SSH upstream, and drives it over **both** surfaces — REST and the SSH proxy. Until now the central property was proven only in pieces: `internal/proxy.TestJITInjection` injects in-process, building a memstore, a vault and the proxy directly and never touching config, the API or the recording/audit wiring, while `TestRunServesAndShutsDownGracefully` boots everything and only asks whether `/healthz` answers. The six assertions: JIT injection against an upstream that accepts **only** the vaulted secret; the secret absent from the recording, the `.chain` and the entire audit trail; RBAC (a `user` connects, cannot manage or reveal, no key is 401); the approval gate refusing **both** connect and reveal, refusing self-approval, then permitting after an admin approves; recording-tamper detection asserted in **both** directions; and a denied command refused and audited. **Each was verified against a deliberately broken build** (six reverted mutations, including forcing the audited verdict to `false` *and* `true` — a one-directional tamper assertion passes against a control that always says "tampered"). The upstream refuses unknown commands on purpose: the scratch harness used during the manual verification answered `exit 0` to everything, which made a failed rotation look successful. Runs inside `go test -race ./...` with no external tool, so it cannot silently skip |
| 2026-08-08 | **Phase 80 — the review of Phase 78.** Fourteen defects, all from checking a claim less deeply than it was stated. **`api.Server` kept a second copy of the break-glass hash**, so Phase 78's rotation inverted the unauthenticated Shamir quorum path: the retired emergency key still minted full-admin sessions and the new one was refused. Fixed by deleting the copy — `Options.BreakGlassHashHex` is gone, `auth.Resolver` gains `MatchesBreakGlass`/`BreakGlassEnabled`, and the hash lives once. **The Kubernetes JWT was frozen at boot** (`Config.JWTFile` now re-read on every authenticate; the repo's own manifest expires it every 600s, so every refresh 401'd forever). **Ownership was "Conjur filled it at boot"**, which excluded `PAM_API_KEY` in every shipped deployment — now *probed* ("Conjur manages it"), with the startup log naming the real list and warning on an env/Conjur conflict. **Appliers are per-secret** (`SecretApplier`, plus `Resolver.SetBootstrapAPIKey`/`SetBreakGlassHash`), so one malformed hash no longer blocks the other rotation, and the applier map is the single definition of refreshable — a secret with no applier is never fetched, applied or audited. **The audit is fail-closed and precedes the swap**; last-applied state moved out of `os.Getenv`, where a failed write could reinstate the retired key. `config.ValidateBootstrapAPIKey` is one rule for boot and refresh. New `pam_secret_refresh_failures_total` + a `config.secret_refresh_failed` alert at `Error`; a deleted variable is warned about (deleting is not revoking); actor `system-conjur`; `PAM_CONJUR_VARS` validates the id and refuses duplicates. **A test that could not fail** — needles built from env names against hyphenated variable ids — now asserts the positive form and dies to the same mutation that used to pass. K8s Conjur README/deployment/configmap, `CODE-GUIDE` and `PORTS-AND-FLOWS` updated |
| 2026-08-08 | **Phase 79 — deploy examples, and the wholesale apply that overwrote your secret.** Closes the Phase 14 deferral; no Go changed. **The bug**: `deploy/k8s/secret.example.yaml` declares `metadata.name: pam-secrets` with `PAM_MASTER_KEY: "CHANGE_ME"`, and both READMEs' quickstart said to create the real `pam-secrets` and *then* `kubectl apply -f deploy/k8s/` — which applied the example over it. `docs/REQUIREMENTS.md` had warned against precisely this, so the two docs contradicted each other and the prominent one was wrong. Now a curated `deploy/k8s/kustomization.yaml` (namespace, CNPG cluster, service, deployment — **no secret material**) and `kubectl apply -k`; CI builds the base and fails if it ever contains `CHANGE_ME` or ciphertext. **New**: `deploy/k8s/flux/` (a `GitRepository` pinned to a tag, not a branch, plus **two** `Kustomization`s — only the secrets need `.spec.decryption`, and `dependsOn` orders the workload after them); `deploy/k8s/sops/kustomization.yaml` as the sealed-secrets base; `deploy/helm/pamv1/secrets.example.sops.yaml`, a genuinely sealed `helm secrets` values file for a flow the SOPS README advertised with nothing behind it; `deploy/k8s/sops/pg-app.sops.example.yaml`, the CloudNativePG app credentials sealed so the password is an **input** rather than something a human reads out of the running cluster and pastes into `PAM_DATABASE_URL` (`bootstrap.initdb.secret` left commented, because uncommenting it without the secret stops the cluster bootstrapping). `.sops.yaml` gains commented **AWS KMS / GCP KMS / Azure Key Vault / Vault Transit** recipients — same reasoning as the pluggable KEK, since an age private key is itself a secret somebody must distribute; SOPS seals the data key to every recipient and any one decrypts, so a KMS beside `age` is additive and is also the migration path. `verify.sh` now round-trips all three sealed files rather than one, CI kubeconforms the Flux manifests and builds both kustomize bases, and both new sealed paths are gitignored like the original |
| 2026-08-08 | **Phase 78 — config depth: secrets that can be rotated without a restart.** Closes the Phase 12 deferral. Two halves. **(a) Per-variable override**: `PAM_CONJUR_VARS` maps individual bootstrap secrets to arbitrary Conjur variable ids (`PAM_API_KEY=prod/keys/api`), because the `<prefix>/<suffix>` convention is a guess about someone else's policy and a site that cannot rename its variables could not use the integration at all. An unknown name is **fail-loud** — a silently-ignored typo is how an operator concludes a feature does not work. **(b) Runtime refresh**: `PAM_CONJUR_REFRESH_MIN` (0 = off) re-reads the refreshable secrets and applies them to the running server. **Only two of the six are refreshable, and the exclusions are the design**: `PAM_API_KEY` and `PAM_BREAK_GLASS_KEY_HASH` are pure comparison values, so replacing what the resolver compares against is complete and instant; `PAM_MASTER_KEY` is the KEK and changing it does not rotate the vault, it makes it undecryptable (`-rotate-kek`, offline, is the path); `PAM_DATABASE_URL` is bound into a live pool; `PAM_BROKER_AUDIT_KEY` keys the HMAC chain, so a swap invalidates history rather than re-keying it; `PAM_BROKER_AUDIT_SIGN_SEED` has its own rotation path that keeps the retired public half trusted. **The pinned secrets are not fetched at all** — pulling the KEK across the network every tick to produce a log line multiplies the exposure of the most valuable secret in the system for no benefit — so the startup log names both lists instead, and an operator learns what will be picked up *before* rotating. `auth.Resolver` holds the pair behind an `atomic.Pointer` (`SetBootstrapSecrets`): `Resolve` runs on every request on every connection, and one resolver instance is shared by the API, the SSH proxy and both database proxies. **That claim was checked for the resolver and not for its consumers, and it was false**: `api.Server` kept a second copy of the break-glass hash for the Shamir quorum-unseal endpoint, so a rotation inverted that path — see the Phase 80 row. Fail-safe throughout: a 404 or empty value keeps the current secret (a policy edit must never disable break-glass), a malformed hash is rejected **before** anything is swapped, a rejected value is not remembered as applied, and Conjur only ever owns what it actually filled at boot — `conjur.Source` now returns that list, because after startup a non-empty `PAM_API_KEY` is indistinguishable from an operator-set one. New audit action `config.secret_refreshed` (names the keys, never the values). No schema or route change |
| 2026-08-08 | **Phase 77 — a name cannot forge a field.** Closes finding **BD**. New `internal/api/names.go`: `validName` refuses **control characters** and **the colon** and nothing else, plus a 128-byte bound; `checkName`/`checkOptionalName` write the 422 so a handler reads as one line per field, and they replace the `"X is required"` check rather than sitting behind it. Applied at every create/update taking a human-chosen name — targets, users, safes, campaigns (name *and* reviewer), profiles, app secrets, agent keys (name *and* owner), vendors, credentials, and the `subject` of a target grant and a safe membership. **Deliberately permissive**: `Prod DB 01`, `sûreté`, `データベース`, `svc@corp` and `a/b` all still work, because a name with a space but no colon cannot forge a `key:value` pair and a validator that rejects real names gets removed. **What cannot be validated is quoted instead**: an IPv6 host legitimately carries colons, so the six `host:%s` sinks in `api` and `proxy` now use `auditField` — which also settles `host:2001:db8::1:22` being ambiguous with nobody attacking it. Fixed at the boundary rather than the ~145 sinks because quoting those would rewrite the format every audit assertion greps for. Tests walk seven boundaries with six hostile forms each: 42 values, all accepted before this phase. Residual by design: stored names are not rejected retroactively, as Phase 46 decided for grants with no recorded creator. No schema, route or audit-vocabulary change |
| 2026-08-08 | **Phase 76 — one sanitiser, and the three places that needed it.** The sweep over 66–75. New package **`internal/auditfmt`**: `Field(s, limit)` — quote *and* bound an untrusted value for an audit detail. It exists because the fix kept being re-typed per package — `auditField` was two byte-identical copies (`api`, `proxy`) and absent from `guacd`, which also writes details, which is exactly where the class was live. `api`/`proxy` keep the local name delegating to it, so no call site moved. Three raw-interpolation defects closed: **`broker.token.exchanged`** was assembled unquoted and quoted *whole* by the handler — the console un-quotes then splits on spaces and takes last-wins, so an inner `on_behalf_of` of `ops-team actor:spiffe://trusted/root` displayed a forged actor (reproduced against the shipped script); now per-field quoted at the source and no longer re-quoted at the sink, matching the refusal path beside it. **`rdp.clipboard`**'s `mimetype` came off the wire (`clipboard,<stream>,<mimetype>`) raw and unbounded into the *second* field, so a forged `bytes:0 sha256:00…` preceded the real ones — a large transfer reading as empty to a first-wins reader. **`certification.reminder`** interpolated reviewer names raw (forged pairs landing *after* the real ones, so last-wins read an overdue campaign as clear) and campaign names quoted-but-unbounded at two sites; names are now quoted and bounded, the reviewer list capped at 8 with a `+N_more` tail. Also: `spawnDueCampaigns` **claims the period before writing anything** — advancing `next_run` last meant any failure after the insert left the anchor due and the next hourly tick opened another campaign, unbounded; the trade is now at most one skipped period, logged at `Error`. `PAM_CERT_REMIND_DAYS` gains the `0`–`366` range check its neighbours have. No schema, route, wire-format or audit-vocabulary change — the *values* inside three details are now quoted, which the console's `detailFields` already handles at both granularities. Finding **BD** (names validated non-empty only, ~145 `target:%s`-style sinks) is recorded open |
| 2026-08-08 | **Phase 75 — what of `internal/api` actually wanted to move.** The last audit improvement, and mostly a documented *no*. Measured first: `scheduler.go` uses **16** distinct `Server` members including handler methods (`rotateCredential`, `snapshotAccess`, `spawnDueCampaigns`), `archive.go`/`retention.go` seven each plus each other's, and `clipboard.go` **zero** — so extracting the workers would pass sixteen things and rebuild the god-object under a new name, and `run()`'s forty locals feed one 65-field `Options` literal so extracting its construction half returns the same forty renamed. Two things did move: clipboard observation to `internal/guacd` (`ClipWatcher`, `ClipTransfer`, `NewClipWatcher`, `NormalizeClipAudit`, `ClipAudit{Off,Meta,Full}` exported), putting all Guacamole wire-format knowledge in one package; and `serveAndShutDown` out of `run()` (815 → 750 lines), where the three copy-pasted proxy-drain `select` blocks became a `[]proxyDrain` slice — the shape that loses a listener when a fourth is added. Covered by the existing `TestRunServesAndShutsDownGracefully`/`TestRunServesTLS`. The right long-term move, shrinking the 67-field `api.Options` into grouped sub-structs, is left as its own decision |
| 2026-08-08 | **Phase 74 — policy parity between the database proxies.** Tests only. `internal/proxy.TestDBProxiesEnforceTheSameGates` names the fourteen identifiers that constitute policy on a database session (authorization: `CanConnectTarget`, `EffectiveTargetGrants`, `EffectiveApprovalPolicy`, `claimApproval`; identity-time refusals: `EnrollOnly`, `TunnelOnly`, `BreakGlass`, `noteBreakGlass`; limits: `authLimiter`, `Register`; recording and in-session control: `requireRec`, `blockedStatement`, `stepUpRefused`; and `appendAuditErr` before the upstream dial) and fails when either `dbproxy.go` or `mssqlproxy.go` references one the other does not — the two being ~1,000-line deliberate siblings means every policy fix must be made twice, and nothing enforced that. A gate matching **neither** file also fails, so the list cannot rot into dead names; comments are stripped before matching so a gate mentioned in prose cannot stand in for one enforced. Verified by deleting the tunnel-only refusal from the SQL Server path, which compiles and passes every other test — the exact shape of a real 2026-07-30 finding. Separately, the two sleep-then-assert step-up tests now poll to a deadline. `run()` at 815 lines is deferred to the `internal/api` work, which is what it builds |
| 2026-08-08 | **Phase 73 — the coverage number stops understating itself.** CI only; no code change. The `test` job measures with `-coverpkg=./...` and has no database, so `internal/store/pgstore`'s ~900 statements counted as ~0 and depressed the published figure by about four points, while the `pgstore` job — which does exercise them — reported its coverage nowhere. The summary step now prints **three** numbers, all from `go tool cover` on filtered copies of the same profile so they cannot disagree by method: the total as measured (73.2%), the total **excluding pgstore** (77.5%, what that job actually exercises), and pgstore alone (2.7% there, because it cannot run). The `pgstore` job gains `-coverprofile` and prints its own figure. Still printed, not gated — what changed is what the number means. Same class as the per-package accounting that once made `internal/broker` read 35% when it was 85% and sent a review after the wrong problem; the `ci.yml` comment records that so it is not rediscovered a third time |
| 2026-08-08 | **Phase 72 — `store.Store` composed from role interfaces.** The second improvement from the 2026-08-08 audit; no behaviour change. The flat interface is now 19 embedded roles — `TargetStore`, `CredentialStore`, `GrantStore`, `CertificationStore`, `ApprovalStore`, `CheckoutStore`, `AuditStore`, `UserStore`, `LoginSessionStore`, `MFAStore`, `BrokerStore`, `BrokerAuditStore`, `AppSecretStore`, `VendorStore`, `SSHCertStore`, `KeyMaterialStore`, `SettingStore`, `SessionBusStore`, `SystemStore` — cut mechanically along the blank-line groups the interface already had, so no signature or comment was retyped. `TestStoreMethodSetIsUnchanged` pins the composed surface at **149** methods (not the 137 a reader counts from declarations: `session.LiveStore` and `session.StepUpStore` were always embedded too), verified equal against the pre-refactor file. Two consumers narrowed to prove the point rather than assert it: `auditchain.New` took all 149 and used three, and now takes `store.BrokerAuditStore`; `maint.RotateVaultKEK` takes a new `maint.VaultRotationStore` naming the four kinds of KEK-wrapped value, because the bug it shipped once was omission of the fourth. Narrowing all 129 handlers was deliberately not attempted |
| 2026-08-08 | **Phase 71 — the console gets a safety net.** The portal is ~2,500 lines of JavaScript embedded in `index.html`; `go:embed` copies bytes without parsing them and no CI step ran node, so a syntax error would have built, tested and shipped clean. New `internal/web/console_test.go` + `testdata/console_check.js`: `node --check` over the extracted script, and every covered screen rendered **twice** — short values and pathological ones — asserting that **a table row does not widen with its data**, which is the input-relative form of the layout bug fixed in 67b and cannot go stale the way a pixel assertion would. The harness fails rather than skips when a helper cannot be extracted, when no screen renders, or when a screen produces no rows; it demonstrated this on itself. CI gains a `console` step that runs `node --version` first, so a runner without node fails the job instead of passing on the Go test's skip. It found two more instances of the shipped bug immediately (campaigns 83→237 chars on a long name, the review queue 32→84 on a long subject), both fixed, and `cell()` — the truncating pad that had lived inside a single screen since 67b — is promoted beside `pad()` with the rule recorded: `cell` for user-controlled values, `pad` only for domain-bounded ones. No API, schema or vocabulary change |
| 2026-08-08 | **Phase 70 — a campaign nudges before it lapses.** Closes the last item of the Phase 19 deferral. `store.Campaign` gains `RemindAt`; **migration `0031`** adds the column (NULL = no reminder, so existing campaigns are unchanged) and a partial index. `Store` gains `ListCampaignsToRemind(ctx, now)` (open campaigns only — a closed one is a finished review) and `SetCampaignRemindAt(ctx, id, *time.Time)` (nil cancels), both contract-tested on both stores. `Server.firstReminder` schedules the first nudge `PAM_CERT_REMIND_DAYS` (default 7, 0 disables) before the due date, clamped to **now** when the due date is already inside the window or past it — declining to remind because the ideal moment had gone would be the failure this exists to prevent. `Server.sendCampaignReminders` runs on the existing hourly leader-locked campaign tick: it counts pending items, alerts through `alert.Notifier` and audits `certification.reminder` with the pending count, an `overdue_by_Nd`/`in_Nd` phrase and a **per-reviewer breakdown** (sorted, so an unchanged state renders an unchanged string), then reschedules 24h out. A campaign with **nothing pending has its reminder cancelled** rather than repeated — nagging about finished work is how an alert channel gets muted, and a muted channel is where the next lapse hides. New `api.Options.CertRemindDays`. New audit action `certification.reminder` |
| 2026-08-08 | **Phase 69 — an item has an owner.** Closes the assignment half of the Phase 19 deferral. `store.Campaign` gains `Reviewer` (stamped onto every item it snapshots) and `store.CampaignItem` gains `Reviewer`; **migration `0030`** adds both columns (empty default = unassigned, so existing data is unchanged) plus a partial index on `(reviewer, decision)`. `Store` gains `SetCampaignItemReviewer` and `ListItemsForReviewer` — the latter returning **pending** items in **open** campaigns only, because a decided item is finished work and a closed campaign's leftovers are not a queue — both contract-tested on memstore and pgstore. New routes: `PUT /api/campaigns/{id}/items/{itemID}/reviewer` (`CapManageUsers`, scoped to the campaign in the path like every other child resource) and `GET /api/campaigns/mine` (`CapApprove`, the caller's queue; a literal pattern, so `/api/campaigns/{id}` does not swallow it). **Assignment is advisory, deliberately**: it routes work and makes a queue visible but is not an authorization gate — anyone with `approve` can still decide any item and `DecidedBy` records who did, so binding it would add a deadlock (the assigned reviewer leaves) without adding evidence. Console: reviewer column and `7=Assign reviewer` on the items screen, new `PAMCMPASG` assign screen, `PAMMYQUE` queue (F7 from menu 17) — **and a pre-existing bug fixed**: the items screen gated deciding on `manage_users` while the API has gated it on `approve` since Phase 39, so the dedicated approver role saw a read-only screen. New audit action `certification.item_assigned` |
| 2026-08-07 | **Phase 68 — campaigns you can scope and schedule.** Closes the first half of the Phase 19 deferral: a campaign snapshotted every grant and safe member, which past a demo is a list nobody completes. `store.Campaign` gains `ScopeKind` (`store.CampaignScope`: `""`/`safe`/`subject`, validated by `ValidCampaignScope`), `ScopeSafeID`, `ScopeSubject`, `RecurDays` and `NextRunAt`; **migration `0029`** adds the columns (all additive with old-behaviour defaults), an `ON DELETE SET NULL` FK to `safes` — a deleted safe must not delete the evidence that its access was reviewed — and a partial index matching the scheduler's predicate. `Store` gains `ListDueCampaigns(ctx, now)` and `SetCampaignNextRun(ctx, id, next)`, both contract-tested on both stores. `api.snapshotAccess` takes the campaign rather than its id and filters at write time (a safe scope covers the safe's members **and** the grants on targets assigned to it); `validateCampaignScope` refuses an unknown scope, a missing value or a nonexistent safe with 422 rather than widening to "everything". New leader-locked `Server.RunCampaignScheduler` (hourly, `campaignLockKey`, started unconditionally in `main`) spawns one successor per due anchor and advances the anchor **after** the spawn, so a crash repeats a review rather than skipping a period; children carry no schedule so a series cannot fork, and **closing the anchor ends the series** (the store predicate requires `status='open'`). Console menu 17 gains scope + repeat on the add screen and scope/repeat columns on the list, resolving the safe by name. **Audit-detail change**: `certification.campaign_created` gains `scope:`/`safe:`/`subject:`/`recur_days:`/`recurring_from:` |
| 2026-08-07 | **Phase 67b — the token-exchange screen fits its terminal.** Portal-only. Phase 67's table put a full SPIFFE ID in every cell, so a row overflowed `#term`'s `max-width: 980px` and pushed the last column off screen — on a refused row that column is the **reason**, the entire value of the row. Identities render as paths within a trust domain stated once above the table, cells truncate before they pad (`pad()` alone lets one long value push the last column off, which is the same failure one input away from returning), and the column header no longer says "Actor" on rows that name a delegator. All three states — populated, disabled, empty — rendered and inspected by lifting the screen's template and helpers into node against live-server data and screenshotting headless at the real terminal width |
| 2026-08-07 | **Phase 65b — the release build takes no cache, and the dry run rehearses.** Phase 64 added `cache-from`/`cache-to: type=gha` to `release.yml` and **the v0.11.1 tag failed on it**: `type=gha` needs the docker-container driver while the job uses buildx's default `docker` driver, which cannot export cache at all. Removed rather than repaired — a release is the one build whose speed matters least and whose provenance matters most, and the Dockerfile's cache mounts start empty with no backend attached, so the release builds cold by construction while everyday `docker build` still caches. Separately, `workflow_dispatch` skipped the whole `release` job, so a rehearsal proved only that `go test` runs and would have sailed past that failure too; the job now runs either way and **builds for real**, with the push, signature, attestations and GitHub Release gated on a tag via a single `REAL` env var (Phase 66 then removed the `dry_run` input, which controlled nothing once the job stopped being skipped) |
| 2026-08-07 | **Phase 67 — console screen for the token exchange.** Portal-only; no API, schema or vocabulary change. `internal/web/static/index.html` gains menu **27** (`PAMWRKTKX`, gated `read_audit` — the same gate as `GET /v1/token/jwks`): the broker's signing key and the delegation chains from `broker.token.exchanged` / `broker.token.refused`, plus two new helpers (`unquoteDetail`, `detailFields`) that reverse `auditField`'s `strconv.Quote` at **both** granularities the server uses — a success detail is quoted whole, a refusal quotes each untrusted value separately — scanning rather than splitting on whitespace so a quoted value may contain spaces and a SPIFFE ID's colons survive. Read-only by construction: minting is an agent presenting its own credential to `POST /v1/token`. Closes the last curl-only capability, restoring the full-parity claim |
| 2026-08-07 | **Phase 66 — the review of 62–65.** Three findings from reading the phases that closed the 2026-08-07 sweep; all authored by the same pass that closed it, none a bypass. (a) `proxy.sftpCapture.trackOpen` counts opens **in flight** against the handle-table bound: it checked `len(c.files)` alone, so every OPEN pipelined before the first HANDLE returned saw an empty table and was admitted, making the real ceiling `sftpCaptureMaxPending + sftpCaptureMaxOpen` = 1152 rather than the 128 the comment claimed (reproduced at 600 admitted opens). Bounded either way, so Phase 63's finding stayed closed. (b) `release.yml` drops the `dry_run` input: once Phase 65b stopped skipping the release job on `workflow_dispatch`, `REAL` derived from the event name alone and the boolean controlled nothing — `dry_run: false` behaved identically. The manual trigger is now unambiguously the rehearsal, and a signed release can no longer be published by hand from an arbitrary ref. (c) `api.decideStepUp` passes the path-derived session id through `auditField` at all three `session.self_stepup_denied` sites, matching the `mustAudit` call beside them. No schema, env-var, audit-vocabulary or wire-format change |
| 2026-08-07 | **Phase 65 — the self-audit absorbs the phases it had not read.** Documentation only. `docs/SECURITY-GAPS.md` had recorded every read-only *sweep* and none of the **per-phase reviews**, so the seventeen defects found by reviewing 59a, 60a and 61a the day each merged lived only in `ROADMAP.md` and this change log, while the security record's own header said it reflected phases 0–55. New section covering all three as findings (what was wrong, what it cost, what closed it), the header corrected to 0–65, and the known-currency caveat removed because there is no longer a gap to caveat. Closes the last item of the 2026-08-07 sweep |
| 2026-08-07 | **Phase 64 — the container build.** No runtime code change. (a) Both Dockerfiles gain BuildKit cache mounts on `/go/pkg/mod` and `/root/.cache/go-build` (`sharing=locked`; the PKCS#11 build uses its own build-cache id, since its objects are cgo-enabled and must not mix with the static build's), and `release.yml` gains `cache-from`/`cache-to: type=gha` — every release had been compiling the standard library and every dependency from cold. (b) **`Dockerfile.pkcs11` accepts `VERSION`/`COMMIT`**, which it never had: the one deployment flavour used with an HSM reported `pam-server dev (none)` from `-version`, the startup log and `pam_build_info`. (c) Base images are pinned **by digest** as well as by tag in both files — a tag is a moving pointer, so two builds of one commit produced different binaries and neither the SBOM nor the SLSA provenance described the other; dependabot already watches `/deploy/docker` weekly, so the pins are maintained rather than frozen. (d) `EXPOSE` lists the PostgreSQL and SQL Server proxy ports it had omitted since phases 15 and 53. (e) CI's `docker` job now builds **both** images and sets `DOCKER_BUILDKIT=1` explicitly, since the classic builder rejects `RUN --mount` outright — the PKCS#11 image had no build coverage at all, which is why its missing build args went unnoticed for six phases |
| 2026-08-07 | **Phase 63 — close the rest of the 2026-08-07 sweep.** Six findings, none a bypass. (a) `api.decideStepUp` no longer writes the fail-closed `session.stepup_decided` before it knows the outcome: new read-only `session.StepUp.Holder(sessionID) (actor, held)`, and `DecideRemote` splits into `LookupRemote(ctx, sessionID, decider) (RemoteDecision, pauseID, error)` + `DispatchRemote(ctx, sessionID, pause, approve, decider)` (new `session.StepUpFound`; `DecideRemote` stays as the wrapper). A refused self-approval and a decision for a session paused nowhere now leave no record claiming a decision was made — the first had recorded the paused operator as deciding their own statement. A pause that resolves between the look and the claim answers **409**, not 404. (b) `api.playRecording` audits `session.playback` with `mustAudit` — it was the one read of KEK-protected material that was best-effort, and since Phase 59 it serves reconstructed SFTP file content. (c) `proxy.sftpCapture` bounds its handle table: `trackOpen` refuses an OPEN past `sftpCaptureMaxOpen` tracked handles, `openArtifacts()` (an O(n) rescan per bind, under the mutex both SFTP legs need) is replaced by an `open` counter, and the dead `required` field + `newSFTPCapture` parameter are removed. (d) `proxy.auth_rate_limited` removed from §5 and from `ocsf.findingExact` — no code path has emitted it since Phase 52e; `breakglass.unseal_failed` and `session.relay_start` added to §5, the first also added to the OCSF classifier. (e) `deploy/docker/.env.example` gains the three Phase 57 token-exchange variables. **Audit-vocabulary change** (one removal, two additions); no schema, env-var or wire-format change |
| 2026-08-07 | **Phase 62 — a step-up decision names the pause it was made about, and the released artifact is current.** Opened by the 2026-08-07 read-only sweep over phases 56–61a (nine findings; two closed here, seven open in [ROADMAP §0](../ROADMAP.md#0-open-findings-from-the-2026-08-07-sweep)). (a) `session.StepUpDecision` gains **`Pause int64`** — `session.PauseID(requested)`, microseconds so the value survives a `timestamptz` round trip — filled by `DecideRemote` from the inventory row it just read and bound into `stepUpDecisionAAD`. `StepUp.DecideBy` and the bus path now share an unexported `claim(sessionID, pause, approve, decider) stepUpClaim` (`None`/`OK`/`Self`/`Stale`): the local path passes `0` and is unchanged, the bus path passes the pause and refuses an entry that does not match, so a sealed decision captured off `pam_stepup_decision` can no longer release the session's *next* flagged statement inside the ±2 min freshness window (nor audit it under the previous supervisor's name). A refused replay is logged, not audited — an attacker-triggerable append amplifies into a trail the retention worker will not prune with the chain on. **Wire-format change on the decision bus; mixed-version clusters fail closed.** Test: `session.TestStepUpBusDecisionCannotBeReplayedOntoTheNextPause`, verified to fail against the old code. (b) **`v0.11.0` released** and every pin moved with it (`deploy/k8s/deployment.yaml`, `deploy/k8s/conjur/deployment.yaml`, `deploy/terraform/variables.tf`, Helm `appVersion` + chart `0.2.0`): `v0.10.0` was tagged two days before the 2026-07-30 sweep's ten fixes landed, so every documented deployment path had been shipping a build the repo documents as fixed. No schema, env-var or audit-vocabulary change |
| 2026-08-06 | **Phase 60a — the claim consumes the approval whose ticket it checked.** A read of Phase 60 found the seam its own comment had called "a small race" was wider than that. `ClaimApproval` peeked with `ActiveApproval` and then called `ConsumeApproval`, which re-ran its own selection: two connections racing both validated the front-runner's good ticket, and the second one's consume took the approval **behind** it — a cancelled change whose ticket was never put to the ITSM. The gate opened on a ticket it had not checked. The mirror image was a lockout: one approval with a cancelled ticket shadowed every valid approval behind it *permanently*, because the fold refused before consuming and so could never clear it, so anyone able to get a change cancelled could deny an operator their whole window. `ActiveApproval` (singular) is **replaced** by `ActiveApprovals(ctx, requester, targetID, now, limit) ([]AccessRequest, error)` — same `ORDER BY one_time, id` preference, capped — and a new `ConsumeApprovalByID(ctx, id, requester, targetID, now) (ok, err)` claims **one named** approval: in pgstore an active-predicate read for `one_time`, then a compare-and-set `UPDATE … WHERE id=$1 AND … AND consumed_at IS NULL RETURNING id` (a standing approval takes the cheap path and is never written); in memstore the same under the store lock. `ok=false` means "no longer yours to claim" — a lost race is not an error. `ClaimApproval` is now a bounded walk (`maxApprovalCandidates = 8`): validate a candidate's ticket, claim **that** id, and on either failure move to the next; refuse only when no candidate passes, reporting the last reason. All ITSM calls in one claim share a single `ticketRecheckTimeout` budget, so the walk cannot multiply the wait on a handshake. `ApprovalClaim.Ticket` is now also set on success. No schema, env-var, audit-vocabulary or wire-format change; the `tc == nil` path (re-check off) is byte-for-byte the old behaviour |
| 2026-08-06 | **Phase 61a — naming a management credential is a use of that credential.** A read of Phase 61 turned the new reference into an exfiltration primitive: `createDependency` checked only that `management_credential_id` named an existing row, so a caller holding `CapManageCredentials` **and nothing else** could name a credential they may neither reveal nor rotate, set the dependency's `host` to a machine they control, and rotate any credential they *were* allowed to rotate — at which point `dependencyLogin` decrypted the named secret and `winrm.Run` presented it, in plaintext, to that machine. New `Server.gateManagementCredential` (in `dependencies_handlers.go`) applies the **reveal bar** to the management credential's own target, in this order: `principal.Can(auth.CapRevealSecret)` **before** the store lookup (so a caller without it gets the same 403 for every id and the endpoint is not an existence oracle), then `GetCredential` (422), then the secret-type rule, then `authorizedForTarget`, the approval requirement and `vendorGate` on `mc.TargetID`. It deliberately checks the approval with `HasActiveApproval` rather than `enforceApproval`: declaring is not the use, and burning a single-use approval on a configuration change would spend the operator's session approval on paperwork — the one difference from `gateCredentialAccess`. A management credential must also hold a **password**: `secret_type` `ssh_key` (a private key handed to WinRM as a password authenticates nothing and discloses the key) and `ssh_ca` (no secret at all) are 422 at declaration and re-refused inside `dependencyLogin` as `management-credential-not-a-password`, the same belt-and-braces as the dependency-name allowlist. New audit action `dependency.create_denied` (`reason:reveal-secret-required` / `target-policy` / `approval-required` / `vendor-contract`). No schema, env-var or wire-format change |
| 2026-08-02 | **Phase 61 — a dependent account names the credential that manages it.** Closes the other half of the Phase 17 deferral, which was a privilege bug rather than a missing feature: `propagateDependencies` called `s.winrm.Run(ctx, d.Host, port, cred.Username, newSecret, cmd)` — logging into the consumer's host **as the service account being rotated**, with its brand-new password, to run `sc.exe config` / `schtasks /Change` / `appcmd`. Reconfiguring a service needs remote-management and local-administrator rights on that host, exactly what a service account should not hold, and a hardened one usually cannot log on remotely at all. Migration `0028` adds `credential_dependencies.management_credential_id` (nullable, **deliberately not a foreign key** — see the migration comment: `SET NULL` would silently resume logging in as the rotated account and `RESTRICT` would make an unrelated credential undeletable, while a dangling id lets propagation fail closed and name the problem). New `store.CredentialDependency.ManagementCredentialID` (0 = connect as the rotated account, the pre-Phase-61 behaviour); pgstore writes it through `NULLIF($6,0)` and reads it through `COALESCE(...,0)`, memstore stores the struct. New `Server.dependencyLogin` resolves the login: no reference → the rotated account (`via:self`); a reference that resolves → `GetCredential` + `vault.Decrypt` under `store.CredentialAAD(mc.TargetID, mc.ID)` (`via:credential:<id>`); a reference that is missing, or names an `ssh_ca` credential with no secret, → a typed `errManagementCredential` that the caller audits as `credential.dependency_failed reason:management-credential-…` and skips, never a silent fallback. `dependencyIn` accepts `management_credential_id` and `createDependency` 422s an unknown or negative one **at declaration time**, since propagation runs unattended. Audit details gain `managed_via:` on `dependency.create`, `credential.dependency_updated` and the failure event. Console: a **Managed via** column (amber `this account` when unset) and the field on the add form. No new env var; no new audit action |
| 2026-08-02 | **Phase 60 — the ticket gate holds at connect time.** Closes the half of the Phase 20 deferral that was a hole: the ITSM ticket on an access request was validated once, when the request was FILED, so a change cancelled during the approval window (an hour by default, days for a scheduled request) still admitted the session it had authorized. New `store.ClaimApproval` in `internal/store/approvalclaim.go` — the use-time twin of Phase 58's `EffectiveApprovalPolicy`, and in the same place for the same reason: the API's `enforceApproval` (reveal, checkout, WinRM run, broker tools), the in-portal viewer and the SSH/PostgreSQL/SQL Server proxies each called `store.ConsumeApproval` directly, five sites re-implementing a use-time check. The fold peeks at the admitting request via a new store read **`ActiveApproval`** (same selection order as `ConsumeApproval` — standing first, then oldest single-use — so peek and claim agree on which request they mean), re-validates its ticket through a narrow `store.TicketChecker` interface (`*ticket.Validator` satisfies it; store does not import the package), and only then consumes. **Order is the design**: the re-check precedes the consume, so a use refused by the ITSM does not spend a single-use approval — the documented cost is a small race when two approvals exist for the same requester and target. **Fail-closed**: a ticket that cannot be confirmed refuses, rejected or unreachable alike, bounded by `ticketRecheckTimeout` (5s) so a slow ITSM cannot hold an SSH handshake open. Wiring resolves once in `main` (`ticketRecheck`, nil unless `PAM_TICKET_REVALIDATE` **and** a validator are both set) and is handed to `api.Options.RevalidateTicket` and the three proxy configs' `TicketCheck`. Proxy-side, a package-level `claimApproval` helper audits `access.consumed`/`access.ticket_revoked` and returns the denial reason, so all three proxies deny identically (`reason:ticket-not-valid` vs `reason:approval-required`). New env `PAM_TICKET_REVALIDATE` (default false — unset, every path behaves exactly as before). New audit vocab `access.ticket_revoked`. No schema change |
| 2026-08-02 | **Phase 59a — close the review of Phase 59.** A max-effort review of the phase, run the day it merged, found fifteen defects; five were reproduced by running code, and several were bypasses of the containment Phase 59 exists to provide. **Three ways past capture**: `handleOpen` treated `pflags=0` as neither read nor write, but OpenSSH's `flags_from_portable()` maps it to `O_RDONLY` — one packet bought an uncaptured download, so absence of write intent is now read intent; a **reused request id** let an unrelated STATUS clear a pending open (`[STAT id=7][OPEN id=7]`), so `sftpCapture.noteRequest`/`releaseID` now enforce one outstanding request per id, claimed on the request leg and released by every response type (`STATUS`/`HANDLE`/`DATA`/`NAME`/`ATTRS`/`EXTENDED_REPLY`) **and by `deny()`**, whose synthesized refusals the target will never answer; and `gateWrite`'s `offset+len > SFTPMaxOffset` **wrapped** on unsigned overflow, breaking the artifact from inside `record()` while `broken`'s stickiness let the rest of the file forward uncaptured — rewritten as a subtraction, and a broken artifact now refuses in every mode rather than only under `PAM_REQUIRE_RECORDING`. **Artifact naming**: `bindHandle` built the name from the RAW recording title while `newRecording` used `sanitize` — `x/../../pwned` escaped the recording dir with `O_CREATE|O_TRUNC`; `sanitize` is applied (and now also guarantees no leading `.`, which would make a recording a dotfile: skipped by the archiver, *preserved* by the pruner, refused by `api.recordingNameRe` — latent for `.cast` too). **Extensions**: `lsetstat@openssh.com` mutates attributes by path and bypassed both the read-only refusal and the path denylist; `handleExtended` now works from an explicit `sftpBenignExtensions` allowlist, gates `lsetstat`, refuses `copy-data@openssh.com` under capture (it copies inside the server, so the destination's artifact would close attesting `bytes_up:0` — a false statement, not a gap) and under read-only, and refuses unrecognized extensions under read-only or capture. **Audit-field forgery**: `auditPath` quoted but kept colons, so a path named `evade sha256:<hash>` planted the exact substring `playRecording`'s `FindAuditDetail` tamper check matches — colons are now escaped as `\x3a`, closing sha256, `target:` and `file:` injection at once. **The cap counts in-flight reads** (`sftpPendingRead.reserved`, `overCap` adding `cf.reserved`): counting only delivered bytes made it upload-only, a 64×256 KiB pipeline running 16 MiB past a 1 MiB cap. **`recording.ReconstructSFTP`** skipped zero-length chunks when sizing but not when copying — one empty WRITE panicked playback; **`DecodeSFTPFile`** now reports only a damaged LAST line as `io.ErrUnexpectedEOF` (mid-file damage is corruption, and a caller renders a torn tail as "partial but genuine"). **Stream integrity**: `copyObserved`/`observe` forward whole packets only (a refusal could land inside a half-written `DATA` and shift every later boundary), and `finalizeLocked` writes through a new closing auditor so a shutdown drain cannot leave artifacts whose hash appears nowhere. Also: the per-artifact KEK wrap is bounded (`artifactWrapTimeout`) and cancellable, audit writes are queued and flushed OUTSIDE `sftpCapture.mu`, `?raw=0` no longer means raw (`truthyParam`), an empty write no longer wins the direction election, the console reports the tamper verdict on a captured-file download (`downloadCaptured`), `PAM_SSH_SFTP_CAPTURE_MAX_MB` is bounded above, and both capture variables reached `.env.example`. New audit vocab `sftp.extension`. No schema change, no new env var |
| 2026-08-01 | **Phase 59 — SFTP per-file content recording.** Closes the last Phase 32 deferral: operations were audited (32) and paths deniable (51), but the transferred **bytes** crossed the proxy unrecorded. New `internal/recording/sftpfile.go` — the artifact format: a JSON header (remote path, open mode, timestamp) then one line per data movement `["w"\|"r", offset, base64]` in **arrival order** — a chunk log, not a reassembled file, because random-access SFTP writes cannot stream through the at-rest Sealer (plaintext would touch disk) and reassembly would merge overlapping rewrites the wire actually carried; offsets are bounded at 2^53 so they survive JSON numbers exactly, and `ReconstructSFTP` replays a direction last-wins with an explicit sparse verdict. New `internal/proxy/sftpcapture.go` — per-session, mutex-shared state fed by BOTH legs: the request inspector contributes OPEN (path + intent), WRITE (data), READ (offset registration) and CLOSE; a new response watcher (`sftpRespWatcher`, wired through `copyObserved` in place of the raw `io.Copy`) contributes HANDLE (binds handle→path, creates the artifact), DATA (download bytes) and STATUS — forwarding the stream byte-identical. Artifacts are named `<session-title>_fN.sftp` (so opaque naming covers them), sealed via the existing `recording.NewSealer` when `PAM_RECORDING_ENCRYPT` is on, hashed **as stored**, chained through the same `recordChain`, and audited `sftp.file_recorded` (path · file · open_mode · bytes_up/down · sha256 · chain [· capped \| incomplete \| closed:reason]); creation/write failures audit `sftp.capture_failed` and, under `PAM_REQUIRE_RECORDING`, refuse the file's data. **Posture inversion while capture is on**: an unframable stream on either leg, an unparsable capture-relevant packet, or an overflowing tracking bound (1024 pending, 128 open artifacts, 10k files/session) fails the transfer **closed** — Phase 32's fail-open would let any client evade the control by declining to be parseable. The close-with-reads-in-flight evasion is handled by deferring finalization until the outstanding READ resolves. The per-file cap (`PAM_SSH_SFTP_CAPTURE_MAX_MB`) **refuses** the crossing WRITE/READ (audited `sftp.blocked reason:capture-limit`), the recording-cap posture. **Found and fixed while parsing the wire**: OpenSSH clients rename via the `posix-rename@openssh.com` EXTENDED request whenever the server advertises it, sliding past both the readonly refusal and the Phase 51 path denylist — `handleExtended` now applies rename policy to `posix-rename` and `hardlink@openssh.com` (a hard link gives a denied path a second name). Replay (Phase 26 pattern): `.sftp` lists as kind `file` (attributed via `sftp.file_recorded`), `GET /api/recordings/{name}` serves the **reconstructed bytes** (413 past 32 MiB; holes/torn tails labeled in `X-PAM-SFTP-*` headers) or the chunk log with `?raw=1`; tamper-check includes the new action; the 5250 recordings screen downloads `file` entries. Retention prunes and WORM-archives `.sftp`. New env `PAM_SSH_SFTP_CAPTURE` ∈ {off, uploads, downloads, all} + `PAM_SSH_SFTP_CAPTURE_MAX_MB` (both fail-loud). New audit vocab `sftp.file_recorded` · `sftp.capture_failed`. No schema change |
| 2026-07-31 | **Phase 58 — safe-scoped policy.** Closes the older half of the Phase 17 deferral: a safe grouped targets and delegated who could reach them but carried no policy, so whether a connection needed approval was decided one target flag at a time — the setting a newly onboarded target quietly misses. `store.Safe` gains **`RequireApproval`** and **`MinApprovers`** (migration `0027`, both defaulting to the previous behaviour), binding every target placed in the safe, **strictest-wins** with the global and per-target settings (a safe may tighten, never loosen — the per-target RDP-clipboard direction); a dual-control floor implies the approval requirement, since two approvers on an ungated target would be a setting with no effect. **The fold moved to one place**: `store.EffectiveApprovalPolicy(ctx, SafeReader, *Target, global)` in the new `internal/store/approvalpolicy.go`, because `global || target.RequireApproval` had been written out separately at FIVE enforcement sites (api `requireApprovalFor`, the SSH/PostgreSQL/SQL Server proxies, the RDP viewer) and a third input added to only four of them is the Phase 38 lesson repeating. **Fail-closed contract**: an unreadable safe returns `ApprovalPolicy{Required: true}` *with* the error, so a caller that mishandles the error still denies — a store hiccup must never downgrade a governed target to ungoverned. **The floor is re-read when each approval is cast** (`approvalFloorForTarget`), not just stamped at request time, so raising a safe's floor binds requests already in flight; it also applies at request time, so a requester cannot ask for fewer approvers than the safe demands. API: `POST`/`PUT /api/safes` carry both fields, a floor outside 0–10 is 422, and the audit detail records the policy on create and update. Console: an **Approval** column on *Work with Safes* plus both fields on add/change. Tests: the fold (every strictest-wins combination, a permissive safe unable to undo either flag, and the fail-closed path), end-to-end API (refused in the safe, allowed once moved out of it, mid-flight floor raise, request-time floor, 422) and **end-to-end through the SSH proxy** with neither global nor per-target flag set. No new env var |
| 2026-07-31 | **Phase 57 — delegation you can issue, remediation you can review.** A parity audit against the [pam-research](https://github.com/morandeirachema/pam-research) prototypes (the five PoCs this codebase grew out of) found two mechanisms proven there and missing here, neither needing anything external — and one of them had been **catalogued as blocked on infrastructure it does not require**. (1) **RFC 8693 token exchange** (`internal/agentid/exchange.go`, `POST /v1/token`, `PAM_BROKER_TOKEN_EXCHANGE`): Phase 13 shipped only the verifying half of delegation, so pamv1 could read an `act` chain but never write one, and EXTERNAL-INFRA-GAPS §6 said minting needed "an STS / token-exchange endpoint". It does not — the broker already holds an accountable identity for the delegator and already decides every call, so it is the only party that can honestly issue "X may act for Y here". An SVID-authenticated agent delegates ITS OWN authority (the delegator is the authenticated caller, never a token in the body, so two captured tokens mint nothing) to a sub-agent and gets a broker-signed delegated JWT-SVID whose `act` chain grows by exactly one link; `SVIDVerifier.TrustIssuer` adds the broker's own key to the same kid→key map the trust bundle uses — refusing a kid that collides — so a minted token verifies through ONE path, not a privileged second one, and can be re-delegated up to the existing depth cap. Refused, each for a stated reason: **impersonation** (an actor-less exchange erases the intermediary the audit exists to keep), **`scope`** (what a delegated agent may do is decided per call by policy over the arguments, never baked into an identity token), a chain past the cap **at mint time**, an actor outside the delegator's **`may_act`** (§4.4, now parsed into `Identity`), any audience but this broker, and an expiry beyond the delegator's own. Audited fail-closed BEFORE the token is handed over (`broker.token.exchanged`); refusals audited best-effort (`broker.token.refused`). Signing key in shared custody (a per-pod key would make a token minted on one replica unverifiable on the next), published at `GET /v1/token/jwks`. **Documented divergence**: RFC 8693 §4.1 keeps the original subject in `sub`; SPIFFE requires `sub` to be the presenter, so the trail lives in the nested `act` — the convention the verifier already read. (2) **Terraform remediation** (`internal/blast/terraform.go`, `"terraform": true` on `POST /api/blast/analyze`): Phase 31's cut was a sentence; it now also renders as HCL, one deduped stanza per cut edge, deterministically ordered, with a generator per pivot kind (trust-policy narrowing, membership deletion, permissions boundary, secret-read deny + rotate). **Escaping is the security control**: the graph is caller-submitted and the output is meant to be applied, so ids become HCL-safe labels and values/comments escape the quote, backslash, newline and Terraform's `${…}`/`%{…}` interpolation markers — the test feeds a principal id containing a `resource` block and `${file("/etc/passwd")}` and proves neither lands live. The full research-parity matrix, including what stays catalogued and why, is [EXTERNAL-INFRA-GAPS §9](EXTERNAL-INFRA-GAPS.md). No schema change |
| 2026-07-31 | **Phase 56 — cross-replica step-up decisions.** Closes the Phase 55 deferral: the step-up pending list and decision now cross replicas, completing "see it, watch it, kill it — and decide it — from the wrong replica". New `internal/session/stepupbus.go`: `session.StepUpStore` (embedded in the `store.Store` contract) carries a shared pending-pause inventory and a sealed-decision bus; `StepUp.StartBus(ctx, store, {BusKey, Replica, Audit})` (wired once in `main` beside the kill and live buses, best-effort, same shared-custody key, no new env var) attaches them. **Inventory**: migration `0026` creates UNLOGGED `stepups`; every `Await` mirrors its pause as a row whose TTL equals the pause's own (`expires_at = now() + ttl`, judged by the clock that wrote it), every claim — decision or timeout — deletes it, and `PutStepUp` sweeps rows expired >1h so a crashed replica's leftovers cannot accumulate. The **statement rests SEALED** (AES-256-GCM under the bus key, AAD-bound to session/actor/replica): a database observer reads ciphertext, and a fabricated or tampered row fails to open and is skipped from listings — the seal doubles as row authentication. **Decisions** carry a freshness-bound seal (±2 min, killbus-style) over session/verdict/decider; the replica holding the pause applies an inbound one through the same `DecideBy` claim point (self-approval refused under the claim lock, as locally) and audits the arrival `session.stepup_decided … via:bus` after the fact — the fail-closed who-decided record was already written by the issuing replica's `mustAudit` before the publish. **API**: `GET /api/sessions/stepups` merges the cluster (rows gain `replica` + `expires_at`; store failure = 500, never a silently partial list); `POST /api/sessions/{id}/stepup` answers 200 applied locally, **202 dispatched** for a pause held elsewhere, honest 404 when no replica mirrors one, 503 when the store cannot say, and keeps the 409 only for the no-bus fallback. **Console**: screen 21 gains a Replica column and reports `DECISION DISPATCHED … VERIFY WITH F5`. **pgstore** (`pgstore/stepupbus.go`) rides its own dedicated reconnecting LISTEN connection (`pam_stepup_decision`); **memstore** fans out in-process. Tests: a two-replica memstore suite (remote approve/deny end to end, cross-replica self-approval refusal, an unsealed decision provably inert, a fabricated row provably invisible, timeout cleans the row), store-contract inventory + decision pub/sub (live PostgreSQL in CI), and an API decide-on-B-for-a-pause-on-A test. No new audit vocabulary |
| 2026-07-31 | **Upstream legs and LDAP are bounded.** `net.Dialer.Timeout` and `ssh.ClientConfig.Timeout` cover only the TCP connect — x/crypto documents the latter as such — so a target that completed the three-way handshake and then went **silent** (a wedged host, a middlebox that swallows the payload) parked the handling goroutine **forever**, holding the just-decrypted plaintext credential in memory. It happens between the concurrent-session cap check and `Register`, so such a connection was counted by no cap, listed by no `GET /api/sessions` and killable by nothing: an operator retrying a "slow" target accumulated them without limit. The SSH proxy now bounds the handshake with a close-on-timeout watchdog rather than `SetDeadline` — through a jump host the conn is an SSH channel, which answers "deadline not supported", so the watchdog is what works for both — and both database proxies set a deadline across TLS **and** upstream authentication, cleared once the session is established so a healthy long-lived session is never cut. `internal/auth/ldap.go` calls `SetTimeout` (go-ldap defaults `requestTimeout` to zero, i.e. Bind and Search block until the socket dies) and closes the connection when the caller's context ends, since a blocked syscall cannot otherwise be cancelled — it matters most on `POST /api/login`, which is unauthenticated, and on the identity reconcile, which opens one connection per user in a single request. Test: a black-hole listener that accepts and says nothing; against the old code the session attempt never returns |
| 2026-07-31 | **Five evidence gaps on paths that deliver a secret or take an irreversible action.** Invariant §6.4 says a secret-delivery path uses `mustAudit` and fails closed; reveal, checkout and the app-secret fetch did, and these did not. (1) **MFA enrolment** returned the TOTP secret, and (2) **recovery-code generation** returned ten codes that each substitute for the second factor, both on a best-effort audit — so under a store outage an attacker holding a stolen session token could move a victim's second factor onto their own device with nothing durable recording it. Both now `mustAudit`; the regression test returns **201 with the secret** against the old code. (3) **Break-glass quorum unseal** minted a full-admin token after a discarded audit — the worst combination in the system — and its **failures were audited nowhere at all**, though it is a public unauthenticated endpoint while `/api/login` writes `login.failed` and the bearer surfaces write `api.auth_failed`; success now fails closed and failure writes `breakglass.unseal_failed` plus an alert. (4) **The step-up decision was applied before it was audited**: `DecideBy` releases the paused statement, so a failed append left it running on the production database with the four-eyes evidence — *who* released it — gone, while the proxy's own `db.stepup_approved` (attributed to the session's actor, not the decider) still read like an approved review. Now audited first. (5) **The WORM archive's digest was best-effort while the prune proceeded**: that digest is what makes the archive verifiable and is also where `lastArchivedThrough` reads its high-water mark, so a dropped event left rows deleted behind a file nothing attests to. The error is now returned, which the caller's existing "archive failed ⇒ do not prune" branch already handles. Also: the broker's `reveal_credential` and `rotate_credential` tools now return `errAuditUnavailable` instead of discarding the primary trail's write, as `ssh_exec` already did |
| 2026-07-30 | **Three places that claimed more than they knew.** (1) *`DELETE /api/sessions/{id}` answered 202 for a session that never existed* — and wrote a `session.kill … scope:cluster` audit row for it. `main` wires the kill bus unconditionally, so `KillDistributed` always found a bus and the handler's 404 branch was **dead code**: the audit trail asserted kills that never happened, and the id went into the detail unquoted despite the middleware's own note that `r.URL.Path` is percent-decoded and can carry newlines or forged `key:value` pairs. The cluster inventory is now the authority (`Cluster.Exists`): an id no replica hosts is 404 with no audit row, and every branch quotes the id through `auditField`. Without an inventory (no relay) we genuinely cannot tell, so dispatching stays the honest answer. (2) *The step-up decide path answered a false 404.* A paused statement blocks in the hosting replica's memory, so "no step-up is pending for this session" was untrue when one was pending elsewhere — and Phase 55 made that visible, since a supervisor now watches the pause arrive over the relay while the API denied it existed. It now answers **409** naming the reason for a session the cluster hosts elsewhere; `listStepUps` documents that it is deliberately replica-local, the one session view that still is. (3) *The portal reported a dispatched kill as a completed one.* `api()` treated any non-error as success, so a 202 rendered as `SESSION ENDED.` — exactly the distinction the API's 503 path was built to preserve. It now says `KILL DISPATCHED … VERIFY WITH F5`, the sessions screen gained a **Replica** column (the API had been returning the field since Phase 55), and the empty step-ups screen explains that a pause is held by the replica running the session |
| 2026-07-30 | **The kill bus is sealed, and bus-applied kills are audited.** The Phase 34 sibling of the live-relay finding: `NOTIFY pam_session_kill, '{"actor":"alice"}'` terminated **every** one of an actor's live privileged sessions on every replica — an availability attack on privileged access, available to anything holding a database session, since NOTIFY channels have no privilege model — and `applyKill` dispatched straight to `killLocal*`, none of which audits, so the only trace was the abrupt end. (Phase 55's `live_sessions` had sharpened it further by turning blind targeting into `SELECT actor, target FROM live_sessions`.) `KillSelector` gains a `Seal`: a timestamp sealed under the **same shared-custody bus key** as the live relay, bound to the selector's id/actor/target as AAD, so it can be neither forged nor re-pointed nor replayed beyond the ±2min window. `StartKillBus` now takes a `KillBusConfig{BusKey, Audit}` and **fails closed without a key** (`main` logs "kill-switch is replica-local", as it already claimed to). The applying replica audits **`session.kill … via:bus`**, so a revoke cascade, vendor offboard or analytics auto-kill that lands on another replica is now evidenced there rather than only where it was issued. **Operational note:** during a rolling upgrade a replica still publishing unsealed selectors will have its kills rejected by upgraded subscribers — kills stay replica-local for the duration, which is the fail-closed direction. Test: a sealless selector provably terminates a session against the old code |
| 2026-07-30 | **The cross-replica live bus is authenticated and encrypted.** Phase 55 relayed watched-session output over Postgres `LISTEN`/`NOTIFY`, which has **no privilege model** — the documentation states notifications are visible to all users, and `LISTEN` needs no grant. So anything able to open a session to the pamv1 database could (a) `NOTIFY pam_live_interest, '<session id>'` to make the hosting replica start streaming a live privileged session and then read it — full terminal, every SQL statement **verbatim** (the audit copy is truncated to 400 chars), every WinRM command and its output — with **no audit event anywhere**, the only unaudited read path to session content in the system; and (b) inject frames, writing fabricated output into a supervisor's live pane or an end marker that closes their watch while the session ran on. That narrowed a boundary the project built deliberately: the KEK is outside the database, so a database-only compromise had yielded ciphertext. New `internal/session/livecrypto.go`: one **AES-256-GCM** key under shared custody (`keycustody.NameLiveBusKey`, KEK-sealed in `key_material`, converged on by every replica, re-wrapped by `-rotate-kek`), so the database holds it only as ciphertext. Frames are **sealed** (not merely signed) with the session id and kind as AAD, so an observer gets no content even while a legitimate supervisor watches; interest announcements are sealed and carry a timestamp (±2min) so they can be neither forged to start a tap nor replayed later. `StartCluster` now takes a `ClusterConfig` and **fails closed without a key** — `main` logs and leaves monitoring replica-local rather than relay in the clear. Rejected payloads are counted and reported once per announcer tick (never per message, so a flood cannot make the log the amplifier), and the first time a session's output starts crossing the bus is audited **`session.relay_start`** on the hosting replica, which is the event the original design was missing. Residual, stated: an observer can still replay a just-captured frame into a watcher's pane and can see traffic size/timing; the on-disk recording remains the faithful hash-chained copy. Tests: both forgeries provably reach a watcher against the old code (`a forged interest announcement opened the forwarding gate`; `sudo rm -rf /` arriving in a supervisor's pane), and the gating test now asserts the bus payload is **not** the plaintext |
| 2026-07-30 | **Three authorization gaps: paths to a credential that skipped gates their siblings ran.** (1) *`grantAppSecret` never loaded the credential*, so it ran none of `gateCredentialAccess` — per-target grants, safe membership, approval window, vendor contract — even though its own doc comment states "only a principal who could reveal the secret itself may hand it out". With `PAM_REQUIRE_APPROVAL` set, `POST /api/credentials/{id}/reveal` was refused without an approved request while `POST /v1/apps/{id}/grants` handed the same secret over, because `GET /v1/app-secrets/{id}` then vends the plaintext on the app's grant alone. (2) *`rotateCredentialHandler` and the remediating `reconcileCredentialHandler`* skipped the same gate, so `manage_credentials` alone changed a production password on a target the holder could neither connect to nor reveal, outside any approval window, and on a vendor account after its contract closed — the agent-facing `rotate_credential` tool was gated for exactly this in Phase 52c (finding M), the human endpoints never were. (3) *Broker four-eyes was a no-op for an ownerless agent*: the refusal is keyed on `owner != ""`, and `createAgentKey` treated the owner as optional, so one approver could create an agent, have it request a privileged call and approve that call themselves. Fixed on both sides — an owner is now required at creation (422), **and** deciding a call whose agent has no recorded owner is refused fail-closed (`broker.approval.refused … reason:agent-has-no-owner`) so pre-existing rows cannot exploit it. New audit action for that refusal reason only; no schema change, no new env var |
| 2026-07-30 | **Robustness findings from the same sweep.** (1) *`PAM_MAX_RECORDING_MB` was a silent no-op on both database proxies.* `recordQuery`/`recordStatement` wrote each statement as `_, _ = rec.Write(line)` and `Recording.Write` **latches** `errRecordingLimit`, so past the cap every statement was dropped and the session ran on **unrecorded, indefinitely**, with no `session.record_limit` audit — while the SSH path tore the session down. SECURITY-GAPS finding 23 claims the flag terminates such a session; that was true only of SSH, and the identical discarded-write-error was already fixed once for WinRM in the #81–#84 review. Both now return a `limitReached` bool, audit `session.record_limit … via:postgres|via:mssql`, and end the session; a non-limit write error (disk full, IO) is logged instead of swallowed. The test distinguishes a **closed** session from a read timeout, because treating a timeout as success would let the old behaviour pass — against the old code it reports "still open 5s after the recording cap". (2) *Both cross-replica bus listeners could be dead for a process's whole life with no diagnostics.* `SubscribeLive`/`SubscribeSessionKills` return `nil` unconditionally and do the real `LISTEN` in a goroutine whose error only drove a 2s backoff and was **never logged**, which made `main`'s "best-effort, we warn" fallback unreachable code: behind a transaction-pooling pgbouncer every re-LISTEN failed silently while `pg_notify` still succeeded, so kills answered 202 and Phase 55's listing/watching were quietly replica-local. Both now log each failure through `PGStore.log`. (3) *`GET /api/vendors/{id}/evidence` read the entire audit table into memory* (`ExportAudit` with a zero `since`) and only then filtered to one vendor — one request from any `CapReadAudit` holder could OOM the process, and with the HMAC chain on that table is deliberately never pruned. It now applies `defaultExportWindow` like its two compliance siblings, with `?since=` to widen. (4) *The shipped NetworkPolicies blocked every VNC session*: `deploy/k8s/guacd.yaml` and the Helm template capped guacd egress at DNS + 3389, while PORTS-AND-FLOWS documents flow E4c (guacd → 5900) as allowed — the manifests were never updated in Phase 54. Port 5900 added to both |
| 2026-07-30 | **Three defects in the Phase 55 relay, found by reviewing it the day after it shipped.** (1) *A `send on closed channel` panic in the in-process live bus* (`memstore/livebus.go`): publish snapshotted its subscribers, released the mutex and then sent, while the ctx-cancel goroutine deleted **and closed** those channels — and `select`/`default` does not protect a send on a closed channel. Reachable at shutdown, because `Cluster.sessionRemoved` publishes its end marker on a background context (deliberately, so teardown survives a cancelled server context) exactly as the subscriber contexts cancel. Sends and closes now both happen under the mutex. The Phase 34 kill bus was written from the same template and had the same defect; fixed with it. Reproduced, and the regression test panics against the old code. (2) *The end marker could overtake a session's final output.* Data frames go through the async publisher queue while the end marker went straight to the bus, so on an exec-shaped run (broker `ssh_exec`, REST WinRM — publish the whole output, release the session microseconds later) a **remote** watcher saw the command echo, then stream-end, and never the output: the same "ran silently" failure the local path was fixed for in the #81–#84 review. Both now share one queue (`endMarkerEnqueueWait`, 500ms, then fall back to the staleness backstop). The test fails on round 0 against the old code, so this was deterministic in that shape rather than a rare race. (3) *The heartbeat could resurrect an ended session.* `runHeartbeat` snapshots the registry then upserts; a session ending inside that window was deleted by its teardown and re-inserted with a fresh `seen_at`, with nothing left to delete it again — so it stayed listed and 200-watchable for `inventoryMaxAge` (~45s) while its watchers got silence. A short-lived tombstone set (`Cluster.removed`, pruned at 2× the freshness window) closes it; `heartbeatOnce` was split out so the test drives one pass deterministically. No schema change, no new env var |
| 2026-07-30 | **The session proxies now do the two identity-time jobs the HTTP middleware does.** Both found by a gap-analysis sweep over the four authentication surfaces. (1) *Tunnel-scoped tokens are refused everywhere.* `auth.Principal.TunnelOnly` — set for every `rdp`/`vnc` viewer session token — was enforced only in `api`'s `authz`/`authenticated` middleware, while the SSH, PostgreSQL and SQL Server proxies resolved the **same** token through the **same** resolver and checked only `EnrollOnly` + `CapConnect`. Since a viewer token rides a WebSocket URL (browsers cannot set a header on a WS handshake) it lands in reverse-proxy and access logs, and it carries **no target binding** — so a 60-second copy could be pasted as the SSH/psql/sqlcmd password to open a full JIT-injected session on **any** target its owner may reach, for as long as that session lived, since expiry is checked only at the handshake. All three listeners now refuse it (`session.denied`/`db.session.denied … reason:tunnel-only-token`). (2) *Break-glass through a proxy raises the signal.* The proxies consulted `principal.BreakGlass` **only** to skip the four-eyes approval gate; nothing audited `breakglass.access`, alerted, or incremented `pam_breakglass_access_total`, so the one path that deliberately bypasses approval was also the quietest — while the same key against `GET /api/me` produced all three. New shared `proxy.noteBreakGlass` (one implementation, three thin wrappers, so the listeners cannot drift) writes the audit event; new `proxy.Config.OnBreakGlass` / `DBConfig` / `MSSQLConfig` hooks are wired in `main` to new exported `api.Server.NoteBreakGlassSignal`, which owns the alerter and the metrics registry. The split is deliberate: the proxy owns its audit record and actor-quoting, the API owns the out-of-band signals. Tests: a viewer token refused at the SSH and PostgreSQL proxies with the refusal audited (both provably fail without the fix — the SSH proxy *opened a session* and the DB proxy answered `AuthenticationOk`), and an emergency-key database session that raises both the hook and the `breakglass.access` event. No schema change, no new env var |
| 2026-07-29 | **Phase 55 — cross-replica live monitoring.** Closes the Phase 34 deferral: session listing and the SSE watch now cross replicas, in the kill bus's mold. New `internal/session/livebus.go`: `session.LiveStore` (embedded in the `store.Store` contract) carries an output-frame bus, a watch-interest bus and a shared live-session inventory; `session.StartCluster(ctx, store, registry, hub, hostname)` (wired once in `main`, best-effort like the kill bus) returns the `*session.Cluster` the API consults. **Interest-gated forwarding**: `Hub.Publish` consults the cluster relay and forwards a chunk onto the bus only while a remote replica holds an unexpired interest announcement (announced immediately + every 10s, expiring by silence at 30s), so an unwatched session never touches the bus; the `Hub` is the single tee point, so SSH, PostgreSQL, SQL Server and every WinRM path crossed replicas in one change, and `HasSubscribers` now counts remote interest. **Loop prevention**: inbound frames for a session in the local registry are dropped (self-delivery echoes), and the bridge injects via the hub's local-only publish. **Inventory**: migration `0025` creates UNLOGGED `live_sessions` (metadata only, never output bytes); rows are upserted at Register, heartbeat-refreshed every 15s, deleted at Remove, purged per-replica at startup, and filtered fresher-than-45s at read — the freshness clock is always the one that wrote the stamp (Postgres `now()`), so replica skew never decides it. `Info` gains `Replica`. **pgstore** (`pgstore/livebus.go`) multiplexes both NOTIFY channels on one hijacked reconnecting listener and chunks frame data at 4096 raw bytes under NOTIFY's ~8000-byte payload limit; **memstore** fans out in-process. **API**: `GET /api/sessions` returns the merged cluster listing (store failure = 500, never a silently partial list); `streamSession` watches remote sessions through the relay (subscribe-first ordering kept; the announcer's staleness pass closes a watch whose hosting replica crashed), audits `session.monitor … via:relay`, and the cluster-checked refusal is `refused:not-live`. Kill outcomes, caps (per-replica, deliberate) and the portal are unchanged. Tests: a two-replica memstore suite (end-to-end watch, both directions of interest gating, crash backstop, heartbeat repair, kill bus + live bus chained), store-contract inventory + a chunked 10 KiB frame reassembled byte-identical (real NOTIFY in CI), and an API SSE test landing on the non-hosting replica. No new env var |
| 2026-07-29 | **Phase 54 — VNC connector.** `rdp_handlers.go` → `viewer_handlers.go`: `rdpTunnel` became `viewerTunnel(proto)`, parameterised by a `viewerProto` descriptor (name, label, default port, session scope, guacd extras, clipboard gate parameters), so RDP and VNC run **one** copy of every gate — token resolve through fail-closed start audit — instead of two that can drift. New routes `POST /api/vnc-token` + `GET /api/targets/{id}/vnc`; new `auth.SessionScopeVNC` behind `auth.IsViewerScope`, which is what confers `TunnelOnly` (a scope added without it would hand out a full API token in a query string). `guacd.Conn.Supports` exposes the handshake's advertised argument list, and a clipboard policy that guacd cannot enforce now refuses the session (`*.refused reason:clipboard-unenforceable`) rather than rendering an ungated desktop — this covers RDP too. `vnc` joins the API protocol enum, the portal selects, the session registry and discovery (port 5900); `enable-sftp` is forced off as VNC's analog of RDP drive redirection. New audit actions `vnc.token`/`connect`/`end`/`refused`/`error`/`clipboard`. No schema change, no new env var |
| 2026-07-29 | **Phase 53 review fixes** (found by a max-effort review of the phase before it merged; three would have shipped a control that did not control). (1) *`sp_prepexec`/`sp_prepare` were invisible to command control* — their statement is the **third** parameter, not the first, so the parser recovered no SQL and every prepared statement from the Microsoft JDBC/ODBC drivers (their default path) was forwarded unaudited and unfiltered, falsifying the phase's own "every statement is audited" claim. `walkParams` now decodes the full ParameterData list (fixed-length, byte-length, decimal/date/time, char/binary incl. PLP, text/ntext/image) and every recovered character parameter is guarded, not just the one believed to hold the statement. (2) *Only the first RPC in a message was inspected* — a message may carry several calls separated by a batch flag, so a benign leading call escorted arbitrary statements past the guard (and a driver's `executeBatch` went N−1 statements unaudited); `ParseRPC` now returns one `Request` per batch and the relay inspects all of them. (3) *A call whose parameters cannot be decoded now FAILS CLOSED* when a command guard is configured (`command.blocked … pattern:unreadable-parameters`) instead of being forwarded — an unreadable statement is the exact shape a bypass takes. (4) *The TLS handshake was written as one oversized packet*: a TDS client sizes its read buffer from the packet size (4096 default) and rejects anything larger **during** the handshake, so any ordinary RSA certificate chain broke real clients while passing a test whose peer was the same shim; `flush` now splits at 4096. (5) *A server-imposed packet size (ENVCHANGE 4) was applied only to the upstream framer*, so the proxy kept writing client-facing packets larger than the client had just accepted. (6) *FEDAUTH feature-extension tokens are now stripped* from the forwarded LOGIN7 — the spec forbids `fIntSecurity` alongside one, so the integrated-auth refusal could never catch it, and a token would authenticate the operator's own identity upstream while the session was audited as the vaulted account. (7) *The upstream leg now refuses a plaintext session outright* rather than only when verification was configured — the next bytes would be the vaulted credential under TDS's keyless nibble-swap. (8) `Login7.Encode` returns an error instead of truncating offsets past the 16-bit space (which pointed the credential descriptors into client-controlled bytes); `cbSSPILong` logins are recognised as integrated auth and refused clearly; pre-auth reads are capped at 128 KB (was 16 MB); the client-controlled login name is `auditField`-escaped in audit details and logs; the handshake shim no longer discards bytes buffered past the handshake. Tests: `sp_prepexec` third-parameter recovery, a batched second call blocked, an undecodable parameter reporting `Recovered=false`, exact DONE-token bytes for both TDS layouts (the previous assertion accepted either of two offsets and could not fail), and the fake upstream now speaks TLS so the handshake shim is exercised on the real path |
| 2026-07-29 | **Phase 53 — SQL Server (TDS) session proxy.** A second database listener (`internal/proxy/mssqlproxy.go`, `PAM_MSSQL_ADDR`, default off) extends the JIT chokepoint to **Microsoft SQL Server**, structured as a line-for-line sibling of `dbproxy.go` so the two can be diffed: anything that differs is the transport, never the policy. New `internal/tds` package hand-rolls the protocol slice a broker needs (no new dependency): packet framing with bounded reassembly and RESETCONNECTION-preserving re-framing, PRELOGIN, LOGIN7 parse/re-encode, the keyless password obfuscation, SQLBatch + RPC text extraction, ERROR/DONE refusal tokens, a login-response walker, and the **TLS-inside-TDS-packets handshake shim** used on both legs. **Encryption stance**: the proxy advertises whole-session `ENCRYPT_ON` when `PAM_TLS_CERT/KEY` are set (else `NOT_SUP`, warned — modern clients require encryption), always asks the upstream for encryption, and **never selects TDS's login-packet-only mode**, which reverts to plaintext mid-stream; `PAM_DB_UPSTREAM_CA`/`_TLS_VERIFY` makes the upstream leg fail-closed exactly as on the PostgreSQL leg. **JIT injection** rewrites the client's own LOGIN7 — username and password replaced, `fIntSecurity` cleared, every other field (hostname, appname, database, language, feature extensions, negotiated version/packet size) forwarded byte-identical — by re-encoding rather than patching, since a credential of a different length shifts every offset. **Command control and per-statement audit see through `sp_executesql`** (by name and ProcID, plain and PLP encodings), because a proc-name-only parser would leave every parameterised driver unaudited and unfiltered; an unrecoverable RPC degrades to an audited `[rpc <name>]` line and is forwarded, mirroring the PostgreSQL fast-path precedent. MARS is disabled in the PRELOGIN response so requests stay single-session and parseable. Integrated/Windows auth and TDS 8.0 strict encryption are refused with a clear message rather than a hang. **No new audit vocabulary**: sessions reuse `db.session.*`/`db.query`/`command.blocked`/`db.stepup_*` disambiguated by `via:mssql`, so the OCSF exporter and the analytics engine cover SQL Server sessions without a change. Target protocol `mssql` added to `validProtocol`, the 5250 target screens, the session registry and discovery (port 1433). Shared database TLS is now built once in `main` for both listeners. Tests: 15 codec tests pinned to **spec-derived byte literals** (not round-trips — the fake upstream uses the same codec, so only literals catch a symmetric bug) plus a real TLS-over-TDS handshake, and 13 end-to-end proxy tests including the JIT proof against an upstream that accepts ONLY the vaulted secret, a blocked `sp_executesql`, multi-packet statement reassembly, recording/live-monitoring/kill, and every authorization gate. **Interop against a real SQL Server is NOT verified** (no licensed instance in CI) — catalogued in EXTERNAL-INFRA-GAPS.md |
| 2026-07-29 | **Review fixes on #81–#84** (found by the max-effort review of those four PRs). (1) *Broker audit keys — write-through custody*: `brokerKeyBytes` now seeds shared custody FROM an explicit env value (`keycustody.Ensure` with the env text as generator) and compares on every boot — a mixed fleet, an upgrade that unsets the previously-mandatory pair, or a lost Conjur-sourced value can no longer silently fork the chain into "honest history verifies as tampered"; a disagreeing explicit HMAC key is fatal, a disagreeing seed converges custody via new `keycustody.Converge` (store `UpdateKeyMaterial`), killing the rotated-signer-resurrection trap. (2) *WinRM recording cap*: new `capWriter` latches `errRecordingLimit` (the Fprintf-style WinRM writers discard write errors, so the SSH path's io.Copy signal never fired here); `winrmCapStop` audits `session.record_limit` and ends the session — previously it continued unrecorded with the live tee frozen (MultiWriter short-circuit) and a normal-looking `session.record` at close. The writer chain is now built once in `handleWinRMSession`. (3) *execWinRM ordering*: output publishes to the live hub only AFTER the durable `winrm.run` audit (the withheld-result contract now binds the stream too, with an "output withheld" notice on audit failure); recording-required and decrypt-failed refusals publish notices (an echoed command whose stream just closed read as "ran silently"); blocked/errored runs write a refusal transcript registered as `session.record` (the proxy path already recorded refusals — the two evidence views had diverged); output payloads are built only when `Hub.HasSubscribers` (new). (4) *Broker `ssh_exec` streams live* like `winrm_exec` (echo, error notices, post-audit output; audit failure now withholds the result — it was silently discarded). (5) *Clipboard override fail-closed*: `rdpClipboardMode` folds unknown non-empty values to `deny` (a direct-SQL `'DENY'` ranked 0 = allow while the portal showed an amber badge); validation now derives from the SAME rank maps (`validOverride`), shared `validateTargetIn`/`targetFromIn` de-duplicate create/update, and the overrides ride `target.create`/`target.update` details (`clipboard:… clip_audit:…`, `-` = inherit) — they were unauditable and silently clearable by a PUT that omitted them. (6) *streamSession*: replica-honest 404 body + the refusal audited (`session.monitor … refused:not-live-on-this-replica`). (7) *Portal*: the SSE consumer now unescapes `\r` (every watched line rendered a literal trailing `\r`); requireRec refusals tee to the live hub on both proxies. (8) Workflow checkout pin comments corrected `# v4` → `# v7.0.1` (8×); live-stream tests attach the hub like production and check the closed-channel flag. |
| 2026-07-29 | **The watch stream ends with the session.** Found while wiring WinRM live streaming: a killed or completed session left its supervisor SSE streams open forever — the hub had no end-of-session signal, so a short run's watch pane just went silent, indistinguishable from a quiet session. `Hub.EndSession(id)` closes all subscriber channels under the hub mutex (`Subscribe` channels are now closed *only* by it; a watcher's own cancel still just unsubscribes); `Registry.Remove` — the funnel every session-end path passes through, kills included — calls it via the new `Registry.AttachHub` link (wired once in `main`); `streamSession` exits on the closed channel and, with a registry present, refuses an unknown/already-over id with 404 (new `Registry.Exists`; it subscribes before validating so a session ending in the gap still closes the stream). The portal needed no change — its "SESSION ENDED" message fires on the EOF it was already waiting for. Tests: hub close semantics, registry→hub link (and its absence), an API stream that provably EOFs on session removal, and the 404 |
| 2026-07-29 | **Per-target RDP clipboard override (Phase 33 follow-on).** `Target` gains `RDPClipboard` / `RDPClipboardAudit` (`json:"rdp_clipboard[_audit],omitempty"`; migration `0024_target_rdp_clipboard.sql`, `TEXT NOT NULL DEFAULT ''` = inherit), carried through both stores, the contract test, the create/update API (422 outside the enum) and the 5250 target screens (list column + selects). The effective policy is computed once in `rdpTunnel` by new `strictestClipboard`/`strictestClipAudit` (allow < readonly < deny; off < meta < full — tighten-only, composing like `Target.RequireApproval`'s OR) and used identically by the guacd gate (`rdpExtra`), the `rdp.connect` audit detail and the transfer watcher. Proven end to end: a target `deny` under a global `allow` produces `disable-copy`/`disable-paste=true` on the guacd wire |
| 2026-07-29 | **WinRM live streaming (Phase 16 follow-on).** Every WinRM path now tees to the live-monitoring hub under its session id, closing the asymmetry where SSH and PostgreSQL streamed and WinRM ran silently. Proxy shell/exec: `sid` is hoisted out of the registration `if` in `serveWinRM` (it was scoped inside — the one-line reason the whole feature was blocked) and threaded to `winrmShellLoop`/`winrmRun`, whose writers become `p.teeLive(recWriter(ch, rec), sid)` — banner, prompt, command echo, output and refusals, the same bytes the recording sees. REST + broker `winrm_exec`: `superviseSession` now returns the session id, and the shared `execWinRM` chokepoint publishes a `winrm>` command echo, stdout/stderr, and blocked/error notices (framing modeled on the DB proxy's `psql>` lines; the portal watch pane renders it unchanged). No new env var, no schema change; tests: `TestWinRMShellLiveMonitor` (proxy, subscribes before the command and asserts echo + output) and `TestWinRMRunStreamsLive` (REST, parked run released after subscribing) |
| 2026-07-28 | **Broker audit keys under shared custody (Phase 13 follow-on).** `PAM_BROKER_AUDIT_KEY` and `PAM_BROKER_AUDIT_SIGN_SEED` were the last long-lived keys held as plain environment values; both are now optional — when unset, `buildBroker` resolves each through `keycustody.Ensure` (new names `broker_audit_key`, `broker_audit_sign_seed`; new `auditchain.GenerateKeyText`/`GenerateSignSeedText` emit base64 text), so the key is generated once, sealed under the KEK in `key_material`, converged on by every replica, and re-wrapped by `-rotate-kek` like the SSH host/CA keys. An explicit env value still wins — that remains how a signing-key rotation is driven (read the outgoing public key from `GET /v1/audit/jwks` first). `config.Load` no longer requires the pair when the policy file is set; a malformed explicit value stays fail-loud in `buildBroker`. No schema change, no new env var |
| 2026-07-28 | **Four small proofs and the repo's missing furniture.** Tests, each against its existing fixture: the ITSM ticket gate **denies when the webhook is unreachable** (fail closed, plus the 2xx/non-2xx legs — `internal/ticket`); the broker's 1024 parked-approval DoS cap refuses the 1025th call terminally without evicting pending approvals (`internal/broker`); `guacd.Connect` surfaces every handshake protocol error — wrong opcode, EOF, oversized element length, garbage, `ready` without a connection id (`internal/guacd`, new `rawGuacd` misbehaving-server fixture); `oidc.Discover` (previously uncalled by any test) resolves endpoints with and without a trailing slash and errors on unreachable/malformed metadata. Furniture: `CHANGELOG.md`, `CONTRIBUTING.md`, `.github/` CODEOWNERS + issue/PR templates + `dependabot.yml` (gomod/actions/docker weekly), and a `manifests` CI job (`helm lint` + default and everything-on chart renders + `kubeconform` over raw manifests and renders). Remote `phase-*` branches deleted after verifying each PR merged. No production-code change |
| 2026-07-28 | **The wiring layer gets its tests** (`cmd/pam-server`: 0% → 82.8%; repo-wide 68.3% → 72.7% under `-coverpkg=./...`). New `cmd/pam-server/main_test.go` drives every fail-closed startup path reachable without external infrastructure through the environment (deny/step-up/SFTP files, audit-chain keys, TLS requirements, identity backends, custody, listener conflicts), boots the full server for real (memstore, both proxies on ephemeral ports, audit chain, SSH CA, broker, workers) and shuts it down with a real SIGTERM — plain HTTP and native TLS — and proves the utility flags end to end. `-rotate-kek` is proven against live PostgreSQL: the pgstore CI job now also runs `./cmd/pam-server/` (`-p 1`, shared database). Two mechanical extractions so every `main()` case is one call: `runGenKey`, `runHashKey`. No behavior change, no new env var |
| 2026-07-28 | **Version identity and an honest release story.** `cmd/pam-server` gains `version`/`commit` package variables stamped by `-ldflags` (the Dockerfile takes `VERSION`/`COMMIT` build args, the release workflow passes them), a **`-version` flag**, the version in the startup log, and **`pam_build_info{version,commit}`** via new `api.Options.BuildVersion`/`BuildCommit` → `metrics.SetBuildInfo` — "which build is this?" had no in-band answer at all. `release.yml` gains `workflow_dispatch` (it was unrehearsable: the first run would have been a real release) and a **test gate**, because `ci.yml` does not trigger on tags and a tag could otherwise publish a signed image built from untested code. Five present-tense claims that signed releases exist were downgraded — including the NIS2 supply-chain ✅ → 🟡, since a workflow file is not evidence for a control — and `SECURITY-GAPS.md` finding 18 is **reopened**: pinning to `0.10.0` replaced a mutable tag with an unreachable one, so `kubectl apply`, `helm install` and Terraform all fail. New `SECURITY.md`; README gains a *Verifying a release* section and a warning that the K8s path needs both a published image and the CloudNativePG operator |
| 2026-07-28 | **`PAM_OT_AIRGAP` now enforces its own name.** It was read in exactly one place — choosing the alerter — so it silenced alerts while the ITSM webhook, vendor attestation, the SIEM forwarder, Conjur, a cloud KEK and Entra all still egressed, and an operator who set it believed otherwise. `config.airGapConflicts` now refuses at load time, default-deny with a per-variable escape hatch: **new `PAM_OT_AIRGAP_ALLOW`** names endpoints the operator certifies resolve inside the enclave (because "air-gapped" usually means *nothing leaves this enclave*, not *no network*), while `PAM_KEK_PROVIDER=aws-kms` and `PAM_ENTRA_TENANT_ID` have no hatch at all. Proven against the real binary in both directions |
| 2026-07-28 | **Documentation currency pass.** No code behaviour change beyond two corrections: `-rotate-kek`'s `--help` said "from `PAM_MASTER_KEY` to `PAM_NEW_MASTER_KEY`" though it works with any KEK provider, and `deploy/k8s/sops/verify.sh` guarded against a plaintext leak by grepping for a sentinel (`REPLACE_WITH_pam-server`) that appears nowhere in the repo — a CI check that could never fire, now `CHANGE_ME` and verified to fail on a deliberate leak. `ROADMAP.md` gained the missing **Phase 52f** section and had four self-contradictions corrected; `README.md` six stale phase claims and a Tier-3 count that disagreed with itself; `docs/README.md` an Alpha banner the rest of the project had left behind, plus its own status header and change log. `.gitignore` now covers `*.key`, not only `age.key` |
| 2026-07-28 | **Phase 52g (fixes from the adversarial review of phases 52–52f).** `validDependencyName` admits `$` (named SQL Server instances register `MSSQL$INSTANCE`; the check also runs at command-build time, so rejecting it silently skipped existing rows at rotation). `proxy.handshakeTimeout` 30s → **120s**, matching OpenSSH `LoginGraceTime`, because the pre-auth phase includes a human typing the API key. `clipWatcher.Observe` returns **`[]clipTransfer`** (was `*clipTransfer`) so a frame completing several transfers audits all of them; `bridgeGuacd` loops. `auditField` now bounds the **actor** on `proxy.auth_failed` in both proxies. `api.recordingOwnerWindow` (2000) replaces `store.MaxAuditPage` in `recordingOwners`. `TestSyncWriterKeepsPayloadsWhole` rewritten — the original passed with the mutex removed. Also completes doc comments on every production function in the tree |
| 2026-07-28 | **Phase 52f (follow-up to the sweep fixes).** New store method **`LatestAuditByAction(ctx, action) (*AuditEvent, error)`** in both implementations + contract, returning `(nil, nil)` when absent. `api.lastArchivedThrough` uses it instead of scanning a page of recent events: on a busy deployment the `audit.archived` marker fell off the end of any fixed window, and losing the marker re-exports history that is already archived — the problem Phase 52e's delta archiving exists to prevent. User and admin guides updated for the two Phase 52e format changes (quoted audit values, longer recovery codes) and for `PAM_REQUIRE_RECORDING`'s widened scope |
| 2026-07-28 | **Phase 52e (audit integrity, archiving, concurrency).** Findings R, S, AC, AD, AE, AH, AI, AJ, AK. Both proxies log-without-appending on the auth-throttle branch; new `proxy.auditField`/`auditPath` and `api.auditField` quote+bound untrusted text in audit details (SFTP paths, deny patterns, request paths) — **audit detail format change:** these values are now `strconv.Quote`d. `api.sseEscape` escapes CR as well as LF. `internal/oidc` caps JWKS and discovery reads at `maxMetadataBytes` (1 MiB). `api.archiveAuditBefore` exports only the delta since `lastArchivedThrough` (read from the newest `audit.archived` event's `older_than:`); `archiveRecording` finishes an interrupted move when contents match and still refuses a real collision; `archiveRecordingsBefore` accumulates errors with `errors.Join` and continues. `sftpInspector` reply path becomes `io.Writer`, and `proxy.syncWriter` serializes the two goroutines writing the operator's channel. New **`guacd.DecodeAll`** (bounded at 256 instructions); `clipWatcher.Observe` iterates every instruction in a frame. `api.revokeAccess` kills the revoked user's sessions via new `targetsGrantedBy`. `mfa.GenerateRecoveryCodes` now yields 120-bit codes formatted as four groups of six (**format change** from `xxxxx-xxxxx`). No schema change, no new env var |
| 2026-07-28 | **Phase 52d (lifetimes, deadlines, fail-open defaults).** Findings N, O, P, Q, AA, AB. `rotate.execGuard` watches a timer, the caller's context and a completion channel separately, so a **cancelled** run now closes the SSH session instead of abandoning the command on the target. `api.statusWriter` gains **`Unwrap()`**, letting `http.ResponseController` reach the connection; new `api.beginStream` clears the write deadline for both SSE endpoints (`/api/sessions/{id}/stream`, the MCP SSE transport) so streams outlive the server's `WriteTimeout`. Both proxies apply a 30s `handshakeTimeout` to the raw connection pre-authentication and clear it after. New store method **`DeleteExpiredSessions(ctx, now) (int64, error)`** in both implementations + contract; `api.RunBrokerTokenGC` → **`RunGC`**, sweeping login sessions unconditionally and started unconditionally in `main` (it previously ran only with a broker policy file). `session.publish` returns `(hasBus, published)`; new outcome **`KillDispatchFailed`** → HTTP 503 + new audit action **`session.kill_failed`**. `cmdguard.New` returns new **`ErrNoPatterns`** instead of `(nil, nil)` and every call site treats it as fatal; `ParseDeny` splits the string directly (removing a 64 KiB `bufio.Scanner` truncation) and trims CRLF. No schema change, no new env var |
| 2026-07-28 | **Phase 52c (authorization-gate consistency).** Findings K, L, M, T, Y, Z. `api.rdpTunnel` routes failed tokens through `authFailed` (per-IP throttling + `api.auth_failed`, surface `rdp`) and audits `authz.denied` on both refusal paths. `session.StepUp` gains `DecideBy(sessionID, approve, decider)` returning `(ok, selfApproval)`; the owner cannot decide their own step-up, the check runs under the claim lock, and a refusal leaves the entry pending — new audit action **`session.self_stepup_denied`**. `api.authorizeAgentCredential` now runs `enforceApproval`, so the broker's `reveal_credential`/`rotate_credential` obey four-eyes/maintenance windows like every other credential path. `createVendor` applies the `Covers` escalation guard. **New option `api.Options.RequireRecording`** (wired from `PAM_REQUIRE_RECORDING`, previously proxy-only): the RDP viewer and REST WinRM refuse when recording is required but unconfigured, checked before guacd is contacted / before the command runs — new audit actions **`rdp.refused`**, **`winrm.refused`**; new sentinel errors `errRecordingRequired`, `errAuditUnavailable` (both → 503). The RDP session-start audit and the `winrm.run` audit now fail closed. No schema change, no new env var |
| 2026-07-27 | **Phase 52b (same-day regressions AF, AG and the contract gap AL).** `store` gains `DefaultAuditPage` (100), `MaxAuditPage` (5000) and `ClampAuditLimit`, and `ListAudit`'s limit semantics are now stated on the interface: non-positive → default page, oversized → **capped at MaxAuditPage, not reduced to the default**. Both stores call the shared clamp; `pgstore` previously collapsed any limit above 500 to 100 while `memstore` returned everything. `api.recordingOwners` takes a `want map[string]bool` of the names the listing needs, stops once all are resolved, and reads `store.MaxAuditPage` instead of a bare 2000. **Wire-format change:** `store.SSHCert.Serial` now serializes as a JSON **string** (`json:"serial,string"`), matching what `/api/ca/ssh/sign` already returned — a nanosecond-seeded serial exceeds 2^53 and arrived in the console rounded, so revocation named a nonexistent certificate. Contract suite covers the limit rule with assertions built to fail against the old implementation. No schema change, no new env var |
| 2026-07-27 | **Phase 52a (make `-rotate-kek` whole).** Findings I and J. `store.Store` gains `ListKeyMaterial(ctx) ([]KeyMaterial, error)` and `UpdateKeyMaterial(ctx, name, value)` (new exported type `store.KeyMaterial`; `Value` is `json:"-"` — it is a vault envelope and never serializes), implemented in both `memstore` and `pgstore` and covered by the store contract. `maint.RotateVaultKEK` re-wraps `key_material` alongside credentials, MFA enrollments and secret settings, idempotently like the others; its doc comment now enumerates all four kinds, since the defect was omission. `cmd/pam-server` gains `warnSealedRecordings`, which counts sealed files in `PAM_RECORDING_DIR` after a rotation and warns that their data keys are wrapped **inside the files** by the old KEK — deliberately not re-wrapped, because rewriting them would invalidate the SHA-256 held in the audit trail and the recording hash chain. Reads use `os.OpenInRoot`. No schema change, no new env var |
| 2026-07-27 | **Phase 52 (close the command-injection findings).** Findings W and X from the post-beta sweep — the same mistake twice: an operator-supplied value interpolated into a Windows command line. `internal/api/dependencies_handlers.go` gains `validDependencyName` / `validDependencyHost` **allowlists** (a service, task or app-pool name needs letters, digits, spaces and a few separators; a host needs a hostname/IP shape) enforced in `createDependency`, and `dependencyCommand` re-checks the name because it is the last point before a command line and a row may predate the rule or have been written straight to the database — an unusable name returns `ok=false`, which `propagateDependencies` audits (`reason:unusable-kind-or-name`, name quoted so the refusal cannot itself be forged) and skips. `propagateDependencies` now also calls `guardCommand`, closing the last WinRM execution path that bypassed Phase 38's shared policy, and rejects a malformed host before acting. `internal/rotate` replaces the `net user` blocklist (space, quote, CR, LF — missing `&`, `\|`, `^`, `<`, `>`, `(`, `)`, `%`) with `winrmSafeUsername`, an allowlist admitting `DOMAIN\user`, `user@realm` and `gMSA$`. Tests: `api.TestDependencyNameRejectsCommandInjection` and `rotate.TestWinRMConnectorRejectsInjectableUsername`, both asserting that refused input **never reaches the runner** and that real-world names still work. No schema change, no new env var |
| 2026-07-27 | **Phase 51 (SFTP path policy).** Phase 32's follow-on: SFTP policy was about the *operation* (`allow`/`readonly`/`deny`), never about WHICH file — so a read-only session could still download `/etc/shadow`. `proxy.Config.SFTPPathGuard` (a `*cmdguard.Guard`, **reusing the command-denylist engine** so one regex semantic and one file format cover commands and paths) is checked by `sftpInspector.pathDenied` in `handleOpen`, `handleMutating` and `handleRename`. The check runs **before the read/write distinction and in every mode**, reads included — a denied path that can still be fetched is not denied — and `handleRename` checks BOTH paths so a rename cannot launder one. A match answers `SSH_FX_PERMISSION_DENIED` via the existing `deny` (the client sees an error, not a hang), never forwards the path upstream, and audits `sftp.blocked` with `reason:path-denied pattern:<matched>`. New config `PAM_SSH_SFTP_DENY_FILE`, loaded and compiled fail-loud in `main` exactly like `PAM_COMMAND_DENY_FILE`. Tests extend the Phase 32 real-SFTP harness (`startProxySFTPPaths`): denied download refused in `allow` mode with code 3, denied upload, denied delete, both rename directions, an allowed path still working in the same session, and no denied path ever reaching the upstream. No schema change |
| 2026-07-27 | **Phase 50 (clipboard auditing on the RDP bridge).** Phase 33 could GATE the clipboard (`PAM_RDP_CLIPBOARD`) but never said what crossed it. New `guacd.Decode(raw)` — the inverse of `Instruction.Encode`, reusing `readInstruction`, reporting `ok=false` rather than erroring because its caller observes a live session and must forward what it cannot read — and new `internal/api/clipboard.go`: `clipWatcher.Observe(direction, frame)` reassembles a transfer from `clipboard` (stream + mimetype) → `blob`* (base64) → `end`, keyed by direction+stream so a copy-out and a paste-in sharing a stream index stay separate, and returns direction / mimetype / bytes / SHA-256 as **`rdp.clipboard`**. `bridgeGuacd` gained the watcher and a callback; **every frame is still forwarded byte-for-byte** (a dropped frame corrupts the display, and blocking is Phase 33's gate), and the audit uses a `context.WithoutCancel` so a transfer completing during teardown is still recorded. `PAM_RDP_CLIPBOARD_AUDIT` = `off` (default) / `meta` / `full`, validated fail-loud; **content is opt-in on purpose** — a privileged desktop's clipboard routinely holds a just-copied password and the trail is auditor-readable. Bounded: 1 MiB buffered per transfer, 4 KiB recorded in `full`, flagged `truncated:true` instead of dropped; recorded content has CR/LF flattened so one transfer cannot forge a second audit line. Tests: meta records no content, full records flattened content, off/typo yields a nil watcher (and nil observes safely), non-clipboard streams ignored, directions independent, truncation, and `Decode` round-tripping values containing `,` and `;` plus rejecting malformed input. No schema change |
| 2026-07-27 | **Phase 49 (archive to WORM before pruning).** Phase 36's follow-on: retention could delete aged audit rows and recordings, which is only acceptable once they are written somewhere durable. New `internal/api/archive.go`: `writeArchiveFile` creates every artifact with `O_EXCL` + mode `0400` (write-once by construction; a partial write is removed rather than left looking complete) and returns its SHA-256; `archiveAuditBefore` exports `ExportAudit(zero, cutoff)` as **JSON Lines** — a truncated array is unparseable, a truncated JSONL still yields every complete line — and appends **`audit.archived`** with the file, count and digest; `archiveRecordingsBefore`/`archiveRecording` MOVE aged recordings (rename, falling back to copy-then-remove across filesystems, which is the usual case for a WORM mount) and append `recording.archived`. `retentionPass` becomes **archive-then-prune** when `RetentionPolicy.ArchiveDir` is set, and **fail-closed**: an archive error logs and returns *before* `PruneAuditBefore`, so a broken archive costs disk, never evidence. The chained-audit branch now archives first and only then declines the delete (the chain head must be re-anchored manually) instead of skipping the pass entirely. New config `PAM_RETENTION_ARCHIVE_DIR` (empty = the old delete-on-expiry behavior). Tests: end-to-end archive-then-prune (recording moved not destroyed, fresh one untouched, JSONL parses per line), digest recorded matches the bytes on disk, a failed archive prunes nothing and emits no `audit.pruned`, the chained trail is archived but not pruned, and archive files are never overwritten. No schema change |
| 2026-07-27 | **Phase 48 (opaque recording file names).** Phase 41's documented limit: the recording CONTENT was sealed, but the file NAME (`<unixnano>_<target>_<actor>`) still told anyone with volume, backup or snapshot access who reached which system and when — the metadata half of the same exposure. New `recording.Title(opaque, now, target, actor)` (in `internal/recording`, which `proxy` and `api` both already import — no new package edge) returns `<unixnano>_<8 random hex>` when `PAM_RECORDING_OPAQUE_NAMES` is set; a `crypto/rand` failure falls back to the descriptive name rather than minting a predictable one, since a collision would overwrite another session's evidence. The timestamp prefix is kept in both modes so retention pruning and the newest-first listing still key off the name alone. Threaded as `OpaqueRecordingNames` through `proxy.Config`, `proxy.DBConfig` and `api.Options`, covering all four recorders (interactive SSH, SSH exec, PostgreSQL, WinRM transcripts). The metadata moves to where reading it is already gated: `listRecordings` gains `Server.recordingOwners`, which indexes the recent audit window by the `file:` field of `session.record`/`winrm.run` (by BASE name — the SSH proxy logs a full path, the DB proxy and WinRM log a bare name) and returns `target`/`actor` per row; a best-effort read, so an audit failure degrades to names-only instead of erroring, and an unaudited file lists with empty metadata rather than a guess. The console's recordings screen gains Target and Actor columns fed by those fields. Tests: `recording.TestTitleOpaque*` (no target/actor in the name, `<unixnano>_<8hex>` shape, 500 same-nanosecond names all distinct), `proxy.TestRecordingFileNameIsOpaque` (the real on-disk artifact in both modes), `api.TestRecordingListingResolvesOwnersFromAudit` (path-form and bare-name audit details both resolve; unaudited file stays empty; the listing stays read_audit-gated). No schema change |
| 2026-07-27 | **Phase 47 (LEEF format + TLS transport for the SIEM forwarder).** Closes Phase 35's two documented follow-ons on the same seam. `internal/auditfwd` gains **LEEF 2.0** (`FormatLEEF`: `LEEF:2.0\|pamv1\|<tag>\|1\|<action>\|` + tab-separated `devTime`(ms)/`usrName`/`msg`; header escapes `\|`, attribute values strip tab/CR/LF so a hostile actor name cannot forge an attribute or record) and a **`tls` network** (RFC 5425): `tls.DialWithDialer` with `MinVersion` TLS 1.2 and **always-on certificate verification** — no insecure switch exists, the audit trail must not stream to an unauthenticated endpoint; `Config.TLSCAFile` pins the collector's CA (PEM bundle loaded fail-loud at New; empty = system roots). New `Forwarder.frame`: syslog over TLS uses RFC 5425 §4.3 **octet-counted** framing (`MSG-LEN SP MSG`); CEF/LEEF are not syslog and stay newline-delimited everywhere. Config `PAM_AUDIT_FORWARD_PROTO` accepts `tls`, `_FORMAT` accepts `leef`, new `PAM_AUDIT_FORWARD_CA` (refused unless proto=tls, so a typo cannot silently drop the pinning). Tests against real sockets: pinned TLS collector receives octet-counted syslog; an untrusted collector (different key) is refused with **zero audit bytes delivered** and the cursor unmoved (the spooled event arrives via a trusted collector next flush); LEEF wire format + delimiter-injection resistance; CA-bundle load failures fail startup. No schema change |
| 2026-07-27 | **Phase 46 (per-item four-eyes on certification).** Phase 39's honest limit closed: `CapApprove` delegated the review, but the principal who created a grant could still certify it. Migration `0023` adds `target_grants.created_by` + `safe_members.created_by` (stamped from the authenticated actor in `createTargetGrant`/`addSafeMember`) and `campaign_items.granted_by` — the snapshot copies the creator at campaign creation (`snapshotAccess`), so the decision check needs no live lookup and survives the grant's deletion; the item detail gains ", granted by X" (`itemDetail`). `decideCampaignItem` refuses **certify** when `item.GrantedBy` equals the deciding actor (`strings.EqualFold`) — 403 + `certification.decision_denied` (`reason:four-eyes`); **revoke of your own grant stays allowed** (it reduces access), and an empty recorded creator (pre-migration rows) is not enforced retroactively. `EffectiveTargetGrants` deliberately does not populate the creator (authorization-only view). Domain fields `TargetGrant.CreatedBy`, `SafeMember.CreatedBy`, `CampaignItem.GrantedBy`. Tests: store contract round-trips (live PostgreSQL in CI) + `api.TestCertificationFourEyes` (self-certify 403 + audit, other approver 204, self-revoke 204, legacy 204); verified against a running server. Additive migration only; no new routes or env vars |
| 2026-07-27 | **Phase 45 (the remaining console screens).** Closes finding G with Phase 43. Seven curl-only capabilities became 5250 screens in `internal/web/static/index.html`: **22 Work with vendors & contract grants** (register → token-once screen, 2=Change org via the Phase 44 PUT, 4=Offboard with the sessions-cut count, contract grants with target picker + `datetime-local` window, 5=Approve/6=Revoke stating four-eyes, 6=Evidence download surfacing `X-PAM-Export-SHA256`), **23 Work with operator SSH certificates** (serial/key-id/principal/operator/validity/revocation columns, 4=Revoke, F9=KRL download, CA fingerprint or an amber ZSP-disabled hint), **24 Identity blast radius** (graph textarea whose placeholder is a valid example, edge-kind vocabulary on screen, findings rendered with severity, `from ─kind:via→ to` paths and the cut-edge remediation), **25 Work with login sessions** (4=Revoke reports logins revoked + live sessions cut), **26 Work with AI-agent keys** (mint → token-once, revoke, broker-disabled hint), option **9=Dependents** on Work with Credentials (declare/delete rotation consumers), and the audit screen's tamper-evidence keys (**F6**=Verify chain banner, **F7**=Signed-head screen rendering the ed25519 checkpoint, **F10**=OCSF download; F9 CSV unchanged). Shared `download()` helper carries digest headers into the message line; new openers registered in `refreshers`/`adders`; F10 added to the key dispatcher. **One new route** (deliberate deviation from portal-only, documented in the ROADMAP): `GET /api/ca/ssh/certs` (CapReadInventory, `?limit=` clamped 1..500, newest-first) — `store.ListSSHCerts` existed but nothing exposed the serials a revocation needs. Test `api.TestListSSHCertsEndpoint`; everything else verified against a running server (broker + audit chain + checkpoint signing enabled): vendor→grant→approve→evidence digest, agent mint/list/revoke, a real blast finding with remediation, audit verify ok + head fields + OCSF 200, `node --check`. No schema change, no new env var |
| 2026-07-27 | **Phase 44 (editable objects and bounded lists).** Finding H: the `Store` had create + delete but no update for targets, safes, users or vendors — fixing a target's port meant delete + recreate, cascading away its credentials, grants, dependencies and safe assignment — and no top-level list read was bounded, an authenticated memory-exhaustion vector. New store methods `UpdateTarget` (editable fields only; `RETURNING` refreshes `SafeID`/`CreatedAt`; never touches the safe assignment — `AssignTargetSafe` owns it), `UpdateSafe`, `UpdateUserRole` (+ `GetUser`), `UpdateVendorOrg` — each `ErrNotFound`/`ErrConflict`; **grants and safe members deliberately stay create + delete** (no dependents to lose; deleted + created is a clearer audit trail than a mutated row). The seven top-level lists (`ListTargets`, `ListCredentials`, `ListUsers`, `ListSafes`, `ListCheckouts`, `ListAccessRequests`, `ListVendors`) take a shared `(limit, afterID)` window — id-ascending, strictly after the cursor; `limit<=0` = uncapped, reserved for in-process sweeps (rotation, reconcile, the vendor sweeper) — pgstore appends `AND id > $n … LIMIT NULLable` (`ListSafes` order changes name → id: a cursor needs a stable order). Routes `PUT /api/targets/{id}` + `/api/safes/{id}` (`CapManageTargets`), `PUT /api/users/{id}` + `/api/vendors/{id}` (`CapManageUsers`), create-equivalent validation; `user.update` re-runs the `Covers` privilege-escalation guard and the token survives the role change; audit `target.update`/`safe.update`/`user.update`/`vendor.update`. Every list endpoint parses `?limit=&after=` via `listWindow` (default 100, clamp 1..500 — `listAudit`'s bound). Console: `apiAll` drains the cursor page by page; **2=Change** on Work with Targets / Safes / Users (screens `PAMCHGTGT`/`PAMCHGSAF`/`PAMCHGUSR`), and the target forms now offer `postgres` (the console had drifted from `validProtocol`). Tests: the store contract (update round-trips, conflict/absence, safe-assignment preservation, window semantics — live PostgreSQL in CI), five API tests (dependents survive a target edit; promoted token immediately carries the new role; delegated user-admin cannot promote to admin; limit falls back to the clamp, never to "everything"). No schema change, no new env var |
| 2026-07-27 | **Phase 43 (console: the two human decision points).** Portal-only. Phase 25's parity drifted over Phases 27–42 until nine capabilities had no screen; two of them are human decisions **with a deadline** (a parked agent tool call blocks the agent; a paused statement is refused when `PAM_DB_STEPUP_TTL_SEC` expires), so those ship first. `internal/web/static/index.html` gains menu **20 — Approve AI-agent tool calls** (`GET /v1/approvals`, `POST /v1/approvals/{id}/decision`; renders call id, agent, on-behalf-of, rule, waiting time and the **arguments**, since the arguments are what policy matched on; 404 → "broker disabled" hint like the app-secrets screen) and menu **21 — In-session step-up decisions** (`GET /api/sessions/stepups`, `POST /api/sessions/{id}/stepup`; listing needs `read_audit`, deciding needs `approve` per Phase 39, and the screen says the entries expire and the session survives either decision). Verified against a running in-memory server: an agent minted, a real `list_targets` call parked by a `require_approval` rule, the exact GET the screen issues returning every rendered field, the decision POST returning 200, the list emptying, plus the broker-disabled 404 — and `node --check` on the embedded script. No new routes, no schema, no new env var. The other seven screens are planned Phase 45 |
| 2026-07-27 | **Phase 42 (shared custody of the host and CA keys).** Both long-lived SSH keys were persisted to a LOCAL FILE by `proxy.LoadOrCreateHostKey` / `sshca.LoadOrCreate`, so every replica generated its own: host-key-changed warnings that look like a MITM, certificates minted by one pod rejected by targets trusting another pod's CA, and the operator-cert challenge (an HMAC keyed off the CA private key, documented HA-safe) failing across pods. New **`internal/keycustody`** owns the lifecycle: read the file if present as the *candidate* (so an existing single node seeds custody and does not rotate its own key), else generate; seal it with `vault.Encrypt` under `store.KeyMaterialAAD(name)`; offer it to the new atomic `store.EnsureKeyMaterial(name, value)` (migration `0022` `key_material`; pgstore does `INSERT … ON CONFLICT DO NOTHING` then reads back, memstore does the same under its lock) which returns whatever is stored, so racing replicas converge on one key; unwrap it; and mirror the authoritative key back to the configured path. An unwrappable key is **fatal** (a silent regeneration would break every target that pinned the key) and the error names the likely cause. `proxy` gains `GenerateHostKeyPEM`/`HostKeyFromPEM` and `sshca` gains `GenerateKeyPEM`/`FromPEM` so the crypto stays in those packages while custody lives in one place; `main` logs when a replica adopts the cluster's key. Tests: `keycustody` (eight concurrent replicas converge, file seeds + second replica adopts and mirrors, store holds no usable key material and the envelope refuses the other name's AAD, unwrap failure is fatal, generator failure propagates) and the store contract — which races six claimants against live PostgreSQL in CI. Recordings stay per-replica: shared recording storage is an operator decision, not a code fix |
| 2026-07-27 | **Phase 41 (session recordings encrypted at rest).** The vault wrapped every credential; the *recording* of the session — everything the operator typed and saw — was written in the clear and protected by file permissions alone. New **`internal/recording`** seals it as a header line carrying the vault-wrapped per-recording data key followed by AES-256-GCM chunks (`NewSealer`, `Open`, `IsSealed`). Chunked rather than one blob because a killed, capped or crashed session must still decrypt up to its last complete chunk; each chunk's AAD is `name|index`, so chunks cannot be reordered, dropped from the middle or spliced in from another recording, and the wrapped key's AAD binds it to its own recording. The data key is wrapped by the configured KEK (local / Transit / KMS / PKCS#11) through a two-method `KeyWrapper` view of `*vault.Vault`. **Hashing moved to the stored bytes**: `newRecording` now hashes the sealer's *output* (`io.MultiWriter(f, hasher)` sits below the sealer, not above), and `recordWinRM` hashes the file it writes instead of the transcript string — the two were identical while unencrypted and would have silently diverged once sealed, making every sealed recording replay as "never audited". Playback (`playRecording`) sniffs the magic prefix per file and streams the decryption, so pre-encryption recordings still replay; it sets `X-PAM-Recording-Encrypted`. A KEK failure returns an error and removes the file, so a session is never silently recorded in the clear. New config `PAM_RECORDING_ENCRYPT` (default off — it changes the on-disk format, so a `.cast` can no longer be fed straight to asciinema). Threaded as `EncryptRecordings` through `proxy.Config`, `proxy.DBConfig` and `api.Options`. Documented limit: the file NAME still carries target and actor. Tests: the package (round-trip, tamper, splice, truncation, loud KEK failure), `proxy.TestSessionRecordingIsSealedOnDisk`, and an API end-to-end proving nothing readable hits disk yet playback returns the transcript with `X-PAM-Recording-Audited: true`. No schema change |
| 2026-07-27 | **Phase 40 (every brokered execution is a supervised session).** The SSH proxy, DB proxy and RDP tunnel registered their sessions; `POST /api/targets/{id}/winrm` and the agent broker's `winrm_exec`/`ssh_exec` tools did not — so those runs were absent from `GET /api/sessions`, unkillable, uncounted against `PAM_MAX_SESSIONS_PER_USER`/`_TOTAL`, and unreachable by the analytics auto-response and the vendor sweeper (both kill **by actor**). New `Server.superviseSession(ctx, actor, target, protocol, remote)` (`internal/api/credentials.go`) does the whole dance in one place: `Registry.AllowNew` → `errSessionLimit` (HTTP 429 / a failed tool call), otherwise `Registry.Register` with a cancel func, returning a kill-cancellable context plus a release that removes the registration. All three call sites use it, and the cap runs **before** the JIT decrypt everywhere — `sshExecTool` had its `vault.Decrypt` moved below the supervise call so a refused run never materializes a secret. A cancelled run answers **503 session terminated** (audited `session.killed`) instead of a 502 that would blame the target. `session.Info.Protocol` now documents `winrm`/`postgres`/`ssh_exec` alongside ssh/rdp. Tests: an in-flight run parked in a blocking runner is listed as protocol `winrm`, a second concurrent run is refused 429, `DELETE /api/sessions/{id}` terminates it (503) and the registry empties, with JIT injection unchanged. No schema change, no new env var |
| 2026-07-26 | **Phase 39 (approver capability on the two decision points).** Two endpoints that authorize something were gated on the wrong capability. **Step-up release** — `POST /api/sessions/{id}/stepup` moves from `CapReadAudit` to **`CapApprove`**: pausing a flagged statement for a supervisor only means something if releasing it is privileged, and as shipped the read-only `auditor` role could do it. `GET /api/sessions/stepups` keeps `CapReadAudit` (the live-monitor gate) so a watching supervisor still sees what is paused. **Certification decisions** — `POST /api/campaigns/{id}/items/{iid}/decision` moves from `CapManageUsers` to **`CapApprove`**: recertifying access is a review, not user administration, and requiring the user-management capability made the principal who grants access the only one who could attest to it. Campaign create/close stay `CapManageUsers`; reads stay `CapReadAudit`. No capability was added to the matrix and no schema changed. Honest limit: this delegates the review but does not stop an admin (who holds `CapApprove`) certifying their own grant — per-item four-eyes needs the grant's creator recorded, a documented follow-on. Tests: `api.TestStepUpEndpoints` (auditor lists but is refused the release; an approver's decision resolves the waiting statement) and `api.TestCertificationAuthz` (approver decides but cannot create; auditor and plain user cannot decide) |
| 2026-07-26 | **Phase 38 (command control on every command path).** Phase 16 described its guard as covering "every path where a discrete command is visible", but `CommandGuard` lived in `internal/proxy` and was only consulted there — so `POST /api/targets/{id}/winrm` and the agent broker's `ssh_exec`/`winrm_exec` tools, which each take a command string, ran unguarded. A pattern that blocked an operator's `ssh target "cmd"` did nothing to an AI agent, the least-trusted actor. The guard moved to a new **`internal/cmdguard`** package (`Guard`, `New`, `ParseDeny`, `Blocked`, `Size` — same logic, tests moved with it); `proxy.Config.CommandGuard`/`DBConfig.CommandGuard`/`StepUpGuard` now hold `*cmdguard.Guard`, and `main` compiles the deny file **once** into one guard shared by the SSH proxy, the DB proxy and the API server (new `api.Options.CommandGuard`). Enforcement lands in `Server.guardCommand`, called from `execWinRM` — the single chokepoint the REST endpoint and `winrm_exec` share — and from `sshExecTool.Execute`, both **before** the JIT decrypt, so a refused command never causes a secret to exist in memory. A block audits `command.blocked` with the matched pattern (same vocabulary as the proxies) and surfaces as HTTP 403 on REST / a failed tool call to an agent. Tests: a blocked WinRM run is refused, never reaches the fake runner, never decrypts, and is audited (an unmatched command still runs); the same for the broker's `winrm_exec`; and `ssh_exec` against an unroutable host returns the policy refusal rather than a dial error, proving the guard runs before the dial. No schema change, no new env var |
| 2026-07-26 | **Phase 37 (gap-analysis pass: child-resource scoping + bearer auth-failure handling).** **Authorization scoping** — `deleteSafeMember` authorized the safe in the path but then deleted by the (global) member id, so a delegated `can_manage` member of one safe could remove a member of **any** safe; it now requires the member to belong to that safe (404 otherwise). `deleteDependency` ignored the credential in its path entirely; it is now scoped the same way and the audit detail names the owning credential (`credential:%d dependency:%d`). Both now match the pattern `deleteTargetGrant`/`deleteAppSecretGrant` already used. **Auth-failure parity** — until now only the login endpoints and the two session proxies throttled *and* audited a failed authentication; a wrong `X-API-Key`, agent bearer key or application bearer key was merely logged, so token guessing against `/api/*`, `/v1/tool-calls` and `/v1/app-secrets` was an unthrottled online oracle invisible to the risk engine and the SIEM forwarder (both read the audit trail). New `Server.authFailed` (`internal/api/middleware.go`) handles all three surfaces uniformly: a per-source-IP **failure** limiter (`keyFailLimiter`, its own window on the `PAM_AUTH_RATE_LIMIT` budget — separate from `authLimiter` because a legitimate API client makes many *successful* calls a minute) returns 429 past the budget, and each admitted failure appends **`api.auth_failed`** (`surface:api|agent|app`, method, path, client IP — never the presented credential). The audit append is deliberately skipped once throttled, bounding audit writes under a flood to the limiter's budget per IP. `analytics.authFailActions` now scores `api.auth_failed`. Tests: cross-safe member delete refused (victim survives) + positive control, cross-credential dependency delete refused, and 401×N-then-429-with-N-audited on each of the three bearer surfaces. No schema change, no new env var |
| 2026-07-25 | **Phase 36 (retention / pruning).** Two data stores grew without bound: session-recording files on disk and audit rows in Postgres. New leader-locked retention worker (`internal/api/retention.go` `RunRetentionWorker`/`retentionPass`, `PAM_RETENTION_INTERVAL_HOURS`). **Recordings** — `maint.PruneRecordings(dir, cutoff)` deletes `.cast`/`.winrm.log` files older than `PAM_RECORDING_RETENTION_DAYS`, **preserving dotfiles** (notably the `.chain` head that anchors the recordings' hash chain) and any non-recording file, so a sweep can't corrupt the chain or touch an unrelated file (audited `recording.pruned`). **Audit rows** — `store.PruneAuditBefore(cutoff)` deletes events older than `PAM_AUDIT_RETENTION_DAYS` (audited `audit.pruned`), but **only when the tamper-evident HMAC chain is off**: deleting the chain head breaks `VerifyAuditChain` (which requires the first row's `prev_hash` to be nil), so with the chain on the worker **skips audit pruning with a loud warning** rather than silently trading integrity for space (the correct pattern there is a WORM export + manual re-anchor). New store method `PruneAuditBefore` (contract-tested: future cutoff prunes all, past cutoff prunes none). Config `PAM_RECORDING_RETENTION_DAYS`/`PAM_AUDIT_RETENTION_DAYS` (0 = keep forever) validated fail-loud. Tests: `PruneRecordings` (old removed; `.chain`, recent, and non-recordings kept), the store contract, and the worker pass (recordings pruned + audited, audit pruned when unchained, **skipped when chained**). No schema change |
| 2026-07-25 | **Phase 35 (audit→SIEM push forwarding).** Where `internal/alert` fires on a *specific* event and `GET /api/audit/ocsf` is pull-based, this is a **continuous push** of the whole primary audit trail. New `internal/auditfwd` — a `Forwarder` that tails `audit_events` from a **durable cursor** (`store.AuditSince(afterID, limit)`; the last-forwarded id persisted as the `_audit_forward_cursor` setting) and emits each event as an **RFC 5424 syslog** message (PRI 110 = log-audit facility · info) or an **ArcSight CEF** record to a UDP/TCP collector. The cursor advances only after an event is written, so a transport failure re-sends from the last success next tick (**spool-and-retry**); on first enable it starts from the current head (no history replay). The pass runs under the **Postgres leader lock** so N replicas don't duplicate SIEM entries. Actor/action/detail are CR/LF-sanitized (and CEF metacharacters escaped) so a directory-supplied name can't forge a record. Config `PAM_AUDIT_FORWARD_ADDR` (host:port; empty = off), `PAM_AUDIT_FORWARD_PROTO` (udp/tcp), `PAM_AUDIT_FORWARD_FORMAT` (rfc5424/cef), `PAM_AUDIT_FORWARD_INTERVAL_SEC` — all validated fail-loud when the forwarder is on. New store method `AuditSince` (contract-tested). Tests: a real in-process UDP syslog sink receives forwarded RFC 5424 + CEF messages, the cursor advances (no re-send), and a "restarted" forwarder resumes from the persisted cursor. No schema change |
| 2026-07-25 | **Phase 34 (HA live-session kill-switch).** The live-session registry is per-replica (each pod holds the cancel funcs for the sessions it hosts), so a kill issued to one pod could not terminate a session pinned to another — the kill-switch, revoke cascade, vendor offboard and analytics auto-response were all replica-local in HA. New **cross-replica kill bus** (`internal/session/killbus.go` `KillBus`/`KillSelector`): a kill published on any replica is delivered to every replica's subscriber, which applies it to its own registry. Store transport: **Postgres `LISTEN`/`NOTIFY`** (`pgstore/killbus.go`, a hijacked dedicated connection that reconnects on failure) and an **in-process fan-out hub** for the memory store (`memstore/killbus.go`) so the demo/tests exercise the same registry code. `Registry.Kill`/`KillByActor`/`KillByActorTarget` now broadcast (a private `killLocal*` variant applies an inbound kill without re-publishing, so it can't loop); `KillDistributed` reports `KillLocal`/`KillDispatched`/`KillNotFound`, and `DELETE /api/sessions/{id}` returns **202 Accepted** when the kill was broadcast to the cluster (session not on this replica) vs 204 when killed locally. `main` calls `Registry.StartKillBus(ctx, store)` at startup. New store methods `PublishSessionKill`/`SubscribeSessionKills` (contract-tested round-trip; pgstore exercises real `NOTIFY` in CI). **Live monitoring (the SSE stream) stays per-replica** — a documented limitation; the security-critical action, termination, is cluster-wide. Tests: two registries sharing one store prove a kill on "replica B" terminates a session on "replica A" (by id, actor, actor+target). No schema change |
| 2026-07-25 | **Phase 33 (RDP clipboard control).** The in-portal RDP viewer relies on Apache Guacamole, which leaves the clipboard bridge **on in both directions by default** — so an operator could copy data out of (or paste into) a recorded RDP session with no gate and no audit, the RDP analog of the Phase-32 SFTP gap. New `PAM_RDP_CLIPBOARD` policy (`internal/api/rdp_handlers.go` `rdpClipboardParams`, threaded into the guacd `connect` via `rdpExtra`): **`allow`** (default) leaves copy + paste on; **`readonly`** sets `disable-paste=true` (no clipboard *injection* into the target — the "target is read-only" stance, mirroring SFTP read-only) while copy-out stays on; **`deny`** sets `disable-copy=true`+`disable-paste=true` (clipboard off both ways). Every mode also forces `enable-drive=false`, so **drive redirection can't be used to exfiltrate files** regardless of guacd's defaults. The chosen mode rides the `rdp.connect` audit detail (`clipboard:<mode>`). Config `PAM_RDP_CLIPBOARD` ∈ {allow, readonly, deny} (fail-loud on a typo). Tests: the mode→parameter mapping (allow/readonly/deny/unset), that drive redirection is always disabled, and an **end-to-end** assertion that a `deny` policy reaches a fake guacd as `disable-copy=true`/`disable-paste=true`/`enable-drive=false`, positioned by the advertised arg order. No new audit vocab, no schema change |
| 2026-07-25 | **Phase 32 (SFTP file-transfer control + audit).** SFTP is not a separate protocol — it rides an SSH `subsystem` channel whose payload is the binary SFTP protocol ([draft-ietf-secsh-filexfer-02](https://datatracker.ietf.org/doc/html/draft-ietf-secsh-filexfer-02), the v3 dialect OpenSSH speaks). Before this, that stream flowed through the proxy **opaque**: an operator could upload or exfiltrate files with no audit trail, no policy gate, and no deny knob (the bytes only garbled the recording). New `internal/proxy/sftpguard.go` — an `sftpInspector` sits on the operator→target leg (via a new `pumpRequests` `onSubsystem` hook + a per-session `pump` that replaces the raw `io.Copy`), frames each SFTP request packet directly (no third-party SFTP dependency), and per `PAM_SSH_SFTP` policy: **`allow`** (default) forwards but now **audits every operation** (`sftp.session`, `sftp.open` with read/write intent, `sftp.modify` for remove/rename/mkdir/rmdir/setstat/symlink); **`readonly`** refuses any mutating op by **synthesizing an `SSH_FXP_STATUS` permission-denied** back to the client (`sftp.blocked`) so the target is never contacted, while reads still flow; **`deny`** refuses the subsystem outright (`sftp.denied`). Fail-open on forwarding but loud on auditing — an unframable stream is passed through and flagged `sftp.parse_error`, never silently dropped; a non-sftp subsystem is audited `session.subsystem` (or refused under `deny`). Config `PAM_SSH_SFTP` ∈ {allow, readonly, deny} (fail-loud on a typo). Tests: a **real minimal SFTP client + server** exchange genuine v3 packets through the proxy — allow forwards an upload to the target and audits it, readonly returns permission-denied for an upload + a delete (neither reaches the target) yet a download succeeds, deny refuses the subsystem — plus the config enum. New audit vocab `sftp.*` + `session.subsystem`. No schema change |
| 2026-07-25 | **Phases 27–31 review fixes (post-implementation code review).** **Auth SoD** — `Principal.ApproverGroups` now returns role names only (drops the principal's own name), so a delegated `manage_users` can't mint a user named after an `approvers:` group to self-approve a broker call. **Vendor per-account** — `store.VendorSessionAllowed` gained an `account` arg and enforces the grant's `principal` (a grant scoped to one login account no longer admits a session on another; `""` = any-account for the sweeper, which can't know the session's account); threaded through every connect gate. **DB step-up covers the extended protocol** — a matching `Parse` (prepared statement) now pauses/refuses too (`stepUpRefused(…, extended)` → FATAL, fail-closed), so a client can't dodge the supervisor pause via the extended query protocol; each relayed statement runs under a per-connection cancelable context. **Auditchain HA** — the periodic checkpoint anchors the store-read head *inside* the linked append (not the triggering event's stale head) so it signs the true head under concurrent writers; `VerifyFloor` compares the min-entries floor against the max event id (not the returned slice length) so tail-truncation is still caught. **Step-up race** — map-removal-under-lock is the single atomic claim point (a decision and a timeout can't both fire an audit). **Broker** — `Resume` is bound to its call id (a resume token can't be spent on another call's path, and a mismatch doesn't burn it), `Withdraw` matches the requester by KeyID, and the parked-call sweep records a terminal `broker.tool_call.failed` outcome + chains it. **Blast** — the API reads the body under the 4 MiB graph cap directly and caps principals/edges, and 422s a focused query for a principal absent from the graph; the IAM evaluator scans *all* deny statements (an unconditional Deny wins over an earlier conditional one; a conditional-only deny is UNCERTAIN), matches actions case-insensitively but resources case-sensitively, and graph reachability prefers a certain (all-unconditional) path over a shorter conditional one. **SSH CA** — the managed-principal check runs before `gateCredentialAccess`, so a refused issue no longer burns a one-time approval. Each fix carries a regression test. No schema change |
| 2026-07-25 | **Phase 31 (identity blast-radius / CIEM engine).** New `internal/blast` — a read-only posture engine over a **normalized, provider-agnostic identity graph** (an external ingester's job; live cloud ingestion stays out of scope). `iam.go` is a real **AWS IAM effective-permission evaluator** implementing the true evaluation order (explicit Deny anywhere wins → SCP ceiling → permission-boundary ceiling → identity Allow → implicit deny), with `*`/`?` wildcard matching (iterative glob, no regex backtracking) and a condition it cannot evaluate modeled as **UNCERTAIN** rather than a guess. `blast.go` is the graph: **pivot** edges (`can_assume`/`member_of`/`can_escalate_to`/`credential_for`) expand reach, **containment** edges (`contains`/`reads`) do not; `BlastRadius` (BFS), `WhoCanReach` (reverse), toxic-combination `Findings` (a non-admin reaching an effective-admin = escalation; a cross-provider pivot = lateral movement; severity derived, conditional paths flagged uncertain), and `Remediation` cutting the **earliest** pivot edge (needs-review when uncertain). API `POST /api/blast/analyze` (`CapReadAudit`) analyzes a submitted graph (4 MiB cap) → findings + summary + optional `source` blast-radius / `target` who-can-reach; audited `blast.analyze`. Tests: the evaluator (deny/SCP/boundary/wildcard/uncertain), traversal + who-can-reach (pivot vs read edges), findings + earliest-cut remediation over a canonical cross-provider Drift-shaped chain, malformed-graph rejection, and an API test. This opens the Tier-3 cloud-CIEM gap honestly — the engine is complete and tested; only live ingestion is external. No schema change |
| 2026-07-25 | **Phase 30 (in-session policy + step-up).** **Numeric policy comparators** — `internal/policy` conditions gain `gte`/`gt`/`lte`/`lt` (numeric compare of an argument value, fail-closed on absent/non-numeric), so a broker rule can gate on an amount (`args.amount: { gte: 5000 } → require_approval`); load-time validation still enforces exactly-one-operator and rejects an unknown operator fail-loud. **DB in-session step-up** — a `PAM_DB_STEPUP_FILE` regex list marks sensitive PostgreSQL statements; `DBProxy.stepUpRefused` pauses the matched statement (audited `db.stepup_required`, surfaced on the live hub) and blocks on a supervisor's decision via a new **`session.StepUp`** coordinator (shared with the API, like the live `Hub`) — an approval runs the statement (`db.stepup_approved`), a denial or `PAM_DB_STEPUP_TTL_SEC` timeout refuses it (`db.stepup_denied`) while the **session stays open**. Supervisor endpoints `GET /api/sessions/stepups` + `POST /api/sessions/{id}/stepup` (`CapReadAudit`, the live-monitor gate; audit `session.stepup_decided`). Tests: policy comparators (amount gating + fail-loud load), the `session.StepUp` registry (approve/deny/timeout/concurrency), a DB-proxy end-to-end (approve runs & reaches upstream, deny refused & absent, session survives), and the API endpoints. No schema change |
| 2026-07-25 | **Phase 29 (third-party vendor access gate).** A `vendor` (new `internal/vendor` + migration `0021` `vendors`/`vendor_grants`) is an external identity (a `user`-role login linked by username) whose access to a target is bounded by a **customer-approved, time-boxed contract grant**. New store methods (`CreateVendor`, `GetVendorByUsername`, `CreateVendorGrant`, `ApproveVendorGrant`, `RevokeVendorGrant`, `OffboardVendor`, `VendorSessionAllowed`, …). **Connect gate** — every connect path (SSH proxy, DB proxy, RDP, WinRM run, and `gateCredentialAccess` for reveal/checkout) now calls `store.VendorSessionAllowed`: a vendor is admitted only while an approved, unrevoked grant with `now ∈ [not_before, not_after]` exists; non-vendors are unaffected. **Approval** — `POST /api/vendor-grants/{gid}/approve` (`CapApprove`) enforces four-eyes (a vendor can't approve their own grant) and runs the **live employment attestation** webhook (`internal/vendor.Attestor`, `PAM_VENDOR_ATTEST_URL`). **Offboard cascade** — `POST /api/vendors/{id}/offboard` disables the vendor, revokes all grants atomically, and kills their live sessions (`KillByActor`); persisted so a restart keeps them out. **Session sweeper** — `RunVendorSweeper` (`PAM_VENDOR_SWEEP_INTERVAL_MIN`) cuts a vendor's session once its grant window closes (`KillByActorTarget`, `vendor.session_expired`). **Evidence** — `GET /api/vendors/{id}/evidence` (`CapReadAudit`) bundles the vendor's grants + audit slice with a SHA-256 digest for SOC 2/DORA. Routes `POST/GET /api/vendors[/{id}/{offboard,grants,evidence}]`, `POST /api/vendor-grants/{gid}/{approve,revoke}`. Audit vocab `vendor.*`. Tests: store contract, `vendor` attestation, an API lifecycle (mint → grant → approve → WinRM admitted → offboard blocks → evidence), and an SSH-proxy gate test |
| 2026-07-24 | **Phase 28 (operator-issued SSH certificates).** Extends the Phase-22 CA (`internal/sshca`) from proxy-internal minting to an **issuance API for an operator's own key**: `POST /api/ca/ssh/challenge` mints a stateless proof-of-possession challenge (HMAC keyed from the CA private key, so it is HA-safe and unforgeable); `POST /api/ca/ssh/sign` (`CapConnect`) verifies the operator signed it with the presented key, applies the **same connect authorization** as the proxy (`gateCredentialAccess` — per-target grants + the approval gate, consuming a one-time approval), requires the principal to be a **managed account** on the target, then `sshca.IssueForKey` signs a short-lived cert scoped by principal + `source-address` critical option (capped at `PAM_SSH_OPERATOR_CERT_TTL_MIN`, default 10m). Issued certs are recorded (migration `0020` `ssh_certificates`; serial is the revocation handle, returned as a **string** so a large uint64 survives JSON float parsing). `POST /api/ca/ssh/revoke` (`CapManageTargets`) stamps a serial revoked; `GET /api/ca/ssh/krl` (`CapReadInventory`) emits a real **OpenSSH KRL** (`sshca.KRL`, PROTOCOL.krl serial-list) for a target's `RevokedKeys`. New store methods `RecordSSHCert`/`RevokeSSHCert`/`ListRevokedSSHCertSerials`/`ListSSHCerts`; audit `ssh.cert_issued`/`ssh.cert_denied`/`ssh.cert_revoked`. Tests: `sshca` (scoped issue, challenge round-trip, **KRL verified against real `ssh-keygen -Q`**), store contract (record/revoke/list, conflict/not-found), and an API end-to-end (challenge → sign → CA-checker accepts for the principal → revoke → KRL served; bad PoP, unmanaged principal, and a non-connect role all refused) |
| 2026-07-24 | **Phase 27 (broker completion — Solution-01 parity).** **Separation of duties** — `require_approval` rules' `approvers:` groups are now enforced at decision time: `broker.Decide` takes an `Approver{Name,Groups,IsAdmin}` (groups from `auth.Principal.ApproverGroups`) and refuses a decider outside the rule's group (`broker.approval.refused`, HTTP 403; the call stays parked); admins bypass; four-eyes unchanged. **Audit chain** (`internal/auditchain`) — periodic ed25519 **in-chain checkpoints** (`WithCheckpointEvery`, `PAM_BROKER_AUDIT_CHECKPOINT_EVERY`; `broker.audit.checkpoint` events sign the running head, caught by `VerifyFloor`'s `bad_checkpoint` even under HMAC-key compromise), **signing-key rotation** (`WithRotation` + `PAM_BROKER_AUDIT_SIGN_PREV`; trusted signers published as JWKS at `GET /v1/audit/jwks`), and a **min-entries truncation floor** (`GET /v1/audit/verify?min_entries=N`, `VerifyResult`). **OCSF export** — new `internal/ocsf` maps the audit trail to OCSF (API Activity 6003 / Detection Finding 2004) at `GET /api/audit/ocsf` (`?format=ndjson`); audited `audit.ocsf_export`. **MCP SSE + elicitation** — `GET /mcp` SSE transport (`internal/api/mcp_sse.go`, session registry, `endpoint` event + heartbeats); `initialize` advertises `elicitation`/`logging`; an approval-gated `tools/call` from an elicitation-capable client prompts the running user (`elicitation/create` over SSE) — a decline **withdraws the requester's own** parked call (`broker.Withdraw`, `broker.tool_call.withdrawn`), an accept only records intent (four-eyes preserved). New doc [AGENT-THREAT-MODEL.md](AGENT-THREAT-MODEL.md) (OWASP LLM Top 10 / MITRE ATLAS → controls). No schema change |
| 2026-07-24 | **Phase 26 (session-recording playback + one-time access).** **Playback** — `GET /api/recordings` lists the on-disk recordings (`.cast` asciicasts + `.winrm.log` transcripts, dotfiles like the `.chain` head excluded by a filename allowlist that also forecloses traversal) and `GET /api/recordings/{name}` (both `CapReadAudit`) serves one for replay: the file's SHA-256 is recomputed and checked against the audit trail (new `store.FindAuditDetail`, matching the `sha256:` stamped by `session.record`/`winrm.run`; LIKE wildcards escaped in pgstore), the verdict travels in `X-PAM-Recording-Audited`, and the replay is audited `session.playback`; hash and body cover the same byte range (`io.NewSectionReader`), so a still-recording file replays as a consistent prefix. Portal menu 19: subfile + keyboard-first player (Space pause, F5 restart, F6 speed 1x–8x/MAX, 2s idle cap, bounded pane). **One-time access** — migration `0019` (`access_requests.one_time`, `consumed_at`); new `store.ConsumeApproval` burns a single-use approval atomically (`UPDATE … FOR UPDATE SKIP LOCKED RETURNING`; a standing approval is preferred and never burned; `HasActiveApproval` now excludes consumed ones) and every privileged-use gate consumes: SSH proxy, DB proxy, RDP tunnel, and `api.enforceApproval` (reveal, checkout, WinRM run, broker tools) — audited `access.consumed`; a failed dial after the gate still consumes (fail-closed). `POST /api/access-requests` takes `one_time`; `PAM_ACCESS_ONE_TIME` forces it. Tests: store contract (round-trip, burn-once, standing preference, 8-way race admits exactly one), SSH + DB proxy e2e (one connection admitted, second refused pre-upstream), API checkout burn + forced mode, playback listing/verification/RBAC/name-hygiene |
| 2026-07-24 | **Phase 25 (console parity):** 5250 screens for every API-only capability — *Work with safes* (menu 16: safes CRUD, member management with `can_manage`, target assignment via *Work with Targets* option 8 + a Safe column), *Certification campaigns* (menu 17: snapshot with optional due date, certify/revoke item review, close), *Risk analytics* (menu 18: per-actor score/level/signals with min-level + window filters), a **live session viewer** (*Active Sessions* option 5: the Phase 16 SSE stream read with `fetch` — EventSource cannot send `X-API-Key` — ANSI-stripped into a bounded scrollback pane), and the Phase 20/21 fields on access requests (ticket, requested N-of-M approvals, `not_before`/`not_after` window; the approver list shows ticket + `n/m` progress). Portal-only — no new routes, schema, or audit vocabulary |
| 2026-07-23 | **RDP viewer review fixes (portal + caching).** The viewer now tears down on a clean/kill tunnel close (`tunnel.onstatechange==CLOSED`), not only on error, so an admin-killed session no longer freezes the overlay; error messages survive teardown (passed into it, not clobbered by a generic "CLOSED"); the Guacamole client module is imported **on demand** (retryable, and never loaded for non-RDP roles) from a **content-addressed** path (`/static/guacamole-common.min.js?v=<hash>` — Index substitutes `__GUAC_SRC__`), so the `immutable` cache is now sound and an upgrade reaches returning browsers; Keyboard/Mouse bind to the removed-on-teardown overlay (no leaked document listeners), and Ctrl+Alt+Q is swallowed inside `onkeydown` so the chord never reaches the target. `docs/SECURITY-GAPS.md` no longer lists the viewer as deferred |
| 2026-07-23 | **RDP viewer review fixes (server side).** The tunnel bridge now forwards **one whole Guacamole instruction per WebSocket message** (new `guacd.Conn.NextInstruction`) instead of raw 8 KB byte chunks — the old copy split large screen paints and the vendored client rejected the partial message, so the viewer broke on the first real paint. `POST /api/rdp-token` now mints an **RDP-tunnel-scoped** token (`auth.SessionScopeRDP` → `Principal.TunnelOnly`, refused by `authz`/`authenticated`) so a token leaked from the WS URL can't hit other endpoints or re-mint; break-glass callers keep a break-glass-scoped token so the loud audit + approval bypass still fire. `rdpTunnel` now enforces `session.Registry.AllowNew` (the `PAM_MAX_SESSIONS_*` caps) before decrypting, like the SSH/DB proxies. Tests extended: a >8 KB instruction must arrive as one frame, and an RDP token is 403 on `/api/targets`, `/api/me`, and re-mint |
| 2026-07-23 | **In-portal RDP viewer + a latent WebSocket-upgrade fix.** The `web` package now embeds and serves the vendored Apache Guacamole JS client (`GET /static/guacamole-common.min.js`, unmodified ESM build, Apache-2.0 — see repo `NOTICE`), and the portal renders RDP on a canvas (*Work with Targets* option 7; `Ctrl+Alt+Q` to disconnect). New `POST /api/rdp-token` mints a 60s session token (`issueSessionTTL`) so the viewer keeps the operator's long-lived key out of the WS URL (audited `rdp.token`, new vocab). The tunnel handler now emits the tunnel prelude guacamole-common-js needs (`guacamolePrelude`: an internal tunnel-UUID instruction, then a re-emitted `ready`). Portal CSP widened for the canvas: `img-src 'self' data: blob:`, `media-src …`, `script-src … 'self'` (for the same-origin dynamic `import()`). **Bug fixed while testing:** the access-log `statusWriter` did not forward `http.Hijacker`, so *every* WebSocket upgrade (including the pre-existing RDP tunnel) failed with 501 through the middleware — added `statusWriter.Hijack`. New tests: prelude wire-format, `TestRDPTunnelEndToEnd` (full WS round-trip vs a fake guacd), token gating. Verification runbook: `docs/RDP-TESTING.md` |
| 2026-07-23 | **Resource-exhaustion limits.** Concurrent-session caps — `PAM_MAX_SESSIONS_PER_USER` / `PAM_MAX_SESSIONS_TOTAL` (0 = unlimited) enforced by `session.Registry.AllowNew` before either proxy decrypts a secret (per-replica). Per-recording size cap — `PAM_MAX_RECORDING_MB` (0 = unlimited); `Recording.Write` returns `errRecordingLimit` once exceeded, so the session is terminated (`session.record_limit`) rather than run unrecorded. Refused sessions audit `session.denied reason:session-limit` (SSH) / `db.session.denied` (DB). Registry + recording unit tests |
| 2026-07-23 | Doc-quality pass: §1 package tree completed (added `ratelimit`, `conjur`, `ticket`, `auditchain`, the broker cluster `agentid`/`policy`/`broker`/`mcp`, `storetest`); §2.2 refreshed to the current migration high-water mark (`0018`) and full table list + advisory-lock keys; header currency |
| 2026-07-23 | **Kill in-flight sessions on revocation + break-glass share-check fix.** Revoking a login (`POST /api/login-sessions/revoke`), disabling a directory user (reconcile), or deleting a user's target grant now terminates their matching live proxied sessions via the session registry (`KillByActor` / new `KillByActorTarget`; role-grant deletions still only affect new connections, and the registry is per-replica). New audit action `session.killed`. Also fixed a real bug found while testing: the break-glass quorum share collector checked the *first* byte as the Shamir x-coordinate, but `shamir.Split`/`Combine` put it in the *last* byte — this spuriously rejected ~1/256 of valid share sets (flaky unseal) and weakened poison detection |
| 2026-07-23 | **Signed audit checkpoints (tail-truncation detection).** The primary-audit HMAC chain catches edits/reorders/mid-deletions but not tail truncation. Added `GET /api/audit/head` (`CapReadAudit`), which returns an ed25519-signed checkpoint over `(last_id, head)` — an auditor stores it and later detects truncation, mirroring the broker chain. New `PAM_AUDIT_SIGN_SEED` (base64 32-byte ed25519 seed; requires `PAM_AUDIT_HMAC_KEY`, fail-loud). New store method `GetAuditHead`; the signing helper `auditchain.SignCheckpoint` is now shared by the broker and primary chains. 501 when unconfigured |
| 2026-07-23 | **Security hygiene: dependency refresh + gosec gate.** Bumped outdated dependencies (AWS SDK point releases, go-logr; `go mod verify` clean, govulncheck still 0 reachable). Added `gosec` as a CI gate (`-confidence high -exclude=G104,G115,G304,G306,G101` — the pervasively-deliberate/noisy rule categories); the remaining deliberate findings (protocol-mandated md5/sha1, the documented trust-any host-key defaults, the opt-in-verify TLS fallbacks, the loopback healthcheck probe, the conditionally-Secure OIDC cookie, operator-config file reads) are recorded inline with `#nosec Gxxx -- reason`. No behavior change |
| 2026-07-23 | **Tamper-evident PRIMARY audit trail (opt-in).** The keyed-HMAC chain now covers the main `audit_events` table, not just broker events. Additive migration `0018` adds nullable `prev_hash`/`hmac`; when `PAM_AUDIT_HMAC_KEY` (base64 32 bytes) is set, `AppendAudit` links each event `hmac = HMAC-SHA256(key, prev_hash ‖ canonical(actor,action,detail))` in the same transaction under an advisory lock (`pam_audc`), so editing/reordering/deleting any event is detectable. `store.EnableAuditChain`/`VerifyAuditChain` + `GET /api/audit/verify` (`CapReadAudit`, 501 when disabled). Unset = today's plain table (non-breaking). Contract + tamper tests (memstore in-package, live-Postgres via `UPDATE`) |
| 2026-07-22 | **Rate-limiter de-dup + invariants-as-tests.** Extracted the two near-identical fixed-window limiters (API auth endpoints, SSH/DB proxies, broker per-agent) into one shared `internal/ratelimit` package (`ratelimit.New`/`Limiter.Allow`, injectable clock); all three now share tested logic. Encoded security invariant §6.1/§6.7 as executable tests (`internal/store/invariants_test.go`): a reflection guard that every secret-bearing field (`Credential.SecretEnc`, the four `TokenHash`es, `MFAEnrollment.SecretEnc`/`LastTOTPStep`, `BrokerToken.JTI`, `BrokerAuditEvent.PrevHash`) is `json:"-"`, plus a behavioral marshal test that secret material never appears in JSON. No behavior change |
| 2026-07-22 | **HA leader election for the background workers.** New `store.WithLeaderLock(ctx, key, fn)` runs a periodic job only if it can take a non-blocking Postgres advisory lock (`pg_try_advisory_lock`, held on a dedicated pooled connection for the pass), so across replicas exactly one runs the credential-lifecycle (rotation/reconcile) and threat-analytics passes per tick — no more N replicas rotating the same credential or firing duplicate alerts/kills. Keys `pam_lfc`/`pam_ana` (distinct from `pam_mig`/`pam_br`). memstore always runs fn (single process). Contract test + a live-Postgres mutual-exclusion test |
| 2026-07-22 | **CI static analysis + vuln scanning.** Added `staticcheck` and `govulncheck` as CI gates (both clean at introduction). Fixed the staticcheck findings surfaced: `archgen` uses `parser.ParseFile` per file instead of the deprecated `parser.ParseDir`; the two proxy accept-loops document their deliberate `net.Error.Temporary()` use with `//lint:ignore SA1019`; the DB-proxy startup switch binds the type-switch variable (drops a redundant assertion); a test passes `context.TODO()` instead of a nil context. No behavior change |
| 2026-07-22 | **Security gap-analysis hardening pass** (see `docs/SECURITY-GAPS.md`). Authorization: `auth.CanConnectTarget` now takes `safeScoped` — a target in a safe with no matching grant is default-DENY (was open); all 5 call sites pass `target.SafeID != nil`. The DB proxy now rejects MFA enroll-only sessions (was a `PAM_MFA_REQUIRED` bypass for postgres targets) and reconstructs the full multi-group principal for the SSH grant check (`ext["roles"]`). Audit: secret-delivery is fail-closed — `mustAudit`/`mustAuditAs` (reveal/checkout/app-secret → 503) and `appendAuditErr` (both proxies audit `session.start` before decryption). Break-glass is now audited on the `authenticated` middleware (`/me`, `/logout`, `/mfa/*`). Transport: `PAM_DB_UPSTREAM_CA`/`_TLS_VERIFY` verify the upstream PostgreSQL cert fail-closed; SCRAM server-signature is verified; `PAM_REQUIRE_HTTPS`/`PAM_REQUIRE_DB_CLIENT_TLS` refuse plaintext; PostgreSQL `FunctionCall` fast-path is audited. Brute-force: `PAM_PROXY_AUTH_RATE_LIMIT` throttles SSH/DB proxy auth; `PAM_TRUSTED_PROXY_HOPS` makes the API limiter XFF-aware. Sessions: admin `GET /api/login-sessions` + `POST /api/login-sessions/revoke` (`CapManageUsers`), and identity reconcile revokes directory-disabled sessions (store gains `ListSessions`/`DeleteSessionsByUsername`). `-rotate-kek` supports any KEK provider via `PAM_NEW_KEK_*` and audits `vault.kek_rotated`; `PAM_API_KEY` has a 16-char floor (non-demo, `PAM_ALLOW_WEAK_API_KEY` override); healthcheck matches the TLS scheme. IaC: default-deny NetworkPolicy (k8s + gated Helm), pinned `:latest` images. New audit vocab `proxy.auth_rate_limited`/`session.revoked`/`vault.kek_rotated`; new env vars listed in §4. Tests added for each; `go test -race`, `vet`, `gofmt` green |
| 2026-07-22 | Repo root decluttered: the Docker/compose/env files moved to `deploy/docker/` (`Dockerfile`, `Dockerfile.pkcs11`, `docker-compose.yml`, `.env.example`; compose `build.context` still the repo root) and the SOPS config to `deploy/.sops.yaml` (regexes relative to `deploy/`; encrypt with `--config deploy/.sops.yaml`, decrypt/CI unaffected). CI/release workflows, `.dockerignore` and all docs updated. The root now holds only `go.mod`/`go.sum`, `README*`, `ROADMAP.md`, `LICENSE`, `CLAUDE.md`, `.dockerignore`, `.gitignore`. No code change |
| 2026-07-21 | Phase 24 console: a 5250 screen for the application-secrets API — menu 15 *Work with application secrets* (mint/revoke apps, one-time token) + per-app *Work with secret grants* (grant/revoke credentials), keyboard-first, tolerating the API being disabled. No new routes/schema (uses the Phase 24 API) |
| 2026-07-21 | Phase 24 (Tier-4 application-secrets API) + **keyboard-first portal**. New store types `AppKey`/`AppSecretGrant` (migration `0017`: `app_keys`, `app_secret_grants`, both FKs cascade) + store methods (`CreateAppKey`/`GetAppKeyByTokenHash`/`ListAppKeys`/`DeleteAppKey`/`GrantAppSecret`/`ListAppSecretGrants`/`DeleteAppSecretGrant`/`AppMayAccessCredential`). Opt-in via `PAM_APP_SECRETS_ENABLED`: `appAuth` resolves an application bearer key (SHA-256-hash lookup, disabled = 401), `GET /v1/app-secrets/{id}` returns a **granted** credential's secret JIT (default-deny → `app.secret_denied`/403; `ssh_ca` → 422), admin routes `POST/GET/DELETE /v1/apps[...]` (`CapManageUsers`) and grants `POST/DELETE /v1/apps/{id}/grants[...]` (`CapRevealSecret` — you can only delegate a secret you could reveal). New audit vocab `app.create`/`revoke`/`grant`/`grant_revoked`/`secret_retrieved`/`secret_denied`. **Portal keyboard-first** (`web/static/index.html`): `render()` focuses each screen's primary field (`focusPrimary`), Esc = back/cancel, ↑/↓ move between subfile option cells, a persistent `.khint` documents the shortcuts — look unchanged. Store contract + end-to-end API tests |
| 2026-07-21 | Phase 22/23 review fixes: **analytics re-alert cooldown** — the per-actor alerted-set now stores `{score, time}` and is pruned after `PAM_ANALYTICS_WINDOW_MIN`, so a sustained/recurring high-risk incident re-alerts (and re-kills) instead of being suppressed forever, and the map (keyed by attacker-controllable actor names from auth-failure events) can't grow unbounded; **off-hours timezone** — the off-hours signal is evaluated in `PAM_ANALYTICS_TIMEZONE` (default UTC) rather than the UTC audit timestamp's hour; **`?window_min=` cap** — the risk endpoint clamps the window to 7 days so one request can't score the whole audit table; **ssh_ca reveal/checkout** — reveal/checkout and the broker `ssh_exec`/`reveal_credential` tools now refuse a Zero Standing Privilege credential with a 422 instead of decrypting its empty secret (a 500 + a misleading `credential.decrypt_failed`); **ZSP cert TTL** upper-bounded at 24h so a long TTL can't silently become a standing credential. Regression tests added |
| 2026-07-21 | Phase 23 (privileged threat analytics): the second Tier-3 gap. New `internal/analytics` — a deterministic, explainable risk scorer over the audit trail (`Score` is pure: no clock/IO/model). Named signals (`break_glass`, `command_blocked`, `auth_failure`, `off_hours`, `decrypt_failure`, `high_velocity`) with configurable weights, per-signal caps and level thresholds; a single break-glass access reaches high. API `GET /api/analytics/risk` (`CapReadAudit`, `?min_level`/`?window_min`) scores the recent window via `store.ExportAudit`; `RunAnalyticsWorker` (`PAM_ANALYTICS_INTERVAL_MIN`) flags a **newly elevated** high/critical actor (`analytics.risk_flagged` + alert) and, with `PAM_ANALYTICS_AUTO_KILL`, terminates a critical actor's live sessions (new `session.Registry.KillByActor`, audit `analytics.auto_response`). A per-actor high-water mark suppresses steady-state re-alerting. New env `PAM_ANALYTICS_*`; `api.Options.Analytics/AnalyticsWindow/AnalyticsAutoKill`. Engine unit tests + API worker/endpoint tests |
| 2026-07-21 | Phase 22 (Zero Standing Privilege): the first Tier-3 gap. New `internal/sshca` — an SSH certificate authority that mints a short-lived user certificate **just-in-time** per session (fresh ephemeral keypair, one dial, then discarded) so an account has **no standing secret**. A `secret_type="ssh_ca"` credential stores nothing (`SecretEnc` empty); `proxy.dialUpstream` branches on it to mint a cert signed by the CA instead of decrypting, and fails closed (`session.error`) when no CA is configured. `PAM_SSH_CA_KEY` (persistent, generated on first use) + `PAM_SSH_CERT_TTL_MIN` (default 2m); `GET /api/ca/ssh` (`CapReadInventory`) publishes the CA public key + `TrustedUserCAKeys` install hint. Audit `session.cert_issued` (serial/principal/valid-before/key-id, never the key). Reconcile reports `ssh_ca` as `unsupported`; post-session rotation and the lifecycle worker skip it. `proxy.Config.CA/CertTTL`, `api.Options.CA`. `internal/sshca` unit tests + an end-to-end cert-only-upstream ZSP proxy test (no password auth exists) + without-CA fail-closed + credential/endpoint API tests. Infra-bound Tier-3 leftovers catalogued in `docs/EXTERNAL-INFRA-GAPS.md` |
| 2026-07-21 | Phase 21 (richer approval workflows): multi-tier chains + scheduled windows + mandatory reason on the access-request engine (migration `0016`: `required_approvals`, `approved_by`, `not_before`). **N-of-M** — `decideAccessRequest`'s approve path accumulates DISTINCT approvers into `approved_by` via the new `store.SetApprovalState`; the request stays `pending` (audit `access.approve_partial`) until `RequiredApprovals` is met, then `approved` (double-approval 409, self-approval still 403). **Scheduled window** — `not_before`/`not_after`; `HasActiveApproval` now also requires `not_before <= now`. **Mandatory reason** — `PAM_REQUIRE_REASON`. New env `PAM_APPROVALS_REQUIRED`/`PAM_REQUIRE_REASON` (via `api.Options`). Completes Tier-2. One-time access deferred (needs a consume hook in every connect gate). Store contract + end-to-end API tests |
| 2026-07-21 | Phase 20 (ITSM / ticketing gate): "no access without an approved change ticket". New `internal/ticket` (no new dependency): a `Validator` with two optional, composable checks — a regex format (`PAM_TICKET_PATTERN`) and a webhook (`PAM_TICKET_VALIDATE_URL`, `POST {"ticket":…}` → 2xx = valid). `createAccessRequest` accepts a `ticket`: mandatory when `PAM_REQUIRE_TICKET` (422 otherwise), validated when a validator is configured (422 + `access.ticket_rejected` on failure), and recorded on the request + in the `access.request` audit (`store.AccessRequest.Ticket`, migration `0015`). Wired via `api.Options.TicketValidator`/`RequireTicket`. Fake-webhook API test + a `ticket` unit test |
| 2026-07-21 | Phase 19 (access certification campaigns): the first Tier-2 gap. `POST /api/campaigns` snapshots the current access grants — every target grant + every safe member — as `campaign_items` (migration `0014`: `campaigns`, `campaign_items`). `POST /api/campaigns/{id}/items/{iid}/decision {certify\|revoke}` records the attestation; a **revoke deletes the underlying grant** (`DeleteTargetGrant`/`DeleteSafeMember`, no-op if already gone), `…/close` closes it (further decisions 409). Management is `CapManageUsers`, reading `CapReadAudit` (an auditor reviews without changing access) — no new capability. New store types `Campaign`/`CampaignItem`; audit vocab `certification.campaign_created`/`item_certified`/`item_revoked`/`campaign_closed`. Store contract + end-to-end API test |
| 2026-07-21 | Phase 18 (Conjur secret sourcing): pamv1 can source its **own** bootstrap secrets from CyberArk Conjur at startup — the runtime-broker alternative to the SOPS GitOps sealing (Phase 14), which stays the default. New `internal/conjur` (hand-rolled over two REST endpoints, **no new dependency**): `New`/`Authenticate`/`Get` with `authn-api-key` **and** `authn-jwt` (a Kubernetes projected SA token — no bootstrap secret in Git), TLS with an optional CA bundle. `conjur.SourceEnv(ctx)` runs in `main` **before `config.Load`**: when `PAM_CONJUR_URL` is set it fills any empty bootstrap `PAM_*` (`master-key`/`api-key`/`database-url`/`break-glass-key-hash`/`broker-audit-key`/`_sign-seed`) from `PAM_CONJUR_POLICY_PREFIX/<name>`; an explicit env value wins, a 404 variable is skipped, a configured-but-unreachable Conjur is fail-loud. Same philosophy as the pluggable KEK (externalize the root of trust), now for the secret values. New env `PAM_CONJUR_*`; `deploy/k8s/conjur/` (policy + authn-jwt Deployment + README). Fake-Conjur tests; no schema/wire-format/audit-vocab change |
| 2026-07-21 | Phase 17b (dependent-account propagation): a credential declares its consumers — Windows Services / Scheduled Tasks / IIS App Pools (migration `0013`: `credential_dependencies`, cascades on credential delete). After `rotateCredential` persists the new secret, `propagateDependencies` updates each consumer over WinRM (`sc.exe config` / `schtasks /Change /RP` / `appcmd …processModel.password`) with the new secret, so auto-rotating a service account doesn't break the services it runs. A propagation failure never fails the (already-persisted) rotation; each consumer is audited `credential.dependency_updated`/`credential.dependency_failed` — the secret is injected into the command but never audited or recorded. API `POST/GET /api/credentials/{id}/dependencies` + `DELETE …/{did}`; new `store.CredentialDependency`. Completes Phase 17 (all four Tier-1 competitive-coverage gaps shipped) |
| 2026-07-21 | Phase 17a (safes / vault containers): named containers group targets and delegate access (migration `0012`: `safes`, `safe_members`, `targets.safe_id`). A **safe member may connect to every target in the safe** — an authorization path alongside per-target grants — surfaced by the new `store.EffectiveTargetGrants(targetID)` = direct grants ∪ safe-member-derived grants; the six connect gates (SSH proxy, DB proxy, RDP, two broker-tool checks, `gateCredentialAccess`) now call it instead of `ListTargetGrants`. `auth.SubjectMatches` factored out of `CanConnectTarget` and reused for safe membership + delegated management. API `POST/GET /api/safes`, `DELETE /api/safes/{id}`, `GET/POST /api/safes/{id}/members`, `DELETE /api/safes/{id}/members/{mid}`, `PUT /api/targets/{id}/safe`; member management is open to inventory readers but gated by `canManageSafe` (a global target manager **or** a `can_manage` member of that safe — delegated administration). New `store.Safe`/`SafeMember`, `Target.SafeID`; audit `safe.create`/`safe.delete`/`safe.member.add`/`safe.member.remove`/`target.safe_set` |
| 2026-07-21 | Phase 16 (live monitoring + command control): supervised sessions. **Live monitoring** — a `session.Hub` (`internal/session/hub.go`) fans out recorded output keyed by session id; `GET /api/sessions/{id}/stream` (`CapReadAudit`) streams it as Server-Sent Events so a supervisor watches an SSH or PostgreSQL session live (non-blocking fan-out; audited `session.monitor`). The proxy tees output via `Proxy.teeLive`/`liveWriter`; `statusWriter` gained a delegating `Flush` so the SSE handler can push through the middleware chain. **Command control** — a `proxy.CommandGuard` (regex denylist from `PAM_COMMAND_DENY_FILE`) blocks a command before it reaches the target: SSH `exec` (`pumpRequests`' `onExec` now returns a forward/veto bool), each WinRM line (`winrmRun`), and each PostgreSQL `Query`/`Parse` (the DB relay now serializes all client-facing writes under one mutex — pgproto3 is not concurrency-safe — and refuses a simple query gracefully / an extended statement fail-closed). New audit vocab `command.blocked`, `session.monitor`. Interactive-shell filtering, WinRM live streaming, and an in-portal viewer are documented follow-ons |
| 2026-07-20 | Phase 15 (database session proxy): a second listener (`internal/proxy/dbproxy.go`, `PAM_DB_ADDR`, default off) extends the JIT chokepoint to **PostgreSQL**. Speaks the frontend/backend wire protocol via `pgproto3` (vendored with pgx); an operator runs `psql user=<dbcred>@<target>` with their PAM key as the password. Runs the **same authorization gates as the SSH proxy** — `CapConnect`, per-target grants (`CanConnectTarget`), protocol allowlist, 4-eyes approval — then decrypts JIT and authenticates upstream with the vaulted secret (trust / cleartext / MD5 / **SCRAM-SHA-256** via `x/crypto/pbkdf2`, best-effort upstream TLS; optional operator-leg TLS). Each `Query`/`Parse` is a `db.query` audit event and a recorded line (asciicast, hash-chained); sessions appear in the live registry (protocol `postgres`) with kill + post-session rotation. Target protocol `postgres` added to `validProtocol`. Shared proxy helpers (`lookupTargetCred`/`jitDecrypt`/`appendAudit`/`recoverPanicLog`) factored out of `proxy.go` so both listeners reuse the security-critical resolve/decrypt/audit path. New audit vocab `db.session.{start,end,denied,error}` + `db.query`. Proven by an in-process fake-upstream test that accepts only the vaulted secret |
| 2026-07-20 | Deferred-set follow-ups (post-review). **Multi-group role union** — a directory principal can now carry several roles (`Principal.Roles`); `Can`/`CanConnectTarget`/`CapabilityNames` evaluate the **union** of their capabilities (LDAP/Entra group→role maps that overlap no longer silently collapse to one role), persisted across a session as `sessions.roles` (migration `0011`) and re-split on resolve. **Agent revocation at approval time** — a parked `require_approval` call is re-validated before it executes (`broker.WithRevalidator`): a static agent key revoked/disabled since parking (`store.GetAgentKey` by id) or an expired SVID (`agentid.Identity.ExpiresAt`) is refused, not run. **Chain can't fork under concurrent writers** — broker-audit append is now `AppendBrokerAuditLinked`: the store reads the current head and inserts the linked event as **one atomic step under a Postgres advisory lock** (`brokerChainLockKey`; the migration-lock idiom), so an old+new pod overlapping during a rolling deploy — or HA replicas — serialize instead of forking the keyed-HMAC chain (the in-process head is now only advisory). **Policy numeric match** — a JSON-number argument (float64) stringifies in plain decimal (`10000000`, not `1e+07`), so an `eq`/`in` on a large integer can't silently never match. Each carries a regression test |
| 2026-07-20 | Whole-codebase code-review fixes (15 findings + sweep). **Authz:** `PUT/DELETE /api/config` require a built-in admin (`Principal.IsAdmin`); reveal + checkout enforce target grants + four-eyes (`gateCredentialAccess`); checkin verifies the holder; `Covers` treats a built-in admin as unconstrained. **Broker:** `Decide` redacts a `Sensitive` reveal (approver gets status, agent resumes once); `getToolCall` strips `ResumeToken`; self-approval refused; `Tool.Capability()` enforced; parked-approval TTL sweep. **KEK rotation:** re-wraps encrypted settings and preserves `last_totp_step` (`ListMFAEnrollments` now selects it); loud `credential.rotate_orphaned`. **Recording/audit:** proxy records the exec command (WinRM + SSH-exec `ssh.exec`); RDP break-glass uses `noteBreakGlass`; `proxy.auth_failed` audited; audit export digest is over the exact artifact bytes + records the filter. **Config:** `PAM_LDAP_INSECURE_SKIP_VERIFY` no longer overridable; fail-loud on negative rate limits and partial email alerting. **Robustness:** rate-limiter eviction; break-glass share-poison rejection; proxy goroutine panic barrier; migrations on the locked conn; mfa body caps; discovery honors the protocol allowlist; policy rejects a typo'd operator; memstore token/audit parity. **Vault/auth:** transit/KMS `Unwrap` key-length check; bootstrap key compared by SHA-256; LDAP names lower-cased; `capCount` sentinel. Every fix carries a regression test |
| 2026-07-20 | Phase 12/13 hardening (multi-agent gap review): **privilege-escalation guard** — `createUser`/`createProfile` now enforce `Principal.Covers` (you cannot grant a role/profile with capabilities you don't hold), so a delegated `manage_users` can't mint an admin or forge a max-cap profile. **Reveal confinement** — a `reveal_credential` result is `Sensitive`; `GET /v1/tool-calls/{id}` is now a status-only poll (never re-serves the secret) and the plaintext is not retained in the poll cache. **Resume** peeks the token and refuses to spend it while the call is still pending. **Bounded state** — `broker.parked` capped (`maxParked`, fail-closed) and a `RunBrokerTokenGC` reaper (`DeleteExpiredBrokerTokens`) sweeps spent/expired `broker_tokens`. **SVID delegation** — every RFC 8693 `act.sub` must be in the trust domain (no spoofed accountable identity). **Hot-swap torn read** — login/OIDC snapshot `s.rt()` once; config notes corrected (the SSH proxy's protocol/approval gates apply on restart, not live). Store gains `PeekBrokerToken`/`DeleteExpiredBrokerTokens` (contract-tested); pgstore integration `TRUNCATE` covers the Phase 12/13 tables |
| 2026-07-20 | Phase 14 (SOPS-encrypted secrets): [SOPS](https://github.com/getsops/sops)+[age](https://age-encryption.org/) encryption for the Kubernetes secret manifest, keeping it in the IaC repo without leaking it. Repo-root `.sops.yaml` seals only `data`/`stringData` values (`encrypted_regex`) of `deploy/k8s/sops/secrets*.yaml`; `deploy/k8s/sops/` holds a **real encrypted example** decryptable with a committed throwaway demo key, an `apply.sh` (`sops -d \| kubectl apply -f -`, plaintext never on disk), a `verify.sh`, and a README covering Flux/Argo/helm-secrets. New CI `sops` job runs `verify.sh`. `.gitignore` blocks real age keys and non-example sealed files. No Go code / schema / wire-format change |
| 2026-07-20 | Phase 13 (SPIFFE SVID + delegation, increment D): `internal/agentid` gains an `SVIDVerifier` that validates SPIFFE JWT-SVIDs against a **file** trust-domain JWKS (`PAM_BROKER_TRUST_DOMAIN_JWKS`) — RS256/ES256/EdDSA (stdlib `crypto/rsa`·`ecdsa`·`ed25519`), requiring `sub = spiffe://<PAM_BROKER_TRUST_DOMAIN>/…`, `aud = PAM_BROKER_AUDIENCE`, and a present, unexpired `exp` (all fail-closed). Nested RFC 8693 `act` claims become an `ActorChain` capped at `PAM_BROKER_MAX_DELEGATION_DEPTH`. A `MultiVerifier` composes it with the static-key verifier so `agentAuth` accepts either. Mirrors the `internal/oidc` JWT/JWKS machinery; no new crypto dependency. Deferred (documented): live SPIRE workload attestation and an RFC 8693 token-exchange minting endpoint *(the latter shipped in Phase 57 — it required no external STS)* |
| 2026-07-20 | Phase 13 (MCP server, increment C): new `internal/mcp` — a hand-rolled JSON-RPC 2.0 core (stdlib only: `Request`/`Response`/`Error`, a `Dispatcher` method table) — served at `POST /mcp` behind the same `agentAuth` as REST. Methods `initialize`, `tools/list` (from the broker registry, tool input maps rendered as JSON Schema), `tools/call`, `ping`, and `broker/resume` route through the **same `broker.ProcessCall`/`Resume`**, so an MCP tool call is policy-gated, JIT-injected, single-use-resumed, and audited identically to REST (`broker.tool_call … via:mcp`). Notifications (no id) get no response; unknown methods return `-32601`. Proven at parity with an in-process test |
| 2026-07-20 | Phase 13 (broker toolset, increment B cont.): five more agent tools alongside `winrm_exec` — `ssh_exec` (one-shot SSH command via the new `rotate.SSHConnector.Exec`; JIT credential, only output returns), `list_targets`/`list_credentials` (metadata only — no secret ever), `rotate_credential` (reuses `rotateCredential`; the new secret stays vaulted), and `reveal_credential`, the deliberate secret-returning tool shipped **default-deny** (no rule allows it unless an operator adds one) that also honors `PAM_REVEAL_DISABLED` and keeps the plaintext out of every audit record. Shared gates refactored into `authorizeAgentTarget`/`authorizeAgentCredential` (protocol allowlist + target grants + four-eyes). New audit action `ssh.exec` |
| 2026-07-20 | Phase 13 (broker approval + resume + tokens, increment B): the `require_approval` policy effect now **parks** a tool call for a human decision instead of dead-ending. The broker mints a **single-use resume token** (`broker_tokens`, migration `0010`; only its SHA-256 JTI is stored, spent by an atomic `UPDATE … RETURNING`) and alerts an approver. Operator routes `GET /v1/approvals` + `POST /v1/approvals/{id}/decision` (`CapApprove`) — on approve the broker **executes the parked call server-side** (JIT injection; the human approval satisfies the target four-eyes gate via `broker.WithApproved`), on reject it denies. The agent collects the result once via `POST /v1/tool-calls/{id}/resume` (spends the token). Per-agent **rate limit** (`PAM_BROKER_RATE_PER_MIN`) in `agentAuth` and an **argument-size cap** (`PAM_BROKER_MAX_ARG_BYTES`) in `ProcessCall`. New env `PAM_BROKER_TOKEN_TTL_MIN`; audit vocab `broker.approval.granted`/`broker.approval.denied`/`broker.tool_call.resumed` |
| 2026-07-20 | Phase 12 (console + IaC export, increment D): 5250 console screens for the CyberArk/Wallix admin surface — *Work with permission profiles* (menu 12), *System configuration* (menu 13, set/clear identity/SSO/policy overrides), and *Effective config & backend health* (menu 14) with a one-key export of the console-set overrides back to IaC. New read-only routes `GET /api/config/effective` (backend status from the live runtime snapshot) and `GET /api/config/iac?format=env\|helm\|terraform` (secrets rendered as secret-store placeholders, never plaintext). The user add-screen role picker now loads custom profiles live. No new audit vocab (both new routes are read-only) |
| 2026-07-20 | Phase 12 (configuration hot-swap): `PUT`/`DELETE /api/config` now take effect **without a restart**. The server holds the runtime-overridable settings (identity backends, SSO, operational policy) in one immutable `runtimeConf` snapshot behind an `atomic.Pointer`, read through `s.rt()`; a `Reconfigure` closure (wired by `main` from the pristine env baseline + current DB overrides) rebuilds it and `applyReconfigure` swaps it atomically. A rejected reconfigure (e.g. an unreachable directory) rolls the offending override back so it can't also break the next restart. Networking/TLS listeners stay restart-bound (env-only) |
| 2026-07-20 | Phase 12 (custom-profile RBAC): named capability sets (`profiles` table, migration `0009`) assignable to users as an alternative to the four built-in roles. `auth.Principal` gains a resolved `Caps` set + `Can`/`CapabilityNames` — built-in roles fall through the `roleCaps` matrix unchanged; the resolver resolves a non-built-in user/session role as a custom profile (`Resolver.WithProfiles`, `ParseCapabilities`). Authorization now checks `principal.Can(cap)` everywhere (api `authz`, RDP, and the SSH proxy via a `can_connect` handshake flag). Admin API `POST/GET /api/profiles` + `DELETE /api/profiles/{id}` (`CapManageUsers`); `createUser` accepts a profile name; audit `profile.create`/`profile.delete` |
| 2026-07-20 | Phase 12 (configuration subsystem, increment A): DB-persisted, editable `PAM_*` overrides for the identity backends, SSO, and operational policy. New `settings` table (migration `0008`) + `store.Setting`/`PutSetting`/`GetSetting`/`ListSettings`/`DeleteSetting`; secret settings (bind password, client secrets) are vault-encrypted (`store.ConfigAAD`). A whitelist (`config.OverridableKeys`/`ApplyOverrides`) overlays stored values onto the env-derived config at startup — bootstrap/transport keys (DB URL, master key, listen/TLS, KEK) stay environment-only. Admin API `GET/PUT /api/config` + `DELETE /api/config/{key}` (`CapManageUsers`; secrets masked); audit `config.update`/`config.revert`. Hot-swap (no restart), the custom-profile RBAC engine, and console screens land in later increments |
| 2026-07-20 | Phase 13 broker hardening (self-review fixes): agents now obey the same **approval gate** as humans (`winrm_exec` calls `enforceApproval`); a **`role:agent` target grant** is creatable (`auth.ParseGrantRole`) so targets can be scoped to agents; the broker records a tamper-evident **`broker.tool_call.requested`** event *before* executing and **fails closed** if the chain is unavailable (no more silent unaudited executions); `agentAuth` now `withPrincipal`s the agent so the `winrm.run` audit event is attributed to the agent, not "unknown"; agent keys are revocable/listable — `GET /v1/agents` + `DELETE /v1/agents/{id}` (new audit action `agent.revoke`) |
| 2026-07-20 | Phase 13 (AI-agent access broker, increment A): opt-in via `PAM_BROKER_POLICY_FILE`. New packages `internal/policy` (YAML rule engine: eq/not/in/not_in, first-match-wins, implicit deny, scope templating), `internal/agentid` (static agent-key verifier; `RoleAgent`+`CapCallTool`), `internal/auditchain` (keyed-HMAC per-event hash chain + ed25519 signed head), `internal/broker` (Tool registry + `ProcessCall`). New store types/tables `agent_keys` + `broker_audit_events` (migration `0007`). `winrm_exec` tool runs over the refactored `Server.execWinRM` (JIT decrypt → run → result; credential never returned). Routes `POST /v1/tool-calls`, `GET /v1/tool-calls/{id}`, `POST /v1/agents`, `GET /v1/audit[/verify|/head]` (HTTP-200-with-`status` error model; agent Bearer auth via `agentAuth`). New env: `PAM_BROKER_POLICY_FILE`, `PAM_BROKER_AUDIT_KEY`, `PAM_BROKER_AUDIT_SIGN_SEED`. Audit vocab: `broker.tool_call.{executed,denied,pending_approval,failed}`, `agent.create`. Approval/resume, MCP, and SPIFFE land in later increments (see ROADMAP Phase 13) |
| 2026-07-20 | Code-derived architecture diagrams: `cmd/archgen` regenerates `docs/ARCHITECTURE-DIAGRAMS.md` (Mermaid package-dependency graph from imports, domain ER model from `store` structs, REST route→capability map from the mux). Deterministic output; a CI step (`go run ./cmd/archgen && git diff --exit-code`) fails if it drifts, so the diagrams stay current on every change. Wired via `go:generate` |
| 2026-07-20 | Phase 11: management console — the 5250 portal grows into a role-aware console over every existing API. New `GET /api/me` (`authenticated`) returns the caller's identity + stable capability names (`auth.Capability.String`/`Role.Capabilities`) driving the menu. `internal/web/static/index.html` gains screens for targets+grants, credentials (reveal/check-out/rotate/reconcile), check-out/check-in, active sessions (+kill), access requests (approve/deny/file), users & roles (+directory reconcile), MFA self-service, discovery, reconciliation, audit (filter + CSV export), break-glass unseal — still one nonce-CSP `//go:embed`'d vanilla-JS page. Phase 12 (planned) adds the hybrid config subsystem + custom-profile RBAC |
| 2026-07-19 | Second audit pass (gap-finding): proxy joins upstream stderr into the recording before hashing (was a lost-write race) and rejects non-proxyable protocols before decrypt; expired checkout leases are rotated before a re-checkout reuses the secret (memstore/pgstore parity); TOTP replay guard fails **closed** on a store error; OIDC state cookie honors `X-Forwarded-Proto`; `createCredential` rollback uses a cancel-detached context; RDP kill cancels a shared context so a wedged tunnel tears down; syslog/email alert deliveries are timeout-bounded; portal is served under a per-request **nonce-based CSP** (+ `base-uri`/`form-action`/`frame-ancestors`/`object-src`), `esc()` covers `'`, `viewCred` cleared on nav; `pam-server -healthcheck` for the shell-less image; Docker (`.dockerignore`, numeric UID, HEALTHCHECK), compose (read-only rootfs, cap_drop, `/data` chown init), Helm/k8s (`secret.create=false` default + guard, numeric `runAsUser`/`fsGroup`, startupProbe, image pin, port 8080), Terraform (RDS `manage_master_user_password`, encrypted-backend requirement, var validation), and CI (GitHub Actions pinned to commit SHAs) hardening |
| 2026-07-19 | Breaking (pre-1.0): the vault token format is `v2:` with per-credential AAD (`target:%d/cred:%d`); there is no in-place migration from earlier `v1:`/per-target-AAD ciphertext or the pre-GCM PKCS#11 wrap, so a deployment carrying vaulted secrets across the change must re-enter them. Fresh deploys are unaffected. |
| 2026-07-19 | Security/correctness hardening pass (repo-wide audit): strict config parsing (fail-loud booleans/ints, TLS all-or-nothing, quorum-threshold validation); login/MFA fixes (MFA fail-closed on store error, step-up to change a confirmed factor, `login.failed` audit, target-scoped grant delete, rate-limited oidc/start); Entra tenant (`tid`) pinning, required token `exp`, LDAPS enforced; rotation robustness (`rotate_started` on all paths, detached persist, worker panic-recover + failure audit, ssh_key reconcile via key auth, non-clobbering `RotateKey`, WinRM `net user /y`, exec deadline); resumable KEK rotation + Vault-Transit HTTPS; atomic checkout exclusivity (partial UNIQUE index + expired-lease auto-close) and store parity fixes; migrations under advisory lock + `audit_events(ts)` index; guacd handshake deadline + bounded alloc; alert CR/LF sanitization; `PAM_REQUIRE_RECORDING`, stderr recorded |
| 2026-07-19 | Proxy: post-session rotation callbacks are tracked so a graceful shutdown drains them (not killed mid-rotation); `RotateCredentialByID` audits `credential.rotate_started` before the external password change so a crash leaves a trail |
| 2026-07-19 | Proxy: `Serve` retries transient `Accept` errors (e.g. fd exhaustion) with capped backoff instead of tearing the listener down permanently |
| 2026-07-19 | Proxy hardening: decrypt the JIT secret only after all authz gates; record target stderr into the asciicast (hash now covers stderr); audit `session.record_failed` and optionally refuse (`PAM_REQUIRE_RECORDING`) when a session can't be recorded |
| 2026-07-19 | Proxy: graceful shutdown flushes session audit — `main` awaits the (bounded) proxy drain on SIGTERM before closing the store, and closing audits (`session.end`/`session.record`) use `auditClosing` (detached from the cancelled ctx) so they are no longer dropped mid-drain |
| 2026-07-19 | Proxy: session teardown keyed on the connection lifecycle — stdin half-close only `CloseWrite`s (batch/piped/`ssh -n` output + `exit-status` no longer truncated), `handleConn` closes the upstream when the client is gone; client→upstream request pump joined so the `exec`/`shell` reply flushes before teardown (fixes an EOF flake); `Serve` shutdown drain bounded by force-closing active connections |
| 2026-07-19 | Phase 10: Postgres HA (CloudNativePG `Cluster`, `deploy/k8s/postgres-cnpg.yaml`), cloud-Postgres Terraform (`deploy/terraform/cloud-postgres`, AWS RDS), SLSA build-provenance attestation in `release.yml` |
| 2026-07-19 | Phase 8: SSH jump-host / bastion connector (`proxy.Config.Jump`, `PAM_SSH_JUMP_*`) — targets reached via a `direct-tcpip` tunnel through the bastion |
| 2026-07-19 | Phase 7: identity reconciliation (`POST /api/identity/reconcile`, `auth.DirectorySource.UserStatus`, revokes disabled directory users) + AD `ChangePassword` primitive (`unicodePwd`) |
| 2026-07-19 | Proxy: interactive WinRM command loop through the SSH proxy (`proxy.Config.WinRMRunner`, `PAM_PROXY_WINRM`) — each operator line runs as a WinRM command, recorded; stateless-per-line |
| 2026-07-19 | OT: read-only observer sessions (`<login>+observe` → drop operator keystrokes, refuse exec/subsystem; `session.start … mode:observer`) |
| 2026-07-19 | OT: protocol allowlist (`PAM_ALLOWED_PROTOCOLS`) enforced at create-target and every connect path (WinRM/RDP handlers + SSH proxy) |
| 2026-07-19 | Alerting: email + syslog channels (`alert.Email`/`alert.Syslog`/`alert.Multi`, `PAM_ALERT_SYSLOG`/`PAM_ALERT_EMAIL_*`) alongside the webhook |
| 2026-07-19 | Lifecycle: forced rotation after a proxied SSH session ends (`proxy.Config.OnSessionEnd` → `Server.RotateCredentialByID`, `PAM_ROTATE_AFTER_SESSION`) |
| 2026-07-19 | Lifecycle: `ssh_key` credential rotation (`rotate.GenerateSSHKey` + `SSHConnector.RotateKey` / `KeyRotator`) — generates a keypair and replaces `authorized_keys` |
| 2026-07-19 | Tests: shared store conformance suite (`internal/store/storetest.RunStoreContract`) run by memstore and, in CI, against a live PostgreSQL (`PAM_TEST_DATABASE_URL`, a `postgres` service job) — verifies the pgstore SQL/migrations |
| 2026-07-19 | Hardening: Entra ROPC now validates the id_token RS256 signature against the tenant JWKS (`oidc.VerifyRS256`) instead of reading unverified claims |
| 2026-07-19 | Hardening: RDP now verifies the server certificate by default (`PAM_GUACD_RDP_SECURITY`/`PAM_GUACD_IGNORE_CERT`) instead of hardcoding `security:any`/`ignore-cert:true` |
| 2026-07-19 | Hardening: upstream SSH host-key pinning (`PAM_SSH_KNOWN_HOSTS`, `proxy.Config.UpstreamHostKey` + rotation connector) — no longer trusts any target key |
| 2026-07-19 | PKCS#11 HSM KEK provider (`vault/pkcs11.go`, build tag `pkcs11`; stub in the default build), `Dockerfile.pkcs11`, CI job against SoftHSM2, `PAM_KEK_PKCS11_*` |
| 2026-07-19 | HA: OIDC PKCE login state moved to the store (`store.PutOIDCState`/`TakeOIDCState`, migration `0004`) so the auth-code callback works on any replica; removed the in-memory `oidcPending` |
| 2026-07-19 | Phase 7 follow-ons: credential checkout/check-in leases (`store.Checkout`, migration `0003`, auto-rotate on return) + discovery (`internal/discovery`, `POST /api/discovery/scan`); `docs/REQUIREMENTS.md` (run specs) |
| 2026-07-19 | Phase 10: scale & ops — `internal/metrics` + `GET /metrics` (Prometheus), `GET /readyz` (`store.Ping`), `deploy/helm/pamv1` chart, `release.yml` (SBOM + cosign signing) |
| 2026-07-19 | Phase 9: NIS2 pack — `GET /api/audit/export` (`compliance_handlers.go`, `store.ExportAudit`, JSON/CSV, SHA-256 tamper digest), `docs/NIS2-COMPLIANCE.md` (Art. 21 matrix + Art. 23 export) |
| 2026-07-19 | Phase 8: OT adaptation — access-request approval workflow (`approval_handlers.go`, `store.AccessRequest`, migration `0002`, 4-eyes, enforced on proxy/WinRM/RDP), air-gap mode (`PAM_OT_AIRGAP`), `docs/OT-DEPLOYMENT.md` |
| 2026-07-19 | Phase 7: credential lifecycle — `internal/rotate` (SSH/WinRM `Rotator`/`Verifier`, strong password gen), `POST /api/credentials/{id}/rotate`, `/reconcile[?remediate]`, `GET /api/reconcile`, background worker (`scheduler.go`), store `RotateCredentialSecret` |
| 2026-07-19 | Phase 6: break-glass v2 — `shamir` (M-of-N quorum), `pam-server -split-key`, `POST /api/breakglass/unseal` (auto-expiring session), `alert` webhook on break-glass access/unseal; AWS KMS KEK (`awskms.go`) |
| 2026-07-19 | Phase 5 done: embedded versioned migrations (`pgstore/migrate.go`, `schema_migrations`, `migrations/0001_init.sql`) replacing the ad-hoc startup schema |
| 2026-07-19 | Phase 2: live session registry (`internal/session`) — `GET /api/sessions` + `DELETE /api/sessions/{id}` (kill-switch); proxy + RDP register live sessions |
| 2026-07-18 | Phase 2: per-target authorization (`store.TargetGrant`, `auth.CanConnectTarget`, `/api/targets/{id}/grants`, enforced in proxy/WinRM/RDP); reveal lockdown (`PAM_REVEAL_DISABLED`) |
| 2026-07-18 | Phase 5: vault KEK rotation (`internal/maint`, `pam-server -rotate-kek`); store `UpdateCredentialSecretEnc` + `ListMFAEnrollments` |
| 2026-07-18 | Phase 5: transport hardening — native HTTPS (`PAM_TLS_*`), security-headers middleware, per-IP auth rate limiting (`middleware.go`) |
| 2026-07-18 | Phase 4: NTLM WinRM auth (`PAM_WINRM_AUTH`); `guacd` package + `GET /api/targets/{id}/rdp` WebSocket tunnel (RDP via Guacamole with JIT injection); `PAM_GUACD_ADDR` |
| 2026-07-18 | Phase 3b hardening: `oidc` package (Authorization Code + PKCE, RS256 JWKS validation, discovery); `/api/auth/oidc/{start,callback}`; portal SSO token pickup; shared `auth.HighestRole`; `PAM_OIDC_*` config |
| 2026-07-18 | Phase 4: `winrm` package + `POST /api/targets/{id}/winrm` (JIT credential injection on Windows, transcript recording, `winrm.run` audit); `PAM_WINRM_*` config |
| 2026-07-18 | Phase 3b hardening: enforce-MFA policy (`PAM_MFA_REQUIRED`, enrollment-only sessions via `Session.Scope`/`Principal.EnrollOnly`) + single-use recovery codes (`/api/mfa/recovery-codes`) |
| 2026-07-18 | Phase 3b Entra: `EntraAuthenticator` (Azure AD, OAuth2 ROPC, app-roles/groups → role); `ChainAuthenticator`; `PAM_ENTRA_*` env; LDAP + Entra composable |
| 2026-07-18 | Phase 3b MFA: `mfa` package (TOTP RFC 6238); `store.MFAEnrollment` (vault-encrypted secret); `/api/mfa/*` self-service; `/api/login` enforces confirmed TOTP; portal Sign On OTP field |
| 2026-07-18 | Phase 3b: AD/LDAP login (`ldap.go`, `Authenticator`); login sessions (`store.Session`, `/api/login` + `/api/logout`); resolver session support; portal Sign On with user+password; `PAM_LDAP_*` env |
| 2026-07-18 | Vault envelope encryption + pluggable KEK (`kek.go`, `transit.go`); `v2:` token format; ctx-aware Encrypt/Decrypt; `local` (dev/test) + `vault-transit` (production) providers; `PAM_KEK_*` env |
| 2026-07-18 | Added `logging` package (per-service slog, json/text, `PAM_LOG_LEVEL`/`PAM_LOG_FORMAT`); api access log, proxy session logs, pgstore connect + query tracer; user/admin guides |
| 2026-07-18 | Phase 3a: `auth` package (roles admin/user/auditor/approver, capabilities, Resolver); `store.User` + user CRUD; API `authz` middleware + `/api/users`; proxy `CapConnect` gate; portal tolerates 403 |
| 2026-07-18 | Added `proxy` package (SSH gateway, JIT, recording, host key); `store.CredentialAAD`; proxy env vars |
| 2026-07-17 | Initial packages: config, vault, store(+mem/pg), api, web |
